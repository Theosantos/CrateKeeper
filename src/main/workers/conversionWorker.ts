// Stub — Task 6 replaces this with the real implementation.
// Present at Task 1 so electron-vite's rollup input resolves the entry and
// the production build emits `out/main/workers/conversionWorker.js` alongside
// scanWorker.js (RESEARCH Pitfall 1 carry-forward).
export {}
