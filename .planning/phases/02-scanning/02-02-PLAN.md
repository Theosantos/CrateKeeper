---
phase: 02-scanning
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/renderer/src/store/useScanStore.ts
  - src/renderer/src/store/useScanStore.test.ts
  - src/renderer/src/components/analyser/ScanToolbar.tsx
  - src/renderer/src/components/analyser/TagBadge.tsx
  - src/renderer/src/components/analyser/VirtualizedFileTable.tsx
  - src/renderer/src/components/analyser/VirtualizedFileTable.test.tsx
  - src/renderer/src/views/AnalyserView.tsx
  - src/renderer/src/assets/main.css
autonomous: false
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-05]
user_setup: []

must_haves:
  truths:
    - "Clicking 'Scanner' on the AnalyserView triggers window.djUtils.scan.start(rootFolder) and begins streaming rows into a virtualized list"
    - "Each row visibly shows path, format, bitrate (kbps), size (MB), sample rate, and duration (mm:ss) — SCAN-01 + SCAN-02"
    - "Each row shows three coloured badges G/B/K reflecting hasGenre/hasBpm/hasKey (filled when true, hollow when false) — SCAN-03"
    - "While a scan of thousands of files is running, the nav remains clickable and other tools (Convertir/Tagger) are still reachable — SCAN-05"
    - "Failed-parse rows still appear, greyed, with an 'Erreur' badge and the error message reachable on hover"
    - "Stop button cancels the running scan; status transitions to 'cancelled' and no further row events arrive"
    - "When no rootFolder is set, the Scanner button is disabled with an empty-state hint pointing to the folder picker"
  artifacts:
    - path: "src/renderer/src/store/useScanStore.ts"
      provides: "Zustand store: scanId, status, rows[], stats, error; start/cancel; subscribe/unsubscribe to scan events on start/stop"
      contains: "useScanStore"
    - path: "src/renderer/src/components/analyser/VirtualizedFileTable.tsx"
      provides: "Fixed-height virtualized table using @tanstack/react-virtual; renders ScannedFile rows"
      contains: "useVirtualizer"
    - path: "src/renderer/src/components/analyser/TagBadge.tsx"
      provides: "Three-letter G/B/K badge component (and 'Erreur' variant)"
      contains: "TagBadge"
    - path: "src/renderer/src/components/analyser/ScanToolbar.tsx"
      provides: "Scanner / Stop buttons + live progress strip (scanned N / elapsed)"
      contains: "ScanToolbar"
    - path: "src/renderer/src/views/AnalyserView.tsx"
      provides: "Composes ScanToolbar + VirtualizedFileTable; reads useAppStore.rootFolder; consumes useScanStore"
      contains: "VirtualizedFileTable"
  key_links:
    - from: "src/renderer/src/store/useScanStore.ts"
      to: "window.djUtils.scan"
      via: "start / cancel / onEvent"
      pattern: "window\\.djUtils\\.scan\\.(start|cancel|onEvent)"
    - from: "src/renderer/src/views/AnalyserView.tsx"
      to: "src/renderer/src/store/useScanStore.ts"
      via: "useScanStore() selectors"
      pattern: "useScanStore"
    - from: "src/renderer/src/components/analyser/VirtualizedFileTable.tsx"
      to: "@tanstack/react-virtual"
      via: "useVirtualizer({ count, getScrollElement, estimateSize })"
      pattern: "useVirtualizer"
    - from: "src/renderer/src/components/analyser/ScanToolbar.tsx"
      to: "src/renderer/src/store/useScanStore.ts"
      via: "store.start(rootFolder) / store.cancel()"
      pattern: "useScanStore"
---

<objective>
Build the renderer vertical slice that turns Plan 02-01's backbone into a working Analyser: a Zustand `useScanStore` that subscribes to `window.djUtils.scan.onEvent` and accumulates rows, a virtualized file table (@tanstack/react-virtual) that handles thousands of rows without DOM blow-up, G/B/K tag badges that reflect the SCAN-03 booleans, and a toolbar with Scanner / Stop + a live progress strip. Wire it into the existing AnalyserView placeholder from Plan 01-02.

Purpose: Completes SCAN-01/02/03/05 as user-observable behavior. After this plan, a real user clicks Scanner and watches the library populate live without the UI freezing — exactly the ROADMAP success criteria 1, 2, 3 for Phase 2.
Output: A scannable, navigable AnalyserView consuming the typed scan bridge; no direct window.djUtils calls in components (store mediates per T-1-04 invariant); virtualized table proven against a 5000-row synthetic dataset in unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scanning/02-RESEARCH.md
@.planning/phases/02-scanning/02-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/shared/ipc-types.ts
@src/renderer/src/store/useAppStore.ts
@src/renderer/src/views/AnalyserView.tsx
@src/renderer/src/assets/main.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useScanStore — Zustand store driving scan lifecycle from window.djUtils.scan</name>
  <files>src/renderer/src/store/useScanStore.ts, src/renderer/src/store/useScanStore.test.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Pattern 4 renderer Zustand sketch; Pitfall 5 MEDIUM batched IPC; Anti-Patterns — append per BATCH not per file)
    - .planning/phases/02-scanning/02-01-SUMMARY.md (final shape of window.djUtils.scan)
    - src/shared/ipc-types.ts (ScannedFile + ScanEvent + DjUtilsApi.scan from Plan 02-01)
    - src/renderer/src/store/useAppStore.ts (Phase 1 store pattern — exactly mirror this shape)
  </read_first>
  <behavior>
    - Test: initial state is { scanId:null, status:'idle', rows:[], totalFiles:null, durationMs:null, error:null }
    - Test: start(folder) calls window.djUtils.scan.start(folder), stores the returned scanId, sets status='running', resets rows to [], and subscribes via window.djUtils.scan.onEvent (mock returns a vi.fn() unsubscribe)
    - Test: when a 'rows' event arrives with 3 rows, the store appends them (rows.length === 3); a second event with 2 more rows yields rows.length === 5 (append, not replace — RESEARCH anti-pattern says batch in main, append once per batch in renderer)
    - Test: 'done' event sets status='done', totalFiles=N, durationMs=N, and calls the unsubscribe fn returned by onEvent
    - Test: 'cancelled' event sets status='cancelled' and unsubscribes
    - Test: 'error' event sets status='error', error=message, unsubscribes
    - Test: cancel() while running calls window.djUtils.scan.cancel(scanId) (does NOT directly set status — the 'cancelled' event drives the transition)
    - Test: start(folder) while a prior scan was 'running' first unsubscribes the old listener then starts fresh (defence-in-depth; the controller also enforces single-active-scan)
    - Test: rows event for a foreign scanId (not the current store.scanId) is ignored — important if backend emits a late event from a cancelled scan
  </behavior>
  <action>
    Create src/renderer/src/store/useScanStore.ts using zustand `create`. Type ScanStatus = 'idle' | 'running' | 'done' | 'cancelled' | 'error'. State per the behaviors above plus an internal `unsubscribe: (() => void) | null` field (private to the store).
    Implement start(folder): if status === 'running', call current unsubscribe() and reset rows. Then call `await window.djUtils.scan.start(folder)` → scanId. Set { scanId, status:'running', rows: [], totalFiles: null, durationMs: null, error: null }. Subscribe: `const off = window.djUtils.scan.onEvent((e) => handleEvent(e))`. Stash `off` as unsubscribe.
    handleEvent(e): if `e.scanId !== get().scanId` return (foreign event guard). Switch on e.type:
    - 'rows' → `set(s => ({ rows: [...s.rows, ...e.rows] }))` (single append per batch — RESEARCH anti-pattern guard against setState-per-file)
    - 'done' → `set({ status:'done', totalFiles: e.totalFiles, durationMs: e.durationMs })`; call unsubscribe() and clear it
    - 'cancelled' → `set({ status:'cancelled' })`; unsubscribe
    - 'error' → `set({ status:'error', error: e.message })`; unsubscribe
    Implement cancel(): if scanId != null call `window.djUtils.scan.cancel(scanId)`; do not transition status here — let the 'cancelled' event drive it.
    Implement reset(): clears everything back to initial state (used when user wants a clean slate before exporting CSV in Plan 02-03 if needed).
    The store reads window.djUtils.scan exclusively (T-1-04 invariant: no direct Node/electron import in renderer; carried from Phase 1 Plan 01-02).
    Create useScanStore.test.ts (jsdom project). Stub `globalThis.window.djUtils.scan` with vi.fn() shapes. For `onEvent`, return a `vi.fn()` unsubscribe AND expose the registered callback so the test can manually invoke it with synthetic ScanEvents to drive state transitions. Reset store state between tests.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/store/useScanStore</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/store/useScanStore` passes all 9 behaviors
    - useScanStore.ts reads only `window.djUtils.scan.*` (grep — no `import.*electron`, no `import.*node:`)
    - 'rows' handler does ONE setState per batch (test asserts via a vi.fn() store-set spy or via counting renders) — Pitfall 5 mitigation
    - Foreign scanId guard exercised (test fires a {type:'rows', scanId:'other', rows:[…]} and asserts rows.length unchanged)
    - cancel() does not set status directly — only the 'cancelled' event does
  </acceptance_criteria>
  <done>Zustand store covers SCAN-05 plumbing at the state-management level; tests prove append-per-batch and lifecycle transitions.</done>
</task>

<task type="auto">
  <name>Task 2: TagBadge + VirtualizedFileTable + ScanToolbar — view layer (SCAN-01/02/03 visual)</name>
  <files>src/renderer/src/components/analyser/TagBadge.tsx, src/renderer/src/components/analyser/VirtualizedFileTable.tsx, src/renderer/src/components/analyser/ScanToolbar.tsx, src/renderer/src/views/AnalyserView.tsx, src/renderer/src/assets/main.css</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Pattern 4 virtualized table; Anti-Patterns — no setState per file, virtualize for thousands of rows)
    - src/renderer/src/store/useScanStore.ts (from Task 1)
    - src/renderer/src/store/useAppStore.ts (rootFolder selector — gates the Scanner button)
    - src/renderer/src/assets/main.css (existing editorial dark-studio :root design tokens from Plan 01-02 — extend these tokens, do NOT introduce a parallel palette)
    - /Users/theo/.claude/rules/ecc/web/design-quality.md (anti-template policy — the table must NOT look like a default shadcn DataTable; lean into the editorial direction established in Plan 01-02)
    - /Users/theo/.claude/rules/ecc/web/performance.md (animate compositor-friendly properties only; explicit width/height on table; no layout animation on row updates)
  </read_first>
  <action>
    Create TagBadge.tsx: `function TagBadge({ kind, present }: { kind: 'G' | 'B' | 'K' | 'Erreur'; present: boolean })`. Renders a small pill — filled with a kind-specific accent (extend main.css tokens: --color-badge-genre, --color-badge-bpm, --color-badge-key, --color-badge-error) when present, hollow outline when absent. The 'Erreur' variant is always present-styled with the error token and a title-attribute hover for the message. Semantic: `<span role="status" aria-label="...">` so screen readers announce "Genre: présent" etc.
    Create VirtualizedFileTable.tsx: receives `rows: ScannedFile[]` as a prop (NOT subscribed directly to the store — composition root in AnalyserView passes the selector). Uses `useVirtualizer({ count: rows.length, getScrollElement: () => scrollRef.current, estimateSize: () => 36, overscan: 8 })` per RESEARCH Pattern 4. Renders a header row outside the virtualized container (sticky) and the virtualized rows inside a scrollable `<div ref={scrollRef}>` with a fixed height (use a CSS custom property `--table-height` so AnalyserView can size it). Each row renders: filename (basename + path tooltip), format, bitrate (`${kbps} kbps` or '—'), size (`(sizeBytes / 1_000_000).toFixed(1)` MB), sample rate (`${Hz/1000} kHz` or '—'), duration (`mm:ss` formatted from durationSeconds or '—'), and three TagBadge instances for G/B/K. For rows where `parsedOk === false` add a class `row--errored` (greyed text via tokens) and render an additional 'Erreur' TagBadge with `title={row.errorMessage}`.
    Create ScanToolbar.tsx: reads `useAppStore(s => s.rootFolder)` and `useScanStore` (start, cancel, status, rows.length, totalFiles, durationMs, error). Renders:
    - A "Scanner" button — disabled when rootFolder is null OR status === 'running'. onClick: `start(rootFolder)`. When rootFolder is null, show an empty-state hint inline ("Choisis un dossier racine pour lancer un scan.").
    - A "Stop" button — visible only when status === 'running'. onClick: cancel().
    - A live progress strip: `<output>` showing `${rows.length} fichiers scannés` while running, and `${totalFiles} fichiers · ${(durationMs/1000).toFixed(1)} s` on done, or the error message on error. Use `aria-live="polite"`.
    Update AnalyserView.tsx: replace the placeholder. Compose `<section aria-labelledby="analyser-heading"><h2 id="analyser-heading">Analyser</h2><ScanToolbar /><VirtualizedFileTable rows={useScanStore(s => s.rows)} /></section>`.
    Extend main.css with the new tokens (badge colors, table colors) and kebab-case classes for the table/toolbar/badges. The design direction is the editorial dark-studio from Plan 01-02 — extend it, don't replace it. Animate only opacity/transform on row hover (web/performance.md compositor-friendly rule); never animate width/height/padding on row updates (that would jank during a streaming scan).
    Run `npm run build` to confirm the renderer typechecks against the extended DjUtilsApi.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - grep "window.djUtils" src/renderer/src/components — returns ZERO matches (components consume the stores, never the bridge directly; T-1-04 carried)
    - VirtualizedFileTable uses useVirtualizer from @tanstack/react-virtual (grep)
    - TagBadge is reused 3× per row (G/B/K) plus optional 4th (Erreur) when parsedOk=false
    - main.css :root block defines new badge/table tokens (no hardcoded hex/oklch values inside component-scoped rules); extends — not replaces — Plan 01-02 tokens
    - ScanToolbar reads rootFolder from useAppStore (not from window.djUtils.getRootFolder directly)
    - AnalyserView is a `<section aria-labelledby>` per the web coding-style semantic rule
    - No animation on layout-bound properties anywhere in the new CSS (grep "@keyframes" + manual inspection — only opacity/transform)
  </acceptance_criteria>
  <done>SCAN-01/02/03 visually rendered against the new bridge; virtualization in place; Phase 1 editorial design language extended; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VirtualizedFileTable render test — 5000-row synthetic dataset stays mounted</name>
  <files>src/renderer/src/components/analyser/VirtualizedFileTable.test.tsx</files>
  <read_first>
    - src/renderer/src/components/analyser/VirtualizedFileTable.tsx (from Task 2)
    - src/shared/ipc-types.ts (ScannedFile shape)
    - vitest.setup.ts (jest-dom matchers)
  </read_first>
  <behavior>
    - Test: rendering &lt;VirtualizedFileTable rows={[]} /&gt; shows the header row and an empty-state message (no error)
    - Test: rendering with 5000 synthetic rows produces only a small number of row DOM nodes (assert document.querySelectorAll('[data-row]').length is significantly less than 5000 — e.g. &lt; 100 — virtualization is working; this is the SCAN-05 "thousands of files" assertion at the DOM level)
    - Test: a row with parsedOk=false renders the 'Erreur' badge (visible by accessible name)
    - Test: a row with hasGenre=true, hasBpm=false, hasKey=true renders G and K badges marked present and B marked absent (assert via aria-label or class)
    - Test: bitrate=null renders as '—'; durationSeconds=125 renders as '2:05'
  </behavior>
  <action>
    Create VirtualizedFileTable.test.tsx (jsdom). Generate 5000 synthetic ScannedFile rows via a helper `makeRow(i)`. Render the component with a fixed scroll container size via inline style or by providing the `--table-height` token. Note: jsdom does not implement real layout, so `useVirtualizer` may render its overscan default without scrolling — that's fine; the assertion is "less than the full 5000", not "exactly N". Use accessible queries (getByRole, getAllByRole('row'), getByText('—'), etc.).
    Mock @tanstack/react-virtual's `useVirtualizer` ONLY if jsdom's lack of layout makes the test brittle; prefer the real implementation with a small jsdom-friendly `getScrollElement` returning a div with `getBoundingClientRect` stubbed to a fixed height. If the real virtualizer returns all rows under jsdom (no scroll context), narrow the assertion to "header + at least one row rendered + the format/badge logic is correct" rather than DOM-count virtualization — and add a comment explaining the manual checkpoint will exercise the real-browser virtualization.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/components/analyser/VirtualizedFileTable</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/components/analyser/VirtualizedFileTable` passes the five behaviors
    - Test uses accessible queries (role/text/aria-label) — no test-ids except `data-row` where needed for the DOM-count virtualization assertion
    - Empty state, error row, partial-tag row, null-bitrate, and duration-formatting all covered (badge logic + cell formatting is the load-bearing unit signal — the manual checkpoint covers real-scan throughput)
  </acceptance_criteria>
  <done>Cell formatting + badge rendering + (best-effort) virtualization asserted at the DOM level.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Phase 2 Analyser UI end-to-end: pick a folder (Phase 1) → click Scanner → live-updating virtualized list with format/bitrate/size/sampleRate/duration + G/B/K badges; nav stays responsive during a multi-thousand-file scan; Stop cancels cleanly; failed-parse files appear greyed with an Erreur badge.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. App opens to the AnalyserView (default tool). If no rootFolder is set, the Scanner button is disabled with a hint pointing to the folder picker — pick a real music folder containing at least a few hundred audio files (ideally 1000+ to exercise SCAN-05).
    2. Click Scanner. Within a second or two, rows begin appearing in the virtualized table. Each row shows the file name, format (MP3/FLAC/M4A/...), bitrate in kbps, size in MB, sample rate in kHz, duration in mm:ss, and three coloured G/B/K badges (filled when the tag is present, hollow when absent) — visually verify SCAN-01 + SCAN-02 + SCAN-03 against files you know the tag state of (e.g. a file you tagged in Rekordbox should show G/B/K filled; an unprocessed download should show them hollow).
    3. While the scan is running, click "Convertir" then "Tagger" in the nav — both should switch instantly, no jank, no main-process freeze. Return to Analyser — rows are still streaming in. This is the SCAN-05 "UI remains scrollable and interactive" success criterion.
    4. Scroll the table mid-scan — scrolling stays smooth (virtualization working).
    5. Click Stop. The scan halts; the row count freezes; status shows "cancelled". Click Scanner again to confirm a fresh scan starts (single-active-scan from Plan 02-01).
    6. If your folder contains any file the parser cannot read (rare — try a 0-byte or truncated .mp3 placed manually), it should appear in the list greyed with an "Erreur" badge whose tooltip shows the parse error message.
    7. Quit the app. Reopen. The rootFolder is still selected (Phase 1 persistence). No orphan worker process (`ps | grep scanWorker` → empty).
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass. Otherwise describe which step failed — most likely candidates: (a) the table looks like a generic default → revisit design-quality.md and main.css tokens; (b) nav freezes mid-scan → check that ScanToolbar reads from the store, not from window.djUtils.scan directly, and that 'rows' handler uses a SINGLE setState per batch (Pitfall 5); (c) Erreur rows don't appear → confirm Plan 02-01 worker emits errorRow on catch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer components → useScanStore → window.djUtils.scan | Components never touch the bridge; the store mediates (T-1-04 invariant carried from Plan 01-02 and Plan 02-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Elevation of Privilege | renderer components | mitigate (carried) | Grep gate in Task 2 acceptance: no `window.djUtils` in src/renderer/src/components — store mediates. |
| T-2-01 | Information Disclosure / Tampering | scan:start folder argument | mitigate (carried) | Store always passes `useAppStore.getState().rootFolder`, never a user-typed path; main also enforces the allowlist (Plan 02-01 Task 6). Defence-in-depth. |
| T-2-07 | Denial of Service | per-file setState storm | mitigate | useScanStore's 'rows' handler does a single `set` per batch event (Plan 02-01 batches at the source). Pitfall 5 mitigation enforced by Task 1 test. |
| T-2-08 | Denial of Service | DOM blow-up from 5000+ rows | mitigate | VirtualizedFileTable uses @tanstack/react-virtual (Pattern 4); Task 3 asserts DOM node count is bounded under jsdom; checkpoint verifies smooth scroll on a real library. |
| T-2-09 | Information Disclosure | rendering raw filesystem path in DOM | accept | Paths are user-owned local data; the renderer is sandboxed and never sends them anywhere external. No new exposure. |
</threat_model>

<verification>
- `npm run build` green.
- `npm run test -- --run src/renderer/src/store/useScanStore` green (9 lifecycle behaviors).
- `npm run test -- --run src/renderer/src/components/analyser/VirtualizedFileTable` green (cell formatting + badge logic + best-effort virtualization).
- grep: zero `window.djUtils` references inside src/renderer/src/components (T-1-04).
- Manual checkpoint: real-folder scan streams rows into a virtualized table; nav stays responsive; cancel works; Erreur rows render greyed; visual design extends — not replaces — the Plan 01-02 editorial direction.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1: User triggers a scan and sees a live-updating list of audio files with format/bitrate/size/sample rate/duration.
- ROADMAP Phase 2 success criterion 2: Each row shows G/B/K badges per the SCAN-03 booleans.
- ROADMAP Phase 2 success criterion 3: UI remains scrollable and interactive during a multi-thousand-file scan.
- SCAN-01/02/03 user-observable; SCAN-05 user-confirmable.
- Failed-parse rows surfaced in the UI (planner decision on RESEARCH Open Question 6 honored end-to-end).
- No regression of Phase 1 nav + folder-picker + persistence (verified by the same launch in step 1).
</success_criteria>

<output>
Create `.planning/phases/02-scanning/02-02-SUMMARY.md` when done.
</output>
