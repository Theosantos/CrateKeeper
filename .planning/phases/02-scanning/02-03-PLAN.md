---
phase: 02-scanning
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/main/scan/csvExport.ts
  - src/main/scan/csvExport.test.ts
  - src/main/ipc/scan.ts
  - src/main/ipc/scan.test.ts
  - src/main/index.ts
  - src/renderer/src/store/useScanStore.ts
  - src/renderer/src/components/analyser/ScanToolbar.tsx
autonomous: false
requirements: [SCAN-04]
user_setup: []

must_haves:
  truths:
    - "Clicking 'Exporter CSV' on a completed scan opens the native save dialog with default name dj-utils-scan-YYYYMMDD-HHmm.csv in app.getPath('downloads')"
    - "Confirming the dialog writes a CSV with a header row and one row per scanned file via a streamed csv-stringify pipeline (constant memory regardless of row count)"
    - "Cancelling the dialog returns null without writing anything"
    - "The exported CSV opens cleanly in a spreadsheet (commas/quotes/newlines in paths are properly escaped)"
    - "The button is disabled when status !== 'done' (no in-progress / errored export)"
  artifacts:
    - path: "src/main/scan/csvExport.ts"
      provides: "streamCsv(rowsIter, savePath, columns?) — pipes scanRepo.iterateFiles → csv-stringify → fs.createWriteStream"
      contains: "createWriteStream"
    - path: "src/main/ipc/scan.ts"
      provides: "scanExportCsvHandler — replaces Plan 02-01 stub with real impl: dialog.showSaveDialog → streamCsv → return savedPath | null"
      contains: "showSaveDialog"
  key_links:
    - from: "src/main/ipc/scan.ts"
      to: "src/main/scan/csvExport.ts"
      via: "streamCsv(repo.iterateFiles(scanId), savePath)"
      pattern: "streamCsv"
    - from: "src/main/scan/csvExport.ts"
      to: "src/main/scan/scanRepo.ts"
      via: "consumes the iterator returned by repo.iterateFiles"
      pattern: "iterateFiles"
    - from: "src/renderer/src/components/analyser/ScanToolbar.tsx"
      to: "src/renderer/src/store/useScanStore.ts"
      via: "store.exportCsv()"
      pattern: "exportCsv"
    - from: "src/renderer/src/store/useScanStore.ts"
      to: "window.djUtils.scan"
      via: "exportCsv(scanId)"
      pattern: "window\\.djUtils\\.scan\\.exportCsv"
---

<objective>
Ship SCAN-04 — CSV export of a completed scan — as a small, focused vertical slice on top of the Plan 02-01 backbone and the Plan 02-02 UI: a `streamCsv` helper in main that pipes `scanRepo.iterateFiles(scanId)` through `csv-stringify` into a `fs.createWriteStream` for constant-memory output, the real `scan:export-csv` handler replacing the Plan 02-01 stub (driven by `dialog.showSaveDialog`), an `exportCsv` action in the renderer store, and an "Exporter CSV" button in the toolbar enabled only when status === 'done'.

Purpose: Completes the fourth and final Phase 2 ROADMAP success criterion (CSV export) without re-touching the worker, the repo schema, or the table — a clean cross-process slice that exercises the iterator cursor the repo already exposes.
Output: A working File → CSV path from any completed scan, with proper escaping for filenames containing commas/quotes/newlines, constant memory regardless of row count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scanning/02-RESEARCH.md
@.planning/phases/02-scanning/02-01-SUMMARY.md
@.planning/phases/02-scanning/02-02-SUMMARY.md
@src/shared/ipc-types.ts
@src/main/scan/scanRepo.ts
@src/main/ipc/scan.ts
@src/renderer/src/store/useScanStore.ts
@src/renderer/src/components/analyser/ScanToolbar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: streamCsv helper — streamed csv-stringify into fs.createWriteStream (SCAN-04 backbone)</name>
  <files>src/main/scan/csvExport.ts, src/main/scan/csvExport.test.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Code Examples > Streamed CSV export snippet; Pitfall 7 MEDIUM — CSV memory blowup; Anti-Patterns — "Building the full CSV string in memory")
    - src/main/scan/scanRepo.ts (iterateFiles cursor from Plan 02-01 — the source of rows)
    - src/shared/ipc-types.ts (ScannedFile column shape)
  </read_first>
  <behavior>
    - Test: streamCsv on an empty iterator writes a single header row to the file (header-only is valid CSV)
    - Test: streamCsv on a 3-row iterator writes header + 3 data rows; reading the file back with `csv-parse` (or a hand-rolled split + comma split is fine here since we control the inputs) round-trips the column values
    - Test: a file path containing a comma (e.g. '/Music/Artist, Band/Song.mp3') is properly quoted in the output (the round-trip yields the original path, single column — proves escaping)
    - Test: a file path containing a double-quote (e.g. '/Music/Said "Hi"/x.mp3') is properly escaped (csv-stringify doubles the quote and wraps in quotes)
    - Test: a file path containing a newline character (rare but possible on macOS) is quoted-and-preserved
    - Test: boolean columns hasGenre/hasBpm/hasKey are emitted as 'true' / 'false' (or '1' / '0' — pick one explicitly in the column config so the format is stable; recommend 'true'/'false' for spreadsheet readability)
    - Test: parsedOk=false rows include the errorMessage column populated
    - Test: streamCsv on a 5000-row iterator completes without buffering all rows into a single string (use a counter + a mocked iterator that yields one row at a time and asserts memory-style behavior by verifying the write stream emitted multiple 'drain'-eligible chunks, OR — simpler — assert that the output file's size matches `expectedHeaderLen + 5000 * approxRowLen ± slack` and that the function returned in well under a per-row-buffered allocation budget)
  </behavior>
  <action>
    Create src/main/scan/csvExport.ts following RESEARCH Code Examples block verbatim:
    - `export const CSV_COLUMNS = ['path','format','bitrate','sizeBytes','sampleRate','durationSeconds','hasGenre','hasBpm','hasKey','parsedOk','errorMessage'] as const` (lock the column set + order — public contract for downstream tooling)
    - `export async function streamCsv(rowsIter: Iterable<ScannedFile>, savePath: string): Promise<void>` — creates a `createWriteStream(savePath)`, pipes a `stringify({ header: true, columns: CSV_COLUMNS, cast: { boolean: (v) => v ? 'true' : 'false' } })` into it, iterates `for (const r of rowsIter) stringifier.write(r)`, ends the stringifier, and resolves on the writeStream `'finish'` event. Reject on either stream's `'error'`.
    - Type the rowsIter as `Iterable<ScannedFile> | AsyncIterable<ScannedFile>` if better-sqlite3's stmt.iterate() returns AsyncIterable in our version; in better-sqlite3 12 it's synchronous Iterable, so plain `Iterable` is correct. Use `for await` if AsyncIterable to keep the contract flexible.
    Create csvExport.test.ts (node project). Use `node:os.tmpdir()` + `node:crypto.randomUUID()` for temp save paths. Build the iterator from a plain array of synthetic ScannedFile objects. After write, `fs.readFile(savePath, 'utf8')` and assert the content. For the comma/quote/newline edge cases, prefer round-tripping through `csv-parse` (it's the sibling lib in the csv.js.org family and is pure JS) — install only if not already a transitive dep; otherwise hand-roll the assertion against the known csv-stringify output (RFC 4180 doubled-quotes).
    Clean up temp files in `afterEach`.
    DO NOT call this function from main yet — Task 2 wires it. This task just proves the pure helper works.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/scan/csvExport</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/scan/csvExport` passes the 7 behaviors above
    - csvExport.ts uses `createWriteStream` + `stringify().pipe(out)` + `for (... of iter) stringifier.write(r)` — NEVER `csvString = stringify.sync(allRows)` (Pitfall 7 mitigation; grep "stringify.sync" returns no matches)
    - CSV_COLUMNS is exported and contains exactly the 11 ScannedFile fields in a fixed order (downstream contract)
    - Boolean cast emits 'true'/'false' consistently (test asserts at least one true and one false in the output)
    - Comma + quote + newline escaping all round-trip correctly
  </acceptance_criteria>
  <done>Streaming CSV export proven on synthetic data with all RFC 4180 edge cases covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real scanExportCsvHandler — showSaveDialog + streamCsv from scanRepo.iterateFiles</name>
  <files>src/main/ipc/scan.ts, src/main/ipc/scan.test.ts, src/main/index.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Open Question 3 — default filename + downloads dir; Security Domain — save path chosen by main only, renderer never names it)
    - src/main/scan/csvExport.ts (from Task 1)
    - src/main/scan/scanRepo.ts (iterateFiles + getScan from Plan 02-01)
    - src/main/ipc/scan.ts (Plan 02-01 stub to replace)
    - src/main/ipc/dialog.ts (Phase 1 pattern for injecting a mocked dialog API into a pure handler)
  </read_first>
  <behavior>
    - Test (pure handler with injected deps): scanExportCsvHandler({ repo, dialogApi, streamCsvFn }, scanId) validates `typeof scanId === 'string'` (V5 carried)
    - Test: when scanId references a non-existent scan in the repo, the handler throws/rejects (no save dialog opened — fast-fail)
    - Test: when scanId is valid, the handler calls `dialogApi.showSaveDialog` with a defaultPath of `path.join(downloadsPath, 'dj-utils-scan-YYYYMMDD-HHmm.csv')` (planner decision on RESEARCH Open Question 3) and `filters: [{ name: 'CSV', extensions: ['csv'] }]`
    - Test: when the dialog returns `{ canceled: true, filePath: '' }`, the handler returns null and does NOT invoke streamCsvFn
    - Test: when the dialog returns `{ canceled: false, filePath: '/tmp/x.csv' }`, the handler calls `streamCsvFn(repo.iterateFiles(scanId), '/tmp/x.csv')` and returns '/tmp/x.csv'
    - Test: a generated default filename matches the regex /^dj-utils-scan-\\d{8}-\\d{4}\\.csv$/ — formatted from a deterministic injected `now()`
    - Test: the save path is ALWAYS dialog-derived; the renderer-supplied scanId never participates in path construction (Security: T-2-01 carried — renderer never names the filesystem write target)
  </behavior>
  <action>
    Modify src/main/ipc/scan.ts: replace the Plan 02-01 stub with the real handler.
    - Add `export function defaultCsvFilename(now: Date = new Date()): string` returning `dj-utils-scan-${YYYYMMDD}-${HHmm}.csv` (zero-padded). Pure function, unit-testable.
    - Replace `scanExportCsvHandler` with: `export async function scanExportCsvHandler(deps: { repo: ScanRepo; dialogApi: { showSaveDialog(opts): Promise<{canceled: boolean; filePath?: string}> }; streamCsvFn: typeof streamCsv; downloadsPath: string; now?: () => Date }, scanId: unknown): Promise<string | null>` implementing the behaviors above.
    - Update `registerScanHandlers` to construct the real dependency set: `dialogApi = dialog` (electron), `downloadsPath = app.getPath('downloads')` (planner decision on RESEARCH Open Question 3 locks the default location), `streamCsvFn = streamCsv`, `repo = getScanRepo()`, `now = () => new Date()`.
    Update src/main/index.ts ONLY if registerScanHandlers' construction args changed (most likely just pass `app` through or import it locally inside scan.ts; prefer the latter so registerScanHandlers stays the single integration point — index.ts unchanged).
    Extend src/main/ipc/scan.test.ts with the new behaviors. Mock dialogApi/streamCsvFn/repo/now. The existing Plan 02-01 stub test that asserted "throws not-implemented" should be REMOVED (it would now fail the wrong way — the stub is gone).
  </action>
  <verify>
    <automated>npm run test -- --run src/main/ipc/scan</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/ipc/scan` passes all 7 behaviors for scanExportCsvHandler + the pre-existing Plan 02-01 behaviors (scanStartHandler folder allowlist, scanCancelHandler) still pass
    - defaultCsvFilename is pure (no Date.now / no module-scope state) — test injects a fixed Date and asserts the exact filename
    - showSaveDialog defaultPath is composed from `app.getPath('downloads')` + defaultCsvFilename(); renderer-supplied scanId never appears in the path (Security T-2-01 carried)
    - The Plan 02-01 stub-throw test is removed (was a placeholder for this very task)
    - `npm run build` exits 0; the existing folder-allowlist test still passes (no regression to Plan 02-01 contract)
  </acceptance_criteria>
  <done>scan:export-csv handler shipped end-to-end on the backend: dialog → stream → save path or null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useScanStore.exportCsv + ScanToolbar button (SCAN-04 user-observable)</name>
  <files>src/renderer/src/store/useScanStore.ts, src/renderer/src/components/analyser/ScanToolbar.tsx</files>
  <read_first>
    - src/renderer/src/store/useScanStore.ts (from Plan 02-02 Task 1 — add a new action)
    - src/renderer/src/components/analyser/ScanToolbar.tsx (from Plan 02-02 Task 2 — add a third button)
    - .planning/phases/02-scanning/02-02-SUMMARY.md (current Toolbar layout + status enum)
    - src/shared/ipc-types.ts (DjUtilsApi.scan.exportCsv signature)
  </read_first>
  <behavior>
    - Test (store): exportCsv() when scanId is null returns null without calling window.djUtils.scan.exportCsv
    - Test: exportCsv() when scanId is set calls window.djUtils.scan.exportCsv(scanId) and returns whatever it returns (path string or null)
    - Test: exportCsv() while status !== 'done' returns null and does NOT call the bridge (defence-in-depth — the button is also disabled in that state, but the store guards too)
    - Test: exportCsv() while another export is in-flight (track an internal `exporting: boolean` flag) returns null instead of double-firing
  </behavior>
  <action>
    Extend src/renderer/src/store/useScanStore.ts:
    - Add `exporting: boolean` to state (default false), `lastExportPath: string | null` (default null).
    - Add `exportCsv(): Promise<string | null>` that guards on scanId !== null && status === 'done' && !exporting; sets `exporting: true`; awaits `window.djUtils.scan.exportCsv(scanId)`; sets `lastExportPath` on success; sets `exporting: false` in a finally. Returns the resolved path (or null).
    - Extend the test from Plan 02-02 (or add a focused new describe block) covering the four behaviors above. The existing 9 tests must still pass — no regression.
    Extend ScanToolbar.tsx:
    - Add an "Exporter CSV" button placed to the right of the progress strip. Disabled when status !== 'done' OR exporting=true (use store selectors). onClick: `const path = await store.exportCsv(); if (path) { show a toast/inline notice "Exporté : ${path}" for a few seconds }`. Keep the notice minimal and accessible (`role="status" aria-live="polite"`), reusing the editorial design tokens from Plan 01-02 / 02-02.
    - DO NOT animate the toast appearance with layout properties (web/performance.md compositor-friendly rule — opacity + transform only).
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/store/useScanStore</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/store/useScanStore` passes the original 9 Plan 02-02 behaviors PLUS the 4 new export behaviors
    - exportCsv guards on scanId !== null AND status === 'done' AND !exporting (defence-in-depth — the button is also disabled)
    - ScanToolbar renders a third "Exporter CSV" button (grep "Exporter CSV" in ScanToolbar.tsx)
    - grep "window.djUtils" src/renderer/src/components — still ZERO matches (T-1-04 carried)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SCAN-04 user-observable: completed scan → button enables → click → save dialog → CSV on disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SCAN-04 end-to-end: an "Exporter CSV" button on the Analyser toolbar that, after a successful scan, opens the native save dialog with a sensible default filename in Downloads, then streams the scanned-files list as a properly-escaped CSV to the chosen path.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. Pick a folder, run a scan, wait for status "done".
    2. The "Exporter CSV" button is now enabled (it was disabled during running). Click it.
    3. The native save dialog opens with a default filename like `dj-utils-scan-20260529-1432.csv` in your Downloads folder (planner decision on Open Question 3). Confirm or change the path; save.
    4. The toolbar shows "Exporté : /path/to/your.csv" briefly. Open the file in a spreadsheet (Numbers / Excel / LibreOffice) — header row reads `path,format,bitrate,sizeBytes,sampleRate,durationSeconds,hasGenre,hasBpm,hasKey,parsedOk,errorMessage`; each row matches a scanned file. Rows for failed-parse files have parsedOk=false and a populated errorMessage.
    5. Cancel the save dialog on a fresh export attempt — nothing is written, no error, button is still usable.
    6. If you have a file with a comma in its name, verify the CSV opens correctly and that field stays in one column (escape working).
    7. Try clicking "Exporter CSV" before a scan has completed — button is disabled. Try it during a scan — still disabled. Try it after status=cancelled or status=error — disabled.
    8. The Phase 1 nav still works, no regression: pick a different folder, re-run scan, re-export — fresh CSV reflects the new scan.
  </how-to-verify>
  <resume-signal>Type "approved" if (a) the default filename matches the YYYYMMDD-HHmm pattern in Downloads, (b) the CSV opens cleanly with the locked column order, (c) cancel returns silently, (d) the button is correctly gated by status. Otherwise describe which step failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (scan:export-csv) | Carried from Plan 02-01; the renderer only supplies scanId (validated string). Save path is dialog-derived in main. |
| main → filesystem (write CSV) | NEW surface vs. Plan 02-01: main now writes a file at a user-confirmed dialog path. Renderer never names this path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Elevation of Privilege | renderer components | mitigate (carried) | ScanToolbar consumes useScanStore; grep gate in Task 3 confirms zero `window.djUtils` references in components. |
| T-2-01 | Tampering | scan:export-csv save path | mitigate | Save path chosen exclusively by `dialog.showSaveDialog` in main (RESEARCH Security row "renderer never names it"). Renderer supplies scanId only; handler test asserts scanId never participates in path construction. |
| T-2-10 | Denial of Service | huge in-memory CSV string | mitigate | streamCsv uses `createWriteStream` + streaming `stringify` — constant memory regardless of row count (Pitfall 7 mitigation; test verifies on 5000-row iterator). |
| T-2-11 | Tampering | malformed CSV from filenames containing CSV-special chars | mitigate | csv-stringify handles RFC 4180 escaping (commas, double-quotes, newlines); Task 1 tests all three edge cases. |
| T-2-12 | Information Disclosure | exporting absolute filesystem paths to a file | accept | The user explicitly chose the export action and the save destination; paths are their own local data. No new exposure. |
</threat_model>

<verification>
- `npm run build` green.
- `npm run test -- --run src/main/scan/csvExport` green (RFC 4180 edge cases + streaming on 5000 rows).
- `npm run test -- --run src/main/ipc/scan` green (full handler suite — folder allowlist from Plan 02-01 + new export-csv behaviors).
- `npm run test -- --run src/renderer/src/store/useScanStore` green (13 total: 9 from Plan 02-02 + 4 new export behaviors).
- grep: zero `stringify.sync` in csvExport.ts; zero `window.djUtils` in src/renderer/src/components.
- Manual checkpoint: real-scan → CSV → opens cleanly with locked column order; default filename matches dj-utils-scan-YYYYMMDD-HHmm.csv; cancel is silent; button gated by status.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 4: User clicks "Export CSV" and receives a file containing all scanned metadata.
- SCAN-04 user-observable end-to-end.
- Constant-memory streaming export (Pitfall 7 honoured) — no in-memory CSV string.
- Save path always dialog-derived; renderer cannot influence the filesystem target (Security T-2-01 / Pitfall 8 spirit extended to writes).
- CSV column order locked as a public contract for downstream tooling.
- No regression of Plan 02-01 folder-allowlist gate or Plan 02-02 scan UI.
</success_criteria>

<output>
Create `.planning/phases/02-scanning/02-03-SUMMARY.md` when done.
</output>
