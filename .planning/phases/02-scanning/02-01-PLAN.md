---
phase: 02-scanning
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - src/shared/ipc-types.ts
  - src/main/db/connection.ts
  - src/main/scan/scanRepo.ts
  - src/main/scan/scanRepo.test.ts
  - src/main/workers/scanCore.ts
  - src/main/workers/scanCore.test.ts
  - src/main/workers/scanWorker.ts
  - src/main/workers/__fixtures__/tagged.mp3
  - src/main/workers/__fixtures__/untagged.mp3
  - src/main/workers/__fixtures__/sample.flac
  - src/main/scan/controller.ts
  - src/main/scan/controller.test.ts
  - src/main/ipc/scan.ts
  - src/main/ipc/scan.test.ts
  - src/main/index.ts
  - src/preload/index.ts
autonomous: false
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-05]
user_setup: []

must_haves:
  truths:
    - "Running `npm run build` emits a bundled worker at `out/main/workers/scanWorker.js` alongside `out/main/index.js`"
    - "scanCore.rowFromMetadata returns format, bitrate, sizeBytes, sampleRate, durationSeconds for parsed audio"
    - "scanCore.rowFromMetadata returns hasGenre/hasBpm/hasKey booleans matching the file's tag state"
    - "ScanController spawns a Worker, forwards 'rows' batches to the persisted webContents AND to scanRepo.insertBatch in one transaction"
    - "ScanController.cancel terminates the active worker and emits a 'cancelled' event"
    - "ScanController.start while running cancels the prior scan before spawning the new one (single-active-scan)"
    - "The settings:get-folder/set-folder bridge surface and the FOUND-03 round-trip continue to work unchanged"
    - "window.djUtils.scan namespace exposes start/cancel/exportCsv/onEvent and only accepts folder === persisted rootFolder"
  artifacts:
    - path: "src/shared/ipc-types.ts"
      provides: "Extended IpcChannels with ScanStart/ScanCancel/ScanExportCsv/ScanEvent; ScannedFile + ScanEvent types; DjUtilsApi.scan namespace"
      contains: "scan:"
    - path: "src/main/scan/scanRepo.ts"
      provides: "createScanRepo factory: createScan, insertBatch (transaction), complete, get, listFiles, iterateFiles, deleteScansForFolder"
      contains: "db.transaction"
    - path: "src/main/workers/scanCore.ts"
      provides: "Pure functions: AUDIO_EXTS, rowFromMetadata, errorRow, hasGenre/hasBpm/hasKey helpers (no node:worker_threads import)"
      contains: "AUDIO_EXTS"
    - path: "src/main/workers/scanWorker.ts"
      provides: "Worker entry: readdirp stream + parseFile + batched parentPort.postMessage; honours {type:'cancel'} message"
      contains: "parentPort"
    - path: "src/main/scan/controller.ts"
      provides: "ScanController({spawnWorker, repo, send}): start/cancel; single-active-scan; persists batches + forwards events"
      contains: "ScanController"
    - path: "src/main/ipc/scan.ts"
      provides: "registerScanHandlers: scan:start (asserts folder === settings.rootFolder), scan:cancel, scan:export-csv (stub forwarder), wires webContents to controller"
      contains: "rootFolder"
    - path: "electron.vite.config.ts"
      provides: "main.build.lib.entry multi-entry including workers/scanWorker so it bundles to out/main/workers/scanWorker.js"
      contains: "scanWorker"
    - path: "src/preload/index.ts"
      provides: "Extended djUtils with scan.{start,cancel,exportCsv,onEvent} bound via shared IpcChannels"
      contains: "scan:"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/main/ipc/scan.ts"
      via: "ipcRenderer.invoke('scan:start' | 'scan:cancel' | 'scan:export-csv') and ipcRenderer.on('scan:event')"
      pattern: "scan:(start|cancel|export-csv|event)"
    - from: "src/main/ipc/scan.ts"
      to: "src/main/scan/controller.ts"
      via: "ScanController.start(folder, sender)"
      pattern: "ScanController"
    - from: "src/main/scan/controller.ts"
      to: "src/main/workers/scanWorker.ts"
      via: "new Worker(path.join(__dirname, 'workers/scanWorker.js'))"
      pattern: "workers/scanWorker"
    - from: "src/main/scan/controller.ts"
      to: "src/main/scan/scanRepo.ts"
      via: "repo.insertBatch(rows, scanId)"
      pattern: "insertBatch"
    - from: "src/main/db/connection.ts"
      to: "src/main/scan/scanRepo.ts"
      via: "initScanSchema(db) at openDb() + getScanRepo() lazy singleton"
      pattern: "initScanSchema"
---

<objective>
Stand up the entire main-process backbone for Phase 2 scanning: extend the typed IPC bridge with a `scan` namespace, ship the `scans` + `scanned_files` SQLite tables behind the same `createXRepo(db) + initXSchema(db)` factory pattern from Phase 1, implement a pure scanCore + a Node `worker_threads` worker that streams `readdirp` through `music-metadata.parseFile`, and a ScanController that owns worker lifecycle + batched persistence + IPC event forwarding. Also fix the bundled-worker path (Wave 0 gap) by adding the worker entry to `electron.vite.config.ts` so the compiled worker lands at `out/main/workers/scanWorker.js`.

Purpose: This is the entire non-UI surface Phase 2 needs. SCAN-01/02/03/05 are all observable through the bridge after this plan ships; Plan 02-02 just renders what this exposes, and Plan 02-03 only adds CSV streaming on top of the same scanRepo.
Output: A typed `window.djUtils.scan` API, a worker that bundles correctly in both dev and packaged builds, persisted scan rows in better-sqlite3, and unit-test coverage on the pure core + repo + controller (mocked Worker + webContents).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scanning/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/shared/ipc-types.ts
@src/main/index.ts
@src/main/db/connection.ts
@src/main/db/settingsRepo.ts
@src/preload/index.ts
@electron.vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — install deps, add worker entry to electron.vite.config.ts, add fixtures</name>
  <files>package.json, electron.vite.config.ts, src/main/workers/__fixtures__/tagged.mp3, src/main/workers/__fixtures__/untagged.mp3, src/main/workers/__fixtures__/sample.flac</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Standard Stack > Installation; Pitfall 1 HIGH on bundled-worker path; Wave 0 Gaps; Runtime State Inventory > Build artifacts)
    - electron.vite.config.ts (current — empty main config; renderer has alias + react plugin only)
    - package.json (Plan 01 stack: electron 39, electron-vite 5, vite 7, better-sqlite3 12)
  </read_first>
  <action>
    Install runtime deps into `dependencies` (NOT devDependencies — externalizeDepsPlugin externalizes deps so workers can `import` them at runtime): `npm install music-metadata readdirp csv-stringify @tanstack/react-virtual`. All four are pure-JS per RESEARCH Package Legitimacy Audit (slopcheck OK across the board); no native build step, no electron-rebuild impact.
    Do NOT add a postinstall or change pretest/predev scripts — STATE.md warns the dual-ABI rebuild for better-sqlite3 is already correctly wired via predev (Electron ABI) and pretest (Node ABI). Adding hooks here would break that contract.
    Update electron.vite.config.ts: keep the existing renderer block; in the `main` block, add `build.rollupOptions.input` (or equivalent `lib.entry` array) declaring BOTH entries so electron-vite emits two files: the existing main entry at `out/main/index.js` AND the worker at `out/main/workers/scanWorker.js`. The exact shape: `main: { build: { rollupOptions: { input: { index: resolve('src/main/index.ts'), 'workers/scanWorker': resolve('src/main/workers/scanWorker.ts') } } } }`. This is RESEARCH Pitfall 1 (HIGH severity) — without it the Worker constructor throws `ERR_MODULE_NOT_FOUND` in packaged builds. Verify the path computed via `path.join(__dirname, 'workers/scanWorker.js')` from `out/main/index.js` resolves to `out/main/workers/scanWorker.js`.
    Add three tiny fixture audio files at `src/main/workers/__fixtures__/` for use by scanCore tests in Task 3: `tagged.mp3` (Genre + BPM + Key all set), `untagged.mp3` (none set), `sample.flac` (any valid FLAC). Keep each <10 KB — strip cover art with `ffmpeg -i in.mp3 -map_metadata -1 -map a -c:a copy -metadata genre=House -metadata TBPM=128 -metadata TKEY=8A -t 1 tagged.mp3`. Source from any short silent clip or trimmed dev file; commit the binaries.
    Run `npm run build` and confirm both `out/main/index.js` AND `out/main/workers/scanWorker.js` exist (they will be empty placeholders until later tasks add code — at minimum stub `src/main/workers/scanWorker.ts` with `export {}` so the build resolves the entry).
  </action>
  <verify>
    <automated>npm run build && test -f out/main/workers/scanWorker.js && test -f out/main/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` adds music-metadata, readdirp, csv-stringify, @tanstack/react-virtual to `dependencies` in package.json (grep confirms — not in devDependencies)
    - No new pretest/predev/postinstall scripts added (preserve dual-ABI contract per STATE.md Known Risks)
    - electron.vite.config.ts main block declares the workers/scanWorker entry alongside index
    - `npm run build` exits 0; both `out/main/index.js` and `out/main/workers/scanWorker.js` exist
    - Three fixture audio files exist under `src/main/workers/__fixtures__/` and `git ls-files` includes them
  </acceptance_criteria>
  <done>Deps installed (deps not devDeps), worker entry bundles to out/main/workers/scanWorker.js (Pitfall 1 HIGH fixed), fixtures committed for the TDD tasks ahead.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend IPC type surface (scan namespace) + preload bridge wiring</name>
  <files>src/shared/ipc-types.ts, src/preload/index.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Pattern 1 typed bridge extension; ScanEvent union; Security Domain — every new channel typed + runtime-validated in handler)
    - src/shared/ipc-types.ts (current IpcChannels + DjUtilsApi shape — preserve Phase 1 contract additively)
    - src/preload/index.ts (current djUtils exposure; sandbox:true preload constraint per STATE.md Plan 01-02 — preload imports ONLY 'electron' + shared types, no @electron-toolkit/preload)
  </read_first>
  <behavior>
    - Purely additive type-level change — no new behavior tests needed here. The runtime test is "Task 5 controller test can import these types" and "Task 6 ipc handler test asserts the wired channel constants match".
    - The preload subscribe pattern must return an unsubscribe closure (RESEARCH Pattern 1) so the renderer Zustand store can clean up on stop/unmount.
  </behavior>
  <action>
    Extend src/shared/ipc-types.ts (additive only — DO NOT change existing IpcChannels values or DjUtilsApi members; Plan 01-02 acceptance proved the existing wires work).
    Add to IpcChannels: `ScanStart: 'scan:start'`, `ScanCancel: 'scan:cancel'`, `ScanExportCsv: 'scan:export-csv'`, `ScanEvent: 'scan:event'` (main → renderer push).
    Add interface ScannedFile { path: string; format: string; bitrate: number | null; sizeBytes: number; sampleRate: number | null; durationSeconds: number | null; hasGenre: boolean; hasBpm: boolean; hasKey: boolean; parsedOk: boolean; errorMessage: string | null }.
    Add discriminated union `export type ScanEvent = { type:'rows'; scanId:string; rows: ScannedFile[] } | { type:'done'; scanId:string; totalFiles:number; durationMs:number } | { type:'error'; scanId:string; message:string } | { type:'cancelled'; scanId:string }`.
    Extend DjUtilsApi with a `scan` namespace per RESEARCH Pattern 1: `scan: { start(folder: string): Promise<string>; cancel(scanId: string): Promise<void>; exportCsv(scanId: string): Promise<string | null>; onEvent(cb: (e: ScanEvent) => void): () => void }`.
    Extend src/preload/index.ts: build a `scan` object wrapping `ipcRenderer.invoke(IpcChannels.ScanStart, folder)` etc. For `onEvent`: create a stable handler `(_, e: ScanEvent) => cb(e)`, call `ipcRenderer.on(IpcChannels.ScanEvent, handler)`, return `() => ipcRenderer.off(IpcChannels.ScanEvent, handler)`. Compose into the existing `djUtils` object as `{ ...existing, scan }` so the Phase 1 surface is preserved verbatim.
    DO NOT introduce @electron-toolkit/preload or any other external preload dep — sandbox:true forbids it (STATE.md Plan 01-02 decision).
    Run `npm run build` to confirm preload typechecks under the existing tsconfig.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (preload + main + renderer all typecheck against the extended DjUtilsApi)
    - grep "scan:" src/shared/ipc-types.ts shows the four new channels + ScanEvent union types present
    - grep "scan:" src/preload/index.ts shows wrapper functions wired to ipcRenderer.invoke/on using the shared IpcChannels constants (no string literals)
    - The four Phase 1 members (pickFolder, getRootFolder, setRootFolder + their channels) remain untouched in IpcChannels and DjUtilsApi
    - preload imports only from 'electron' and '../shared/ipc-types' (grep — no @electron-toolkit/preload, no other relative imports)
  </acceptance_criteria>
  <done>Typed scan bridge surface exists end-to-end; renderer-side TS will see window.djUtils.scan.* fully typed in Plan 02-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: scanCore — pure functions for AUDIO_EXTS, rowFromMetadata, errorRow (SCAN-01/02/03)</name>
  <files>src/main/workers/scanCore.ts, src/main/workers/scanCore.test.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Pattern 2 scanCore; Code Examples > Genre/BPM/Key presence detection; Pitfall 3 MEDIUM on `{duration:true, skipCovers:true}`; Pitfall 9 LOW on music-metadata ESM-only)
    - src/main/workers/__fixtures__/* (fixtures from Task 1)
    - src/shared/ipc-types.ts (ScannedFile shape from Task 2)
  </read_first>
  <behavior>
    - Test: AUDIO_EXTS contains exactly ['.mp3', '.flac', '.m4a', '.aac', '.wav', '.aiff', '.aif', '.ogg', '.opus'] (planner decision on Open Question 1 from RESEARCH)
    - Test: rowFromMetadata(taggedMp3Path, sizeBytes, parsedMeta) returns format='MP3' (or container value), numeric bitrate, sizeBytes, numeric sampleRate, numeric durationSeconds
    - Test: rowFromMetadata on tagged.mp3 returns hasGenre=true, hasBpm=true, hasKey=true; parsedOk=true; errorMessage=null
    - Test: rowFromMetadata on untagged.mp3 returns hasGenre=false, hasBpm=false, hasKey=false; parsedOk=true (SCAN-03 badge logic)
    - Test: rowFromMetadata on sample.flac returns format containing 'FLAC' (or matching container) and durationSeconds > 0 (multi-format coverage — Assumption A2 verification)
    - Test: errorRow(path, sizeBytes, 'boom') returns parsedOk=false, errorMessage='boom', sensible nullish numeric fields; planner decision on Open Question 6: failed-parse rows are still emitted so the UI can show them greyed with an "Erreur" badge
    - Test: hasGenre returns false for [] and for [''] and for ['  ']; true for ['House']
  </behavior>
  <action>
    Create src/main/workers/scanCore.ts as a PURE module — DO NOT import `node:worker_threads`. Export:
    - `export const AUDIO_EXTS = ['.mp3', '.flac', '.m4a', '.aac', '.wav', '.aiff', '.aif', '.ogg', '.opus'] as const` (planner decision on RESEARCH Open Question 1: locks the v1 allowlist to the researcher-proposed safe DJ-format coverage)
    - `inferFormat(filePath, meta?)`: prefer `meta?.format?.container` (uppercased); fallback to `path.extname(filePath).slice(1).toUpperCase()`; final fallback `'UNKNOWN'`
    - `hasGenreTag(meta)`, `hasBpmTag(meta)`, `hasKeyTag(meta)` helpers per RESEARCH Code Examples block (TBPM → common.bpm, TKEY → common.key, both normalised by music-metadata across ID3/MP4/FLAC — Assumption A2)
    - `rowFromMetadata(filePath, sizeBytes, meta): ScannedFile` — populates all 11 ScannedFile fields; bitrate is `Math.round(meta.format.bitrate / 1000)` when defined else null; parsedOk=true; errorMessage=null
    - `errorRow(filePath, sizeBytes, message): ScannedFile` — parsedOk=false; errorMessage=message; bitrate/sampleRate/durationSeconds = null; format from path extension; has* booleans all false
    Create src/main/workers/scanCore.test.ts (node project) using vitest. Import { parseFile } from 'music-metadata' (ESM — Pitfall 9; our tsconfig main module is fine, but `npm test` runs via Vitest which is ESM-friendly). Pre-parse each fixture once in `beforeAll`, then run the behaviors above. Use `stat` from `node:fs/promises` to get real sizeBytes.
    Do NOT import readdirp, worker_threads, or anything from src/main/scan/* — keep scanCore parity with the Phase 1 pattern (handler/orchestrator factored from electron, here worker factored from worker_threads).
  </action>
  <verify>
    <automated>npm run test -- --run src/main/workers/scanCore</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/workers/scanCore` passes all behaviors above
    - grep confirms scanCore.ts has NO `import.*worker_threads` and NO `import.*readdirp`
    - AUDIO_EXTS array literal in scanCore.ts is `as const` with exactly 9 entries matching RESEARCH default
    - rowFromMetadata returns a ScannedFile with all 11 fields populated; errorRow with parsedOk:false and errorMessage:string
    - Multi-format coverage exercised: at least one MP3 fixture test + one FLAC fixture test (Assumption A2 verification per RESEARCH)
  </acceptance_criteria>
  <done>Pure scanCore covers SCAN-01 (format/bitrate/size), SCAN-02 (sampleRate/duration), SCAN-03 (G/B/K badges) at the function level; unit-tested without a Worker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: scanRepo — better-sqlite3 schema + batched-insert factory (Phase 1 repo pattern)</name>
  <files>src/main/scan/scanRepo.ts, src/main/scan/scanRepo.test.ts, src/main/db/connection.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Pattern 3 SQLite schema + batched insert; Pitfall 2 HIGH per-row inserts; Open Question 5 scan persistence — one scan per root folder, replaced on rescan)
    - src/main/db/settingsRepo.ts (Phase 1 createXRepo(db) + initXSchema(db) factory pattern — EXACTLY mirror this shape; STATE.md decision)
    - src/main/db/connection.ts (existing openDb + getSettingsRepo lazy singleton — extend with getScanRepo + initScanSchema call)
    - src/shared/ipc-types.ts (ScannedFile shape from Task 2)
  </read_first>
  <behavior>
    - Test: initScanSchema creates `scans` and `scanned_files` tables with the columns from RESEARCH Pattern 3; safe to call twice (`IF NOT EXISTS`)
    - Test: createScan(scanId, rootFolder, startedAt) inserts a row with status='running'
    - Test: insertBatch(rows[], scanId) wrapped in db.transaction inserts all rows; second insertBatch for same (scanId, path) replaces via INSERT OR REPLACE (idempotent retry safety)
    - Test: complete(scanId, {status:'done', totalFiles, endedAt}) updates the scans row
    - Test: listFiles(scanId) returns rows in stable order with the ScannedFile shape (booleans deserialised from 0/1)
    - Test: iterateFiles(scanId) yields ScannedFile one at a time (for csvExport streaming — used by Plan 02-03; build it now while the repo is fresh)
    - Test (planner decision on Open Question 5): replaceScanForFolder(newScanId, rootFolder, startedAt) deletes prior scans + their files for that root_folder in one transaction then inserts the new running scan — verifies "at most one scan per root folder" semantics
    - Test: parameterised statements only (assert by reading scanRepo.ts source — no template-literal SQL with values)
  </behavior>
  <action>
    Create src/main/scan/scanRepo.ts mirroring the createSettingsRepo shape:
    - `export function initScanSchema(db)` runs the exact CREATE TABLE statements from RESEARCH Pattern 3 (scans + scanned_files + idx_scanned_files_scan); ON DELETE CASCADE keeps file rows tied to scans.
    - `export function createScanRepo(db)` prepares all statements once and returns:
      - `createScan(id, rootFolder, startedAt)` — INSERT with status='running'
      - `replaceScanForFolder(id, rootFolder, startedAt)` — `db.transaction(() => { delete prior scans where root_folder=?; createScan(id, rootFolder, startedAt) })`. Planner decision on RESEARCH Open Question 5: locks single-scan-per-root-folder, replaced on rescan. Simpler UX, matches user intuition, and Phase 4 tagger queue still has the latest list to query.
      - `insertBatch(rows, scanId)` — `db.transaction((rows, scanId) => for (r of rows) insertStmt.run({...})`. RESEARCH Pitfall 2 HIGH — one transaction per IPC batch, ~100× faster than per-row.
      - `complete(scanId, { status, totalFiles, endedAt })` — UPDATE scans
      - `getScan(scanId)` — single-row read
      - `listFiles(scanId)` — `stmt.all(scanId)` mapped back to ScannedFile (0/1 → boolean)
      - `iterateFiles(scanId)` — `stmt.iterate(scanId)` generator yielding ScannedFile (Pitfall 7 streaming CSV foundation)
      - `deleteScansForFolder(rootFolder)` — also exposed for explicit clears
    - All statements `db.prepare(...)`; bind values via `?` or named `@param` — no string concatenation (RESEARCH Security V5).
    Extend src/main/db/connection.ts (additive, mirror getSettingsRepo): import `initScanSchema, createScanRepo, type ScanRepo`. In openDb() AFTER initSettingsSchema(db), also call `initScanSchema(db)`. Add a `scanRepoInstance` lazy singleton and `export function getScanRepo(): ScanRepo`. Update closeDb to null `scanRepoInstance` too.
    Create src/main/scan/scanRepo.test.ts in the node project. Use `new Database(':memory:')` + `initScanSchema(db)` + `createScanRepo(db)` — exactly the SettingsRepo test pattern. Reset between tests.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/scan/scanRepo</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/scan/scanRepo` passes the 7+ behaviors above
    - scanRepo.ts uses db.prepare(...) exclusively; grep "\\$\\{.*\\}.*FROM\\|\\$\\{.*\\}.*INSERT\\|\\$\\{.*\\}.*UPDATE" returns no matches (no interpolated SQL)
    - insertBatch wraps inserts in db.transaction (grep "db\\.transaction" in scanRepo.ts ≥ 1)
    - connection.ts calls initScanSchema(db) inside openDb() AFTER initSettingsSchema(db); exports getScanRepo() lazy singleton
    - iterateFiles is a generator-style cursor (uses stmt.iterate) so Plan 02-03 csvExport can stream without buffering
    - Test reads scanRepo.ts source file once and asserts no template-literal SQL pattern (defence-in-depth for V5)
  </acceptance_criteria>
  <done>scanRepo factored exactly like settingsRepo; batched inserts proven; iterator cursor ready for CSV streaming; one-scan-per-folder semantics tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: scanWorker entry + ScanController (SCAN-05 backbone, single-active-scan)</name>
  <files>src/main/workers/scanWorker.ts, src/main/scan/controller.ts, src/main/scan/controller.test.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (System Architecture Diagram; Pattern 2 worker entry; ScanController sketch; Pitfalls 1, 4 (symlink loops handled by readdirp v5), 5 (batched IPC); Open Question 4 single-active-scan)
    - src/main/workers/scanCore.ts (from Task 3 — rowFromMetadata, errorRow, AUDIO_EXTS)
    - src/main/scan/scanRepo.ts (from Task 4 — insertBatch + replaceScanForFolder + complete)
    - src/shared/ipc-types.ts (ScanEvent, ScannedFile, IpcChannels.ScanEvent from Task 2)
  </read_first>
  <behavior>
    - Test (controller, mocked spawnWorker + repo + send): controller.start('/Music') returns a UUID scanId; calls repo.replaceScanForFolder(scanId, '/Music', startedAt); spawns the worker with workerData={folder, scanId}
    - Test: when the mocked worker emits {type:'rows', scanId, rows: [r1,r2]}, controller calls repo.insertBatch([r1,r2], scanId) AND send(IpcChannels.ScanEvent, {type:'rows', scanId, rows:[r1,r2]}) — both, in that order (persist then notify)
    - Test: when the mocked worker emits {type:'done', scanId, totalFiles:42, durationMs:1234}, controller calls repo.complete(scanId, {status:'done', totalFiles:42, endedAt:NUMBER}) and forwards {type:'done',...} to send; clears its `activeScan` field
    - Test: when the mocked worker emits {type:'error', scanId, message:'boom'}, controller calls repo.complete(scanId, {status:'error', ...}) and forwards the error event; clears activeScan
    - Test (single-active-scan, planner decision on Open Question 4): controller.start(folder) while a prior scan is running → calls priorWorker.postMessage({type:'cancel'}) + priorWorker.terminate(), waits for it to settle, then spawns the new worker with a new scanId. Locks the v1 UX: starting a scan while one runs cancels-and-restarts (no concurrent scans)
    - Test: controller.cancel(scanId) on the active scan posts {type:'cancel'} to the worker, terminates it, sends {type:'cancelled', scanId} to send, repo.complete(scanId, {status:'cancelled', ...}); on a non-active scanId, no-op
    - Test: spawnWorker is injectable — production wires `new Worker(path.join(__dirname, 'workers/scanWorker.js'), { workerData })`; the test passes a fake that returns an EventEmitter-like object with .postMessage/.terminate
  </behavior>
  <action>
    Create src/main/workers/scanWorker.ts as the worker entry — implements RESEARCH Pattern 2 verbatim:
    - imports `{ parentPort, workerData } from 'node:worker_threads'`, `{ readdirp } from 'readdirp'` (v5 named export), `{ parseFile } from 'music-metadata'`, `{ stat } from 'node:fs/promises'` (only if entry.stats missing), and the pure helpers from `./scanCore` (relative — co-located in the same compiled dir `out/main/workers/`).
    - Top-level IIFE async function: open `readdirp(folder, { type:'files', fileFilter: (e) => AUDIO_EXTS.includes(path.extname(e.basename).toLowerCase()), alwaysStat: true })`. For each entry: if `cancelled` break; try `parseFile(entry.fullPath, { duration: true, skipCovers: true })` (RESEARCH Pitfall 3 — both flags needed for reliable durationSeconds on VBR MP3s); push `rowFromMetadata(entry.fullPath, entry.stats.size, meta)` OR catch errors → push `errorRow(entry.fullPath, entry.stats?.size ?? 0, err.message)` (failed-parse rows still emitted — planner decision on Open Question 6). Flush buffer when `buffer.length >= 50 || Date.now() - lastFlush >= 200` (RESEARCH Pitfall 5 batching).
    - On completion: flush remainder; if cancelled post `{type:'cancelled', scanId}`, else post `{type:'done', scanId, totalFiles, durationMs}`. Top-level try/catch around the whole flow posts `{type:'error', scanId, message}` so the controller cleanup path is always reached (Security threat in RESEARCH — "malformed audio crashes worker").
    - Listen for `parentPort.on('message', m => { if (m?.type === 'cancel') cancelled = true })`.
    Create src/main/scan/controller.ts:
    - `export interface ScanControllerDeps { spawnWorker: (folder: string, scanId: string) => WorkerLike; repo: ScanRepo; send: (channel: string, payload: ScanEvent) => void; now?: () => number }` where WorkerLike is `{ on(event, cb): this; postMessage(msg): void; terminate(): Promise<number> }`.
    - `export function createScanController(deps): ScanController` with `start(folder): Promise<string>`, `cancel(scanId): Promise<void>`. Maintains `activeScan: { scanId, worker } | null`. start: if activeScan exists, cancel it and await the prior worker's settle before continuing (single-active-scan). Generate `scanId = crypto.randomUUID()`; call `repo.replaceScanForFolder(scanId, folder, now())`; spawn worker; wire `worker.on('message', (msg: ScanEvent) => …)` switching on msg.type to do `insertBatch + send` for rows, `complete + send + clear active` for done/cancelled/error.
    Create controller.test.ts (node project). Mock spawnWorker with a `new EventEmitter()` shaped object that also has `postMessage = vi.fn()` and `terminate = vi.fn(() => Promise.resolve(0))`. Mock repo with vi.fn() for replaceScanForFolder/insertBatch/complete. Mock send = vi.fn(). Drive the controller, then call `worker.emit('message', {...})` to simulate batches, and assert call ordering/arguments.
    DO NOT instantiate a real Worker in tests — keep the test in the node env, no electron import, no real readdirp.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/scan/controller</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/scan/controller` passes all 6+ behaviors above (start/persist+forward/done/error/single-active-cancel-and-restart/cancel-noop)
    - controller.ts has zero import of 'electron' (handlers in Task 6 own the webContents wiring); spawnWorker, repo, send are all injected
    - scanWorker.ts imports only node built-ins + readdirp + music-metadata + ./scanCore (grep — no electron, no scanRepo)
    - The `insertBatch` call happens BEFORE the `send` call inside the 'rows' branch (test asserts call order via vi.fn().mock.invocationCallOrder)
    - On message 'done'/'cancelled'/'error', controller.activeScan is cleared (next start works without invoking cancel path)
    - `npm run build` re-emits out/main/workers/scanWorker.js (worker source is no longer the empty stub)
  </acceptance_criteria>
  <done>End-to-end backbone wired and unit-tested with a fake Worker: spawn → batched rows → persist → forward → done/cancel/error; single-active-scan locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: scan IPC handlers + main wiring (folder allowlist, webContents binding)</name>
  <files>src/main/ipc/scan.ts, src/main/ipc/scan.test.ts, src/main/index.ts</files>
  <read_first>
    - .planning/phases/02-scanning/02-RESEARCH.md (Pitfall 8 MEDIUM — renderer-supplied folder must equal settings.rootFolder; Security Domain V4/V5; "registerScanHandlers" responsibility)
    - src/main/scan/controller.ts (from Task 5)
    - src/main/db/connection.ts (getSettingsRepo + getScanRepo from Task 4)
    - src/main/ipc/dialog.ts, src/main/ipc/settings.ts (Phase 1 handler pattern + handler unit-test pattern)
    - src/main/index.ts (after-app-ready handler registration site)
  </read_first>
  <behavior>
    - Test (pure handler, injected deps): scanStartHandler({controller, settingsRepo}, folder) when folder !== settingsRepo.get('rootFolder') throws (or returns an error result); controller.start NOT called (RESEARCH Pitfall 8 — attacker-chosen path)
    - Test: scanStartHandler with folder === persisted rootFolder calls controller.start(folder) and returns the resulting scanId
    - Test: scanStartHandler when typeof folder !== 'string' throws (V5 input validation; Phase 1 idiom from settings:set-folder)
    - Test: scanCancelHandler with a non-string scanId throws; with a string scanId calls controller.cancel(scanId)
    - Test: the registerScanHandlers function (when called) registers exactly four `ipcMain.handle` channel names matching IpcChannels.ScanStart/ScanCancel/ScanExportCsv and an event-forwarder wired via setSender — assert via a fake ipcMain that records `.handle(channel, fn)` calls
    - The scan:export-csv handler is a stub in this plan that throws `Error('not implemented yet — Plan 02-03')`; it exists so the IPC surface is complete and the renderer's TS compile in Plan 02-02 sees the contract. Plan 02-03 will replace the stub with the streaming CSV implementation.
  </behavior>
  <action>
    Create src/main/ipc/scan.ts following the Phase 1 handler pattern (pure functions + ipcMain.handle registration):
    - `export function scanStartHandler(deps: { controller; settingsRepo }, folder: unknown): Promise<string>` — runtime-validates `typeof folder === 'string'`; asserts `folder === settingsRepo.get('rootFolder')` (RESEARCH Pitfall 8 — without this a compromised renderer scans any disk location); returns `controller.start(folder)`.
    - `export function scanCancelHandler(deps: { controller }, scanId: unknown): Promise<void>` — validates string then `controller.cancel(scanId)`.
    - `export function scanExportCsvHandler(_deps, _scanId): Promise<string | null>` — stub: `throw new Error('scan:export-csv not implemented in Plan 02-01; see Plan 02-03')`.
    - `export function registerScanHandlers(opts: { ipcMain; controller; settingsRepo; getSender: () => WebContents | null }): void` — calls `ipcMain.handle` for the three invoke channels passing `(_e, arg) => xxxHandler(deps, arg)`. Wires the controller's `send` dep (constructed here) to `getSender()?.send(IpcChannels.ScanEvent, payload)`.
    Update src/main/index.ts:
    - import `createScanController` and `registerScanHandlers`; import `getScanRepo, getSettingsRepo` from db/connection.
    - Inside `app.whenReady().then(...)` AFTER createWindow(): keep a `mainWindow` reference (refactor createWindow to return BrowserWindow), construct the controller with `spawnWorker = (folder, scanId) => new Worker(path.join(__dirname, 'workers/scanWorker.js'), { workerData: { folder, scanId } })` (RESEARCH Pitfall 1 — path relative to the bundled `out/main/index.js`). Pass repo from getScanRepo(); send via the controller-internal forwarder; settingsRepo from getSettingsRepo().
    - Call `registerScanHandlers({ ipcMain, controller, settingsRepo, getSender: () => mainWindow?.webContents ?? null })` after the existing `registerDialogHandlers()` + `registerSettingsHandlers()` calls.
    Create src/main/ipc/scan.test.ts in the node project. Construct fake controller/settingsRepo/ipcMain (vi.fn() shapes). Exercise the four behaviors above against the pure handlers + the registration function. No real Electron import in tests.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/ipc/scan</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/ipc/scan` passes all behaviors (folder allowlist enforcement is the load-bearing one)
    - scanStartHandler rejects (throw or rejected promise) when folder !== settingsRepo.get('rootFolder') (Pitfall 8 mitigation; test must assert controller.start was NOT called in that path)
    - scan:export-csv handler is wired but throws "not implemented" — Plan 02-03 replaces this
    - src/main/index.ts constructs the worker via `new Worker(path.join(__dirname, 'workers/scanWorker.js'), { workerData })` (grep "workers/scanWorker.js")
    - registerScanHandlers is called AFTER registerDialogHandlers + registerSettingsHandlers (preserves Phase 1 ordering)
    - `npm run build` exits 0; the existing Phase 1 IPC handlers still register (no regression — verified by checkpoint below)
  </acceptance_criteria>
  <done>Bridge surface live in main; folder-allowlist guard tested; controller wired to the real Worker constructor with the bundled path; CSV stub in place for Plan 02-03 to fill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 2 main-process backbone end-to-end through the typed bridge: extended IpcChannels + DjUtilsApi.scan namespace, scans + scanned_files tables behind the createXRepo+initXSchema factory pattern, pure scanCore (multi-format), a Node worker that streams readdirp + music-metadata in batches, a ScanController with single-active-scan semantics, scan:start with a folder allowlist gate, and the worker entry bundled to out/main/workers/scanWorker.js so it loads in both dev and packaged builds (RESEARCH Pitfall 1 HIGH). No UI yet — verification is purely via the DevTools console + the existing Phase 1 nav shell.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. The Phase 1 three-tool nav still renders, no DevTools errors. Confirm: `await window.djUtils.getRootFolder()` still works (no Phase 1 regression).
    2. In the DevTools console: `await window.djUtils.setRootFolder('/path/to/a/small/music/folder')` (any folder with 5–20 audio files). Then: `const off = window.djUtils.scan.onEvent(e => console.log('SCAN EVENT', e)); const id = await window.djUtils.scan.start('/path/to/a/small/music/folder'); console.log('scanId', id)`. Expect a sequence of `{type:'rows', rows:[…]}` events ending with `{type:'done', totalFiles:N}`.
    3. Confirm folder-allowlist gate: `await window.djUtils.scan.start('/etc')` (or any path that is NOT the persisted rootFolder) — expect the promise to reject with an error mentioning rootFolder mismatch (RESEARCH Pitfall 8 mitigation working).
    4. Confirm worker bundling: `ls out/main/workers/scanWorker.js` after `npm run build` — file exists and is non-trivial (RESEARCH Pitfall 1).
    5. Confirm CSV stub is wired but unimplemented: `await window.djUtils.scan.exportCsv(id)` should reject with "not implemented yet — Plan 02-03".
    6. Call `off()` then quit. No worker process should remain (run `ps | grep scanWorker` for sanity — should be empty).
  </how-to-verify>
  <resume-signal>Type "approved" if (a) Phase 1 still works, (b) a real folder produces row events and a done event, (c) the allowlist gate rejects non-rootFolder paths, (d) the bundled worker file exists, (e) no orphan worker after quit. Otherwise describe which step failed (most likely: bundled worker path → revisit electron.vite.config.ts; or ABI mismatch → run `npx electron-rebuild -f -w better-sqlite3` per STATE.md Known Risks).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload → main | Phase 1 boundary preserved verbatim; the new scan namespace crosses the same contextBridge with the same sandbox:true + contextIsolation:true posture. |
| main → filesystem (selected folder) | NEW SURFACE: main now traverses an arbitrary directory tree under the persisted rootFolder. Worker holds no DB / no IPC to renderer directly — it only postMessages back to main. |
| main → spawned Worker | NEW SURFACE: main spawns a Node Worker via `new Worker(file)`. workerData is the {folder, scanId} pair only; no user-controlled code is loaded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | BrowserWindow | mitigate (carried from Plan 01-01) | contextIsolation:true, nodeIntegration:false, sandbox:true preserved; no change to webPreferences |
| T-1-02 | Tampering | settings:set-folder handler | mitigate (carried) | Existing Phase 1 type-check unchanged |
| T-1-03 | Tampering | parameterised SQL | mitigate (carried) | scanRepo continues the prepared-statements-only invariant (grep gate in Task 4) |
| T-1-04 | Elevation of Privilege | renderer components | mitigate (carried) | Phase 1 invariant preserved; Plan 02-02 will re-assert via its own grep gate |
| T-2-01 | Information Disclosure / Tampering | scan:start handler accepts arbitrary folder path | mitigate | Task 6 scanStartHandler asserts `folder === settingsRepo.get('rootFolder')`. Without this, a compromised renderer could read any disk location (RESEARCH Pitfall 8). Test enforces the rejection path. |
| T-2-02 | DoS | Malformed audio file crashes worker | mitigate | per-file try/catch in scanWorker.ts emits errorRow and continues; top-level try/catch posts {type:'error'} so the controller cleanup path always fires (no dangling worker) |
| T-2-03 | DoS | Symlink-loop traversal | mitigate | readdirp v5 inode-cycle protection (RESEARCH Pitfall 4 + sources). Acceptable for v1; network-volume hangs documented as known limitation per RESEARCH. |
| T-2-04 | DoS | Unbounded memory parsing huge file | mitigate | parseFile called with `{ duration:true, skipCovers:true }` to avoid loading large cover-art atoms (Pitfall 3) |
| T-2-05 | Elevation of Privilege | Worker file path tampering | mitigate | spawnWorker uses `path.join(__dirname, 'workers/scanWorker.js')` — never accepts a renderer-supplied path; the worker source is bundled by electron-vite at a fixed location (Pitfall 1) |
| T-2-06 | Elevation of Privilege | New IPC channels expand attack surface | mitigate | All four channels registered in IpcChannels const + typed in DjUtilsApi.scan + runtime-validated in handlers (Phase 1 idiom); test covers V5 input validation per handler |
| T-1-SC | Tampering | npm installs (music-metadata, readdirp, csv-stringify, @tanstack/react-virtual) | mitigate | RESEARCH Package Legitimacy Audit ran slopcheck on all four — 4/4 [OK], 10+ year track records on three, 3+ years on @tanstack/react-virtual; zero postinstall scripts |
</threat_model>

<verification>
- `npm run build` green; both `out/main/index.js` and `out/main/workers/scanWorker.js` exist (Pitfall 1 verified).
- `npm run test -- --run src/main/workers/scanCore` green (SCAN-01/02/03 at function level, multi-format).
- `npm run test -- --run src/main/scan/scanRepo` green (batched transaction insert + iterator + replace-per-folder).
- `npm run test -- --run src/main/scan/controller` green (spawn / persist-then-forward / done-error-cancel / single-active-scan).
- `npm run test -- --run src/main/ipc/scan` green (folder allowlist gate is the load-bearing assertion).
- grep: no `import.*worker_threads` in scanCore.ts; no template-literal SQL in scanRepo.ts; preload imports only 'electron' and shared types.
- Manual checkpoint: real folder produces row events + done; non-rootFolder path rejected; no orphan worker after quit.
</verification>

<success_criteria>
- SCAN-01/02/03 observable through window.djUtils.scan.onEvent on a real folder (format/bitrate/sizeBytes/sampleRate/durationSeconds/has* all populated per file).
- SCAN-05 mechanism in place: parse work runs in a worker_threads.Worker, main process only persists+forwards in batches; the Phase 1 nav UI remains responsive during a real scan.
- Single-active-scan locked at the controller level (RESEARCH Open Question 4 decision).
- One scan per root folder, replaced on rescan (RESEARCH Open Question 5 decision).
- Failed-parse rows still emitted with parsedOk=false + errorMessage so the UI can surface them in Plan 02-02 (RESEARCH Open Question 6 decision).
- Worker bundling fix (RESEARCH Pitfall 1 HIGH) verified by file existence + manual scan working in dev.
- Folder-allowlist gate (RESEARCH Pitfall 8) verified by reject-path test + manual non-rootFolder attempt.
</success_criteria>

<output>
Create `.planning/phases/02-scanning/02-01-SUMMARY.md` when done.
</output>
