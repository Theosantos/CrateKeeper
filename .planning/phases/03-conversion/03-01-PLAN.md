---
phase: 03-conversion
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/shared/ipc-types.ts
  - src/main/db/connection.ts
  - src/main/conversion/conversionRepo.ts
  - src/main/conversion/conversionRepo.test.ts
  - src/main/conversion/presets.ts
  - src/main/conversion/presets.test.ts
  - src/main/conversion/ffmpegPath.ts
  - src/main/conversion/ffmpegPath.test.ts
  - src/main/conversion/controller.ts
  - src/main/conversion/controller.test.ts
  - src/main/workers/conversionCore.ts
  - src/main/workers/conversionCore.test.ts
  - src/main/workers/conversionWorker.ts
  - src/main/workers/__fixtures__/tagged.m4a
  - src/main/conversion/tagRoundtrip.test.ts
  - src/main/ipc/conversion.ts
  - src/main/ipc/conversion.test.ts
  - src/main/index.ts
  - src/preload/index.ts
  - electron.vite.config.ts
autonomous: false
requirements: [CONV-01, CONV-02, CONV-03, CONV-04, CONV-06]
user_setup: []

must_haves:
  truths:
    - "Running `npm run build` emits a bundled worker at `out/main/workers/conversionWorker.js` alongside `out/main/index.js`"
    - "PRESETS exports exactly 5 entries: mp3-320, mp3-v0, aac-256, flac, wav (per D-CONV-FORMAT)"
    - "buildFfmpegArgs for any .mp3 target preset includes both `-map_metadata 0` and `-id3v2_version 3` flags in that order (Rekordbox lock)"
    - "parseFfmpegTimeProgress returns the LAST `time=HH:MM:SS.MS` match per chunk as total seconds; null when absent"
    - "computeOutputPath returns `{outputDir}/{basename}.{preset.extension}` and never escapes outputDir"
    - "resolveFfmpegPath rewrites `app.asar` → `app.asar.unpacked` only when isPackaged=true; passthrough otherwise"
    - "initConversionSchema creates `conversions` and `conversion_files` tables; safe to call twice"
    - "conversionRepo.insertFileBatch wraps inserts in db.transaction; updateFile is parameterised"
    - "ConversionController spawns a Worker, forwards file events to webContents AND persists conversion_files transitions in that order (persist-then-forward)"
    - "ConversionController.start while a batch is active rejects with `BatchAlreadyActive` (single-active, per D-CONV-CONCURRENCY)"
    - "ConversionController.cancel posts {type:'cancel'} to the worker FIRST, then awaits worker settle BEFORE terminating (Pitfall 5)"
    - "Tag round-trip on tagged.mp3 → tagged.mp3 (mp3-320 preset) preserves TIT2/TPE1/TALB/TCON/TBPM/TKEY/COMM via music-metadata read-back"
    - "Tag round-trip on sample.flac → flac→mp3 (mp3-320) maps Vorbis TITLE/ARTIST/ALBUM/GENRE/BPM/INITIALKEY/COMMENT to ID3v2.3 frames"
    - "window.djUtils.conversion namespace exposes start/cancel/listResumable/resume/onEvent and rejects file paths that don't resolve under settings.rootFolder"
  artifacts:
    - path: "src/shared/ipc-types.ts"
      provides: "Extended IpcChannels with ConversionStart/ConversionCancel/ConversionListResumable/ConversionResume/ConversionEvent; Preset, ConversionFileStatus, ConversionEvent, ResumableBatch types; DjUtilsApi.conversion namespace"
      contains: "conversion:"
    - path: "src/main/conversion/presets.ts"
      provides: "PRESETS const (5 entries), buildFfmpegArgs(src,out,preset), presetSlugOf(preset), getPresetBySlug(slug)"
      contains: "PRESETS"
    - path: "src/main/conversion/ffmpegPath.ts"
      provides: "resolveFfmpegPath({rawPath, isPackaged}) helper with app.asar.unpacked rewrite"
      contains: "app.asar.unpacked"
    - path: "src/main/conversion/conversionRepo.ts"
      provides: "createConversionRepo factory: createConversion, insertFileBatch (transaction), updateFile, bumpHeartbeat, complete, listFiles, findResumable, markStaleAsCrashed, getResumablePending"
      contains: "db.transaction"
    - path: "src/main/conversion/controller.ts"
      provides: "createConversionController({spawnWorker, repo, send, resolveFfmpegPath, now}): start/cancel/resume; single-active; persist-then-forward; heartbeat timer"
      contains: "BatchAlreadyActive"
    - path: "src/main/workers/conversionCore.ts"
      provides: "Pure functions: parseFfmpegTimeProgress, parseFfmpegDuration, computeOutputPath (no node:worker_threads / no node:child_process imports)"
      contains: "parseFfmpegTimeProgress"
    - path: "src/main/workers/conversionWorker.ts"
      provides: "Worker entry: child_process pool of min(cpus-1,4) slot-runners spawning ffmpeg, stderr time-parse, batched progress (20/200ms), per-file completion flushed immediately; honours {type:'cancel'}"
      contains: "parentPort"
    - path: "src/main/ipc/conversion.ts"
      provides: "registerConversionHandlers: conversion:start (asserts every filePath resolves under settings.rootFolder), conversion:cancel, conversion:list-resumable (stub returns []), conversion:resume (stub throws), wires webContents to controller"
      contains: "rootFolder"
    - path: "electron.vite.config.ts"
      provides: "main.build.rollupOptions.input includes workers/conversionWorker so it bundles to out/main/workers/conversionWorker.js"
      contains: "conversionWorker"
    - path: "src/preload/index.ts"
      provides: "Extended djUtils with conversion.{start,cancel,listResumable,resume,onEvent} bound via shared IpcChannels"
      contains: "conversion:"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/main/ipc/conversion.ts"
      via: "ipcRenderer.invoke('conversion:start' | 'conversion:cancel' | 'conversion:list-resumable' | 'conversion:resume') and ipcRenderer.on('conversion:event')"
      pattern: "conversion:(start|cancel|list-resumable|resume|event)"
    - from: "src/main/ipc/conversion.ts"
      to: "src/main/conversion/controller.ts"
      via: "ConversionController.start({rootFolder, filePaths, preset})"
      pattern: "ConversionController|createConversionController"
    - from: "src/main/conversion/controller.ts"
      to: "src/main/workers/conversionWorker.ts"
      via: "new Worker(path.join(__dirname, 'workers/conversionWorker.js'), { workerData })"
      pattern: "workers/conversionWorker"
    - from: "src/main/conversion/controller.ts"
      to: "src/main/conversion/conversionRepo.ts"
      via: "repo.createConversion, repo.insertFileBatch, repo.updateFile, repo.bumpHeartbeat, repo.complete"
      pattern: "ConversionRepo"
    - from: "src/main/db/connection.ts"
      to: "src/main/conversion/conversionRepo.ts"
      via: "initConversionSchema(db) at openDb() + getConversionRepo() lazy singleton"
      pattern: "initConversionSchema"
    - from: "src/main/conversion/controller.ts"
      to: "src/main/conversion/ffmpegPath.ts"
      via: "resolveFfmpegPath({rawPath: require('ffmpeg-static'), isPackaged: app.isPackaged}) — resolved once at controller construction in main, passed to worker via workerData"
      pattern: "resolveFfmpegPath"
---

<objective>
Ship the entire main-process backbone for Phase 3 conversion as a single end-to-end vertical slice (no UI): extend the typed IPC bridge with a `conversion` namespace, add `conversions` + `conversion_files` SQLite tables behind the existing `createXRepo(db) + initXSchema(db)` factory pattern, implement the preset registry + pure conversionCore (ffmpeg argv builder, stderr time parser, output-path computer), the `conversionWorker.ts` with an intra-batch child_process pool that spawns `min(cpus-1, 4)` ffmpeg processes against the bundled `ffmpeg-static` binary, and a `ConversionController` that owns worker lifecycle + heartbeat + batched persist-then-forward + IPC event forwarding. Also bundle the worker via `electron.vite.config.ts` (Pitfall 1 carry-forward).

Purpose: This is the entire non-UI surface Phase 3 needs to satisfy CONV-01/02/03/04/06 at the bridge level. Plan 03-02 renders what this exposes; Plan 03-03 layers resume-from-crash UX on top of the same controller + repo. Tag round-trip (CONV-06) is proven here via an integration test that spawns the real bundled ffmpeg binary and reads back tags with `music-metadata`.

Output: Typed `window.djUtils.conversion` API; worker bundled in dev + packaged builds; persisted conversion rows + per-file rows in better-sqlite3; ffmpeg pool with SIGTERM-first cancel and partial-output unlink; unit-test coverage on the pure core + repo + presets + ffmpeg-path helper + controller (mocked worker + mocked ffmpeg); integration test proving MP3→MP3 and FLAC→MP3 Rekordbox tag preservation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-conversion/03-CONTEXT.md
@.planning/phases/03-conversion/03-RESEARCH.md
@.planning/phases/03-conversion/03-VALIDATION.md
@.planning/phases/02-scanning/02-01-SUMMARY.md
@.planning/phases/02-scanning/02-03-SUMMARY.md
@src/shared/ipc-types.ts
@src/main/index.ts
@src/main/db/connection.ts
@src/main/db/settingsRepo.ts
@src/main/scan/controller.ts
@src/main/scan/scanRepo.ts
@src/main/workers/scanWorker.ts
@src/main/ipc/scan.ts
@src/preload/index.ts
@electron.vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle conversionWorker entry + add tagged.m4a fixture</name>
  <files>electron.vite.config.ts, src/main/workers/conversionWorker.ts, src/main/workers/__fixtures__/tagged.m4a</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Pitfall 1 — asar.unpacked path; Runtime State Inventory > Build artifacts; Environment Availability > tagged.m4a)
    - electron.vite.config.ts (current — already declares main entries `index` and `workers/scanWorker`; extend additively)
    - src/main/workers/__fixtures__/tagged.mp3 (existing fixture — source for the M4A fixture)
  </read_first>
  <action>
    Extend electron.vite.config.ts: add a third entry to `main.build.rollupOptions.input` named `'workers/conversionWorker': resolve('src/main/workers/conversionWorker.ts')`. Preserve the two existing entries (index, workers/scanWorker) verbatim. Without this entry the Worker constructor in production throws ERR_MODULE_NOT_FOUND — Phase 2 Pitfall 1 carry-forward.

    Stub `src/main/workers/conversionWorker.ts` with `export {}` so the build resolves the entry. Task 6 fills it with the real worker implementation; the stub here is just to make the build green for the Wave 0 verify gate.

    Generate `src/main/workers/__fixtures__/tagged.m4a` from the existing `tagged.mp3` fixture so the tag-round-trip integration test in Task 7 has an M4A source. Use the bundled ffmpeg-static binary: `node -e "require('child_process').spawnSync(require('ffmpeg-static'), ['-y','-i','src/main/workers/__fixtures__/tagged.mp3','-c:a','aac','-b:a','256k','-map_metadata','0','-metadata','TBPM=128','-metadata','TKEY=8A','src/main/workers/__fixtures__/tagged.m4a'])"`. Commit the binary (<50 KB).

    Run `npm run build` and confirm `out/main/workers/conversionWorker.js` exists alongside `out/main/workers/scanWorker.js` and `out/main/index.js`.
  </action>
  <verify>
    <automated>npm run build && test -f out/main/workers/conversionWorker.js && test -f out/main/workers/scanWorker.js && test -f out/main/index.js && test -f src/main/workers/__fixtures__/tagged.m4a</automated>
  </verify>
  <acceptance_criteria>
    - electron.vite.config.ts main block declares all three entries (index, workers/scanWorker, workers/conversionWorker)
    - `npm run build` exits 0; the three bundled JS files exist under `out/main/`
    - `src/main/workers/__fixtures__/tagged.m4a` exists, is <100 KB, and `git ls-files` includes it
    - Existing tagged.mp3, untagged.mp3, sample.flac fixtures unchanged (Phase 2 SCAN-03 regression guard)
  </acceptance_criteria>
  <done>Worker entry bundled (Pitfall 1 fixed); M4A fixture ready for tag-round-trip test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend IPC type surface (conversion namespace) + preload bridge wiring</name>
  <files>src/shared/ipc-types.ts, src/preload/index.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Code Examples §1 — IPC channel extension; ConversionEvent union; Preset interface; DjUtilsConversionApi shape)
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED IPC namespace = conversion.*)
    - src/shared/ipc-types.ts (current IpcChannels + DjUtilsApi.scan + Phase 1 surface — preserve additively)
    - src/preload/index.ts (current djUtils.scan exposure; sandbox:true preload constraint — only 'electron' + shared types imports allowed)
  </read_first>
  <behavior>
    - Additive type-level change — runtime tests live in Tasks 3-7. Compile-time test = `npm run build` typechecks renderer/main/preload against extended DjUtilsApi.
    - The preload `onEvent` subscribe pattern returns an unsubscribe closure (mirror Phase 2 scan.onEvent) so the renderer Zustand store in Plan 03-02 can clean up.
  </behavior>
  <action>
    Extend src/shared/ipc-types.ts ADDITIVELY — do not mutate existing channel values or DjUtilsApi members.

    Add to IpcChannels:
    - `ConversionStart: 'conversion:start'`
    - `ConversionCancel: 'conversion:cancel'`
    - `ConversionListResumable: 'conversion:list-resumable'`
    - `ConversionResume: 'conversion:resume'`
    - `ConversionEvent: 'conversion:event'` (main → renderer push)

    Add `export interface Preset { slug: string; label: string; codec: string; bitrateKbps: number | null; vbrQuality: number | null; sampleRate: number | null; extension: string }` exactly as RESEARCH §1 spells it.

    Add `export type ConversionFileStatus = 'pending' | 'running' | 'done' | 'error' | 'skipped' | 'cancelled'`.

    Add `export type ConversionEvent` as a discriminated union with five members: `progress` ({conversionId, filePath, percent, phase: 'transcoding' | 'finalizing'}), `fileDone` ({conversionId, filePath, status: ConversionFileStatus, outputPath: string | null, errorMessage: string | null}), `done` ({conversionId}), `cancelled` ({conversionId}), `error` ({conversionId, message: string}).

    Add `export interface ResumableBatch { conversionId: string; rootFolder: string; preset: Preset; outputDir: string; pendingCount: number; doneCount: number; errorCount: number; startedAt: number }`.

    Add `export interface DjUtilsConversionApi { start(params: { rootFolder: string; filePaths: string[]; preset: Preset }): Promise<string>; cancel(conversionId: string): Promise<void>; listResumable(): Promise<ResumableBatch[]>; resume(conversionId: string): Promise<void>; onEvent(cb: (e: ConversionEvent) => void): () => void }`.

    Extend `DjUtilsApi` to include `conversion: DjUtilsConversionApi` alongside the existing `scan` and Phase 1 members.

    Extend src/preload/index.ts: build a `conversion` object wrapping `ipcRenderer.invoke(IpcChannels.ConversionStart, params)` etc. For `onEvent`: stable handler closes over the cb, registers `ipcRenderer.on(IpcChannels.ConversionEvent, handler)`, returns `() => ipcRenderer.off(IpcChannels.ConversionEvent, handler)`. Compose into the existing `djUtils` object as `{ ...existing, conversion }`. DO NOT introduce @electron-toolkit/preload (LOCKED sandbox constraint from Phase 1).

    Run `npm run build` to confirm preload + main + renderer all typecheck.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - grep "conversion:" src/shared/ipc-types.ts shows the five new channels + ConversionEvent union + Preset interface
    - grep "conversion:" src/preload/index.ts shows wrapper functions wired to ipcRenderer.invoke/on using the shared IpcChannels constants (no string literals)
    - Phase 1 + Phase 2 members of IpcChannels and DjUtilsApi remain untouched
    - preload imports only 'electron' and '../shared/ipc-types' (grep — no other imports)
  </acceptance_criteria>
  <done>Typed conversion bridge surface exists end-to-end; the renderer-side TS in Plan 03-02 will see window.djUtils.conversion.* fully typed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Presets registry + ffmpeg argv builder + ffmpeg-path helper (CONV-02, CONV-06 setup)</name>
  <files>src/main/conversion/presets.ts, src/main/conversion/presets.test.ts, src/main/conversion/ffmpegPath.ts, src/main/conversion/ffmpegPath.test.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Code Examples §2 PRESETS + buildFfmpegArgs; Pattern 2 resolveFfmpegPath; Pitfall 1 asar rewrite; Pitfall 2 libfdk_aac unavailable; Tag Preservation Matrix)
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED: 5 presets, Custom escape hatch; default MP3 320; `-id3v2_version 3` for MP3 targets; sample rate preserve-source)
    - src/shared/ipc-types.ts (Preset interface from Task 2)
  </read_first>
  <behavior>
    - Test: PRESETS exports exactly 5 entries with slugs `mp3-320`, `mp3-v0`, `aac-256`, `flac`, `wav` in that order; each Preset shape complete (codec/bitrate/vbr/sampleRate/extension)
    - Test: PRESETS[0] (mp3-320) has codec='libmp3lame', bitrateKbps=320, vbrQuality=null, extension='.mp3'
    - Test: PRESETS for aac-256 has codec='aac' (NOT 'libfdk_aac' — Pitfall 2) and extension='.aac' (NOT '.m4a' — Pitfall 7 + RESEARCH Open Question 2 lock)
    - Test: buildFfmpegArgs(src, out, mp3-320) returns argv starting `-hide_banner -loglevel error -stats -y -i {src}` and contains `-c:a libmp3lame -b:a 320k` AND `-map_metadata 0 -id3v2_version 3` AND ends with the output path. The `-id3v2_version 3` flag appears ONLY when extension is '.mp3' (Rekordbox lock)
    - Test: buildFfmpegArgs for mp3-v0 emits `-q:a 0` (not `-b:a`) (VBR path)
    - Test: buildFfmpegArgs for flac and wav emits `-map_metadata 0` but NOT `-id3v2_version 3` (those targets don't carry ID3)
    - Test: buildFfmpegArgs sample rate clause — when preset.sampleRate is null the `-ar` flag is absent (preserve source); when set it appears
    - Test: getPresetBySlug('mp3-320') returns the matching Preset; getPresetBySlug('bogus') returns undefined
    - Test: presetSlugOf({slug:'custom', ...}) returns 'custom' (escape hatch for Custom forms in Plan 03-02)
    - Test: resolveFfmpegPath({rawPath:'/Users/x/app/Resources/app.asar/node_modules/ffmpeg-static/ffmpeg', isPackaged:true}) returns the same string with 'app.asar' replaced by 'app.asar.unpacked'
    - Test: resolveFfmpegPath with isPackaged=false is a passthrough (dev mode — binary lives in node_modules directly)
    - Test: resolveFfmpegPath on Windows-shaped path (`C:\\app\\app.asar\\node_modules\\ffmpeg-static\\ffmpeg.exe`) rewrites correctly (path.sep-aware)
  </behavior>
  <action>
    Create src/main/conversion/presets.ts:
    - `export const PRESETS: ReadonlyArray<Preset>` per RESEARCH Code Examples §2 — exactly 5 entries (mp3-320, mp3-v0, aac-256, flac, wav). Lock `codec: 'aac'` for aac-256 with a comment citing Pitfall 2 (libfdk_aac unavailable in ffmpeg-static).
    - `export function buildFfmpegArgs(src: string, out: string, preset: Preset): string[]` exactly per RESEARCH §2. Argv ORDER MATTERS for ffmpeg — `-i` before output codec flags, `-map_metadata 0` before `-id3v2_version 3`, output path LAST.
    - `export function getPresetBySlug(slug: string): Preset | undefined` — linear scan over PRESETS.
    - `export function presetSlugOf(preset: Preset): string` — trivial accessor (useful for output-dir composition).

    Create src/main/conversion/ffmpegPath.ts per RESEARCH Pattern 2:
    - `export function resolveFfmpegPath(opts: { rawPath: string; isPackaged: boolean }): string` — uses `path.sep` (not hardcoded '/') so the rewrite works on Windows. The function is pure and synchronous; no fs touches.

    Create src/main/conversion/presets.test.ts and src/main/conversion/ffmpegPath.test.ts (vitest, node project). Cover the behaviors above. For the Windows-path test, hardcode the `path.win32.sep` shape — do NOT skip on non-Win runners.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/conversion/presets src/main/conversion/ffmpegPath</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/conversion/presets src/main/conversion/ffmpegPath` passes all behaviors above
    - PRESETS array literal has exactly 5 entries (grep + length-assertion test)
    - buildFfmpegArgs result for mp3-320 contains both `-map_metadata 0` and `-id3v2_version 3` in that order (string-index assertion)
    - buildFfmpegArgs result for flac/wav does NOT contain `-id3v2_version`
    - aac-256 codec is `'aac'` (not `'libfdk_aac'`) — explicit assertion citing Pitfall 2 in the test name
    - resolveFfmpegPath has no fs/process side effects (pure function — easy unit test)
  </acceptance_criteria>
  <done>Preset registry + ffmpeg argv builder + asar.unpacked path helper proven; CONV-02 mapping locked at function level; Rekordbox `-id3v2_version 3` enforced for MP3 targets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: conversionRepo — schema + persist-then-forward batches + heartbeat (CONV-04 backbone)</name>
  <files>src/main/conversion/conversionRepo.ts, src/main/conversion/conversionRepo.test.ts, src/main/db/connection.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Code Examples §4 SQLite schema; Pattern 3 heartbeat; Pitfall 9 resume safety; Security V5 parameterised SQL)
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED tables shape including heartbeat_at column; status enums)
    - src/main/scan/scanRepo.ts (Phase 2 repo pattern — EXACTLY mirror this shape; STATE.md decision)
    - src/main/db/connection.ts (existing openDb + initScanSchema + getScanRepo — extend with the conversion symmetric pair)
    - src/shared/ipc-types.ts (Preset, ConversionFileStatus, ResumableBatch from Task 2)
  </read_first>
  <behavior>
    - Test: initConversionSchema creates `conversions` (with `heartbeat_at INTEGER NOT NULL`) + `conversion_files` (with status CHECK constraint + FK CASCADE) + `idx_conversion_files_conv`; safe to call twice (`IF NOT EXISTS`)
    - Test: createConversion(id, rootFolder, preset, outputDir, startedAt) inserts a row with status='running', heartbeat_at=startedAt, preset stored as JSON
    - Test: insertFileBatch(rows[], conversionId) wrapped in db.transaction; each row has status='pending'; second call for same (conversion_id, file_path) is INSERT OR REPLACE (idempotent retry safety on resume)
    - Test: updateFile(conversionId, filePath, {status, errorMessage, outputPath}) UPDATEs the row; status validates against the enum via DB CHECK
    - Test: bumpHeartbeat(conversionId, ts) UPDATEs heartbeat_at only (not status); silently no-ops on missing row (race with terminal)
    - Test: complete(conversionId, {status:'done'|'cancelled'|'crashed', endedAt}) UPDATEs the conversions row
    - Test: findResumable() returns crashed batches as ResumableBatch[] with aggregated pendingCount/doneCount/errorCount per-file rolled up via a GROUP BY query; sorted by startedAt DESC
    - Test: markStaleAsCrashed({thresholdMs, now}) flips `status='crashed'` for rows where `status='running' AND heartbeat_at < (now - thresholdMs)`; returns count affected
    - Test: getResumablePending(conversionId) returns file_path[] for rows where status IN ('pending', 'running', 'error') — used by Plan 03-03 resume to re-queue
    - Test: listFiles(conversionId) returns conversion_files rows in stable order with deserialised fields
    - Test: parameterised statements only (assert by reading conversionRepo.ts source — no template-literal SQL with embedded values)
    - Test: FK CASCADE — deleting a conversions row removes its conversion_files rows
  </behavior>
  <action>
    Create src/main/conversion/conversionRepo.ts mirroring createScanRepo:
    - `export function initConversionSchema(db)` runs the CREATE TABLE statements from RESEARCH §4 verbatim (including heartbeat_at + CHECK constraints + idx_conversion_files_conv + `db.pragma('foreign_keys = ON')`).
    - `export interface ConversionRow` and `export interface ConversionFileRow` for return shapes.
    - `export function createConversionRepo(db): ConversionRepo` prepares statements once and returns:
      - `createConversion({id, rootFolder, preset, outputDir, startedAt})` — INSERT with status='running', heartbeat_at=startedAt, preset_json = JSON.stringify(preset)
      - `insertFileBatch(rows, conversionId)` — `db.transaction((rows, conversionId) => for(r of rows) stmt.run({...}))`. Pitfall: one transaction per IPC batch (~100× faster).
      - `updateFile(conversionId, filePath, patch)` — single UPDATE; validate patch.status against the enum at call site (or rely on DB CHECK)
      - `bumpHeartbeat(conversionId, ts)` — UPDATE conversions SET heartbeat_at=? WHERE id=? AND status='running'
      - `complete(conversionId, {status, endedAt})` — UPDATE status + ended_at
      - `findResumable(): ResumableBatch[]` — SELECT conversions.* + COUNT aggregates from conversion_files (one GROUP BY query joining the two tables) WHERE conversions.status='crashed'. Map preset_json back to Preset via JSON.parse.
      - `markStaleAsCrashed({thresholdMs, now}): number` — UPDATE conversions SET status='crashed', ended_at=now WHERE status='running' AND heartbeat_at < (now - thresholdMs); return `result.changes`
      - `getResumablePending(conversionId): string[]` — SELECT file_path FROM conversion_files WHERE conversion_id=? AND status IN ('pending','running','error') (note: NOT 'cancelled' — cancelled batches are NOT resumable per LOCKED decision)
      - `listFiles(conversionId): ConversionFileRow[]` — SELECT all rows for that conversion in INSERT order
      - `deleteConversion(conversionId)` — DELETE FROM conversions WHERE id=? (CASCADE drops files); used by "Ignorer (supprimer)" in Plan 03-03

    Extend src/main/db/connection.ts (additive, mirror getScanRepo): import `initConversionSchema, createConversionRepo, type ConversionRepo`. In openDb() AFTER `initScanSchema(db)`, also call `initConversionSchema(db)`. Add `conversionRepoInstance` lazy singleton and `export function getConversionRepo(): ConversionRepo`. Update closeDb to null `conversionRepoInstance` too.

    Create src/main/conversion/conversionRepo.test.ts in the node project. Use `new Database(':memory:')` + `initConversionSchema(db)` + `createConversionRepo(db)`. Reset between tests.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/conversion/conversionRepo</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/conversion/conversionRepo` passes all behaviors above
    - conversionRepo.ts uses db.prepare(...) exclusively; grep `\$\{.*\}.*FROM|\$\{.*\}.*INSERT|\$\{.*\}.*UPDATE` returns no matches
    - insertFileBatch wraps inserts in db.transaction (grep "db\.transaction" in conversionRepo.ts ≥ 1)
    - connection.ts calls initConversionSchema(db) inside openDb() AFTER initScanSchema(db); exports getConversionRepo() lazy singleton
    - findResumable returns ResumableBatch[] with the aggregated counts shape required by Plan 03-03 banner
    - markStaleAsCrashed only affects status='running' rows (not 'done'/'cancelled'/'crashed' — explicit test)
    - getResumablePending excludes 'cancelled' status (LOCKED decision: cancelled batches NOT resumable)
  </acceptance_criteria>
  <done>conversionRepo factored exactly like scanRepo; batched inserts + heartbeat + crash detection + resume-pending all proven in-memory; one repo serves all three Phase 3 plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: conversionCore pure helpers (parseFfmpegTimeProgress, parseFfmpegDuration, computeOutputPath)</name>
  <files>src/main/workers/conversionCore.ts, src/main/workers/conversionCore.test.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Code Examples §3 parser; Pitfall 4 chunk-boundary stderr; Pitfall 8 path-traversal on output dir)
    - src/main/workers/scanCore.ts (Phase 2 pure-core pattern — NO worker_threads / NO electron / NO child_process imports here either)
    - src/shared/ipc-types.ts (Preset from Task 2)
  </read_first>
  <behavior>
    - Test: parseFfmpegTimeProgress('frame=1024 time=00:00:03.50 bitrate=...') returns 3.5
    - Test: parseFfmpegTimeProgress with multiple `time=` matches in one chunk returns the LAST one (rightmost wins — chunks arrive non-line-aligned per Pitfall 4)
    - Test: parseFfmpegTimeProgress with HH:MM:SS spanning hours: 'time=01:02:03.40' returns 3723.4
    - Test: parseFfmpegTimeProgress returns null on a chunk with no `time=` token
    - Test: parseFfmpegTimeProgress is stateless — TIME_RE.lastIndex resets on every call (regression test for the `g` flag bug)
    - Test: parseFfmpegDuration('Duration: 00:03:24.12, start: 0.000000, bitrate: 320 kb/s') returns 204.12
    - Test: parseFfmpegDuration returns null when no Duration line present
    - Test: computeOutputPath('/Music/song.mp3', '/Music/converted/mp3-320', mp3-320 preset) returns '/Music/converted/mp3-320/song.mp3'
    - Test: computeOutputPath strips the source extension before appending preset.extension — '/Music/song.flac' → '/Music/converted/mp3-320/song.mp3'
    - Test: computeOutputPath('/Music/sub/song.mp3', '/Music/converted/mp3-320', mp3-320) — basename only (no subfolder mirror in v1); output flattens to '/Music/converted/mp3-320/song.mp3' (per LOCKED output dir decision)
    - Test: computeOutputPath result NEVER escapes outputDir — for any source path including ones with `..` segments in the basename area, `path.resolve(result).startsWith(path.resolve(outputDir))` (defence in depth)
  </behavior>
  <action>
    Create src/main/workers/conversionCore.ts as a PURE module:
    - Imports: only `node:path` (for basename/extname/join). DO NOT import `node:worker_threads`, `node:child_process`, `node:fs`, or anything from src/main/conversion/*.
    - `export function parseFfmpegTimeProgress(chunk: string): number | null` per RESEARCH §3. Use a LOCAL regex with the `g` flag and `lastIndex = 0` reset on entry to avoid the well-known "stateful regex" pitfall.
    - `export function parseFfmpegDuration(chunk: string): number | null` — extract from the canonical `Duration: HH:MM:SS.MS, start: ...` line that ffmpeg emits once at stream open. Returns total seconds or null.
    - `export function computeOutputPath(srcPath: string, outputDir: string, preset: Preset): string` per RESEARCH §3 — `path.join(outputDir, basename(src, extname(src)) + preset.extension)`. Result is just a string; the caller (worker convertOne) checks for existence and handles the conflict policy.

    Create src/main/workers/conversionCore.test.ts (vitest, node project). Cover all behaviors above. The chunk-boundary safety is the load-bearing test for Pitfall 4 (jagged progress bars). The "never escapes outputDir" test is the defence-in-depth path-traversal test (complements the IPC-layer allowlist gate in Task 8).
  </action>
  <verify>
    <automated>npm run test -- --run src/main/workers/conversionCore</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/workers/conversionCore` passes all behaviors above
    - grep confirms conversionCore.ts has NO `import.*worker_threads`, NO `import.*child_process`, NO `import.*node:fs`, NO `import.*electron`
    - parseFfmpegTimeProgress on a multi-`time=` chunk returns the rightmost (test asserts numeric inequality between first and last `time=` values)
    - computeOutputPath result startsWith path.resolve(outputDir) for any input (assertion in test)
  </acceptance_criteria>
  <done>Pure conversionCore covers stderr parsing (CONV-03 source of truth for progress) and output-path computation; chunk-boundary correctness locked at function level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: conversionWorker — child_process pool + SIGTERM-first cancel + batched events</name>
  <files>src/main/workers/conversionWorker.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Pattern 1 worker pool sketch — implement near-verbatim; Pitfalls 3 SIGTERM cleanup, 4 chunk-boundary, 5 worker.terminate doesn't kill children, 6 heartbeat safety)
    - src/main/workers/scanWorker.ts (Phase 2 worker shape — top-level IIFE, parentPort handling, top-level try/catch)
    - src/main/workers/conversionCore.ts (parseFfmpegTimeProgress, parseFfmpegDuration, computeOutputPath from Task 5)
    - src/shared/ipc-types.ts (Preset, ConversionEvent from Task 2)
  </read_first>
  <behavior>
    No standalone unit tests on the worker itself in this task — the worker is tested indirectly via Task 7 (real ffmpeg integration) and Task 8 (controller test with mocked spawnWorker). This task implements the worker; tests live in Tasks 7 and 8.

    Worker contract (verified in Tasks 7+8):
    - Accepts workerData: `{ conversionId, files: string[], ffmpegPath, preset, outputDir, parallelism }`
    - Spawns up to `parallelism` ffmpeg child_process concurrently via `child_process.spawn(ffmpegPath, args, { stdio: ['ignore', 'ignore', 'pipe'] })`
    - Per file: emit `{type:'progress', conversionId, filePath, percent, phase:'transcoding'}` events (batched 20/200ms) AND a single `{type:'fileDone', ...}` event (flushed immediately)
    - Conflict policy: if output exists, emit `{type:'fileDone', status:'skipped', errorMessage:'output_exists'}` and skip spawn
    - On per-file error: emit `{type:'fileDone', status:'error', errorMessage:<one-line French-friendly>}` and unlink the partial output
    - On worker-level cancel ({type:'cancel'} from parentPort): SIGTERM all live children, await slot-runners, post `{type:'cancelled'}` once
    - On natural completion: post `{type:'done', conversionId}`
    - On unhandled error: top-level try/catch posts `{type:'error', conversionId, message}` so controller cleanup always fires
  </behavior>
  <action>
    Replace the Task-1 stub at `src/main/workers/conversionWorker.ts` with the full implementation. Follow RESEARCH Pattern 1 sketch verbatim with these specifics:

    Imports: `parentPort, workerData` from 'node:worker_threads'; `spawn, type ChildProcess` from 'node:child_process'; `default as fs` from 'node:fs/promises' (for unlink + access); `default as path` from 'node:path' (used by conversionCore); local `./conversionCore` for `parseFfmpegTimeProgress`, `parseFfmpegDuration`, `computeOutputPath`; local `../conversion/presets` for `buildFfmpegArgs`.

    Top-level IIFE async `run()`:
    1. Destructure workerData into `{conversionId, files, ffmpegPath, preset, outputDir, parallelism}`.
    2. State: `let cancelled = false`; `const live = new Set<ChildProcess>()`; a progress buffer + last-flush timestamp (mirror scanWorker batching primitive — BATCH_SIZE=20, BATCH_MS=200, but ONLY for `progress` events; `fileDone` events flush immediately per LOCKED decision).
    3. `parentPort.on('message', m => { if (m?.type === 'cancel') { cancelled = true; for (const c of live) try { c.kill('SIGTERM') } catch {} } })` (Pitfall 5 — SIGTERM first, terminate last).
    4. `convertOne(src)`:
       a. If cancelled, return early.
       b. `const out = computeOutputPath(src, outputDir, preset)`.
       c. Conflict check via `fs.access(out)` — if exists, post `{type:'fileDone', status:'skipped', outputPath:out, errorMessage:'output_exists'}` and return.
       d. `const args = buildFfmpegArgs(src, out, preset)`; `const child = spawn(ffmpegPath, args, { stdio: ['ignore','ignore','pipe'] })`; `live.add(child)`.
       e. Update repo proxy via `{type:'fileStart', conversionId, filePath:src}` so the controller can UPDATE conversion_files.status='running' (persist-then-forward foundation for Task 8).
       f. Stderr handler maintains a per-child string buffer; on every `data` chunk, append + extract duration once via `parseFfmpegDuration`, then extract latest time via `parseFfmpegTimeProgress`; if both known, percent = `Math.min(100, Math.round(time/duration*100))`. Push progress event into the batched buffer; flush condition `buffer.length >= 20 || Date.now() - lastFlush >= 200`.
       g. Await `new Promise<number>(resolve => child.on('exit', code => resolve(code ?? 1)))`. Remove child from `live`.
       h. If cancelled OR exitCode !== 0: `await fs.unlink(out).catch(() => undefined)` (Pitfall 3 partial cleanup). If exitCode !== 0 AND !cancelled, post `{type:'fileDone', status:'error', errorMessage:'ffmpeg exited with code ' + exitCode}`. If cancelled, do NOT post fileDone for this file — the batch-level 'cancelled' event covers it.
       i. Else (success): flush progress buffer, then post `{type:'fileDone', status:'done', outputPath:out, errorMessage:null}`.
    5. Slot pool: `const slotRunner = async () => { while (true) { const f = next(); if (f === null) return; try { await convertOne(f) } catch (err) { post fileDone status:'error' } } }`. `await Promise.all(Array.from({length: parallelism}, slotRunner))`.
    6. Final flush of progress buffer. Post `cancelled ? {type:'cancelled', conversionId} : {type:'done', conversionId}`.

    Wrap the entire `run()` in a top-level try/catch that posts `{type:'error', conversionId, message}` (Pitfall 5 safety net + scanWorker pattern carry-forward).

    DO NOT import `electron`. DO NOT import `src/main/conversion/conversionRepo` — the worker only posts messages; the controller owns persistence. DO NOT import fluent-ffmpeg.

    Add an internal event type `{type:'fileStart', conversionId, filePath}` emitted right before spawn so the controller can flip the conversion_files row to status='running' (extends ConversionEvent in src/shared/ipc-types.ts — IF this isn't in Task 2, add it now; it's main-internal but typed consistently). Confirm: the renderer-facing ConversionEvent in Task 2 may omit 'fileStart' if cleaner (it's a controller-internal message); planner discretion — pick one and keep it consistent.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `out/main/workers/conversionWorker.js` is non-trivial (>3 KB, no longer the stub)
    - grep src/main/workers/conversionWorker.ts: NO `import.*electron`, NO `import.*fluent-ffmpeg`, NO `import.*conversionRepo` (worker is pure Node, talks only via postMessage)
    - grep src/main/workers/conversionWorker.ts shows `SIGTERM` literal AND `fs.unlink` for the cleanup path (Pitfalls 3 + 5 mitigation visible in source)
    - The progress-event batching uses BATCH_SIZE=20 and BATCH_MS=200 constants (grep `20` and `200`)
    - fileDone events are NOT in the batched buffer (flushed immediately per LOCKED decision)
  </acceptance_criteria>
  <done>Worker implements the intra-batch ffmpeg pool with SIGTERM-first cancel, partial-output cleanup, chunk-aware stderr parsing, and batched/immediate event split. Real ffmpeg behavior verified in Task 7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Tag round-trip integration test (CONV-06 verification with real ffmpeg)</name>
  <files>src/main/conversion/tagRoundtrip.test.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Tag Preservation Matrix — three HIGH-confidence rows; Assumption A2 M4A risk; Validation Architecture > Tests Map for CONV-06)
    - src/main/workers/__fixtures__/tagged.mp3 (existing Phase 2 fixture with TIT2/TPE1/TALB/TCON/TBPM/TKEY/COMM populated)
    - src/main/workers/__fixtures__/sample.flac (existing FLAC fixture)
    - src/main/workers/__fixtures__/tagged.m4a (from Task 1)
    - src/main/conversion/presets.ts (buildFfmpegArgs, PRESETS from Task 3)
  </read_first>
  <behavior>
    - Test (MP3 → MP3 320, HIGH confidence): spawn the real ffmpeg-static binary with `buildFfmpegArgs(tagged.mp3, /tmp/out.mp3, mp3-320 preset)`, await exit code 0, read /tmp/out.mp3 with music-metadata.parseFile, assert common.title, common.artist, common.album, common.genre[0], common.bpm, common.key, common.comment[0] all equal the source values
    - Test (FLAC → MP3 320, HIGH confidence): same flow with sample.flac. Verify Vorbis TITLE→TIT2, ARTIST→TPE1, GENRE→TCON, BPM→TBPM, INITIALKEY→TKEY mapping. (If sample.flac doesn't have tags, fail the test with a clear message saying "fixture needs Vorbis tags — regenerate via ffmpeg -metadata flags")
    - Test (M4A → MP3 320, MEDIUM confidence per Assumption A2): same flow with tagged.m4a. Assert TIT2, TPE1, TALB, TCON, TBPM map cleanly. TKEY assertion is SOFT — if absent in output, log a warning via console.warn but do not fail the test (RESEARCH A2 mitigation — TKEY freeform atom is the known fragile mapping)
    - Test: output file is non-empty (>1 KB) — sanity check the encode actually produced audio data
    - All temp output files are cleaned up in `afterEach` to keep the workspace tidy
  </behavior>
  <action>
    Create src/main/conversion/tagRoundtrip.test.ts (vitest, node project, longer timeout). Use `import ffmpegStatic from 'ffmpeg-static'` to get the binary path (dev mode — no asar rewrite needed in tests). Use `import { spawnSync } from 'node:child_process'` for synchronous ffmpeg invocations (avoids async timing complexity in tests). Use `import { parseFile } from 'music-metadata'` for read-back.

    Test scaffolding:
    - `beforeEach`: create a `tmpdir` via `fs.mkdtempSync(path.join(os.tmpdir(), 'dj-utils-tag-rt-'))` so each test gets a clean dir
    - `afterEach`: `fs.rmSync(tmpdir, { recursive: true, force: true })`
    - Helper `runFfmpeg(src, out, preset)`: calls `spawnSync(ffmpegStatic, buildFfmpegArgs(src, out, preset), { encoding: 'utf8' })`, asserts `result.status === 0`, fails with stderr if not
    - Helper `readTags(file)`: `await parseFile(file)`; returns `common`

    Test bodies: for each source fixture, run the conversion, read tags, assert each Rekordbox-relevant field equals the expected value (hardcode expected from the fixture — use the same values used when fixtures were generated in Phase 2 Task 1: Genre='House', BPM=128, Key='8A').

    Set per-test timeout to 30 seconds (`{ timeout: 30000 }` on each it block) — ffmpeg-static's first run on macOS can have a Gatekeeper delay.

    DO NOT run this test in CI environments where ffmpeg-static isn't installed (it's already a dependency in package.json so `npm test` works locally). If `ffmpegStatic` is null (Pitfall 1 in dev — ffmpeg-static returns the path or null on platforms it doesn't support), skip the suite with `describe.skip` and a clear message.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/conversion/tagRoundtrip</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/conversion/tagRoundtrip` passes all three round-trip scenarios
    - MP3 → MP3 320 preserves Genre + BPM + Key (the three load-bearing Rekordbox fields)
    - FLAC → MP3 320 maps Vorbis comments to ID3v2.3 frames (CONV-06 cross-format proof)
    - M4A → MP3 320: TIT2/TPE1/TALB/TCON/TBPM mapped; TKEY soft-asserted (warn-on-miss per A2)
    - Test uses the REAL ffmpeg-static binary (not a mock) — this is the only place we prove the bundled binary works end-to-end at the function level
  </acceptance_criteria>
  <done>CONV-06 proven empirically: the bundled ffmpeg with our flag set round-trips Rekordbox tags across the three high-value source formats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: ConversionController + heartbeat timer + single-active invariant</name>
  <files>src/main/conversion/controller.ts, src/main/conversion/controller.test.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (System Architecture Diagram; Pattern 3 heartbeat; Pitfalls 5 SIGTERM-first cancel, 6 heartbeat safety; LOCKED single-active concurrency)
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED: BatchAlreadyActive rejection; heartbeat every 5s; threshold 30s)
    - src/main/scan/controller.ts (Phase 2 controller shape — EXACTLY mirror lifecycle/clearActive/persist-then-forward primitives)
    - src/main/conversion/conversionRepo.ts (from Task 4)
    - src/main/conversion/ffmpegPath.ts (from Task 3)
    - src/shared/ipc-types.ts (ConversionEvent, Preset from Task 2)
  </read_first>
  <behavior>
    - Test (start, mocked spawnWorker + repo + send): controller.start({rootFolder:'/Music', filePaths:['/Music/a.mp3'], preset: mp3-320}) returns a UUID conversionId; calls repo.createConversion with outputDir='/Music/converted/mp3-320'; calls repo.insertFileBatch with one row {file_path:'/Music/a.mp3', status:'pending'}; spawns the worker with workerData containing {conversionId, files, ffmpegPath, preset, outputDir, parallelism: min(os.cpus().length - 1, 4)}
    - Test (BatchAlreadyActive — LOCKED single-active): controller.start while a prior batch is active throws (or rejects with) `BatchAlreadyActive`; the prior batch is NOT cancelled (different from Phase 2 scan behavior — conversion is more expensive to lose, so we reject not pre-empt)
    - Test (progress event forwarding): when worker emits `{type:'progress', conversionId, filePath, percent:42, phase:'transcoding'}`, controller calls send(IpcChannels.ConversionEvent, msg) but does NOT touch the repo (progress is volatile per LOCKED decision)
    - Test (fileDone success): worker emits `{type:'fileDone', filePath, status:'done', outputPath}` → controller calls repo.updateFile(conversionId, filePath, {status:'done', outputPath, errorMessage:null}) AND send(msg) — in that order (persist-then-forward; assert via vi.fn().mock.invocationCallOrder)
    - Test (fileDone error): same as above but status='error', controller persists errorMessage and forwards; batch does NOT terminate (CONV-04 — errors don't abort)
    - Test (fileDone skipped): same as above but status='skipped'; controller persists with the 'output_exists' errorMessage; batch continues
    - Test (batch done): worker emits `{type:'done', conversionId}` → controller calls repo.complete(conversionId, {status:'done', endedAt:N}) AND clears heartbeat timer AND forwards; activeConversion cleared
    - Test (cancel, LOCKED Pitfall 5 ordering): controller.cancel(conversionId) posts `{type:'cancel'}` to the worker FIRST, awaits the worker's `'cancelled'` event from postMessage, THEN calls worker.terminate() as a safety net, THEN repo.complete(status:'cancelled'). Assert call order strictly.
    - Test (cancel no-op): controller.cancel(unknownId) is a no-op (no repo writes, no worker postMessage)
    - Test (heartbeat — uses vi.useFakeTimers): after start(), advance fake clock by 5s → repo.bumpHeartbeat called once; by 10s → twice; cancel/done clears the timer (no bumps after terminal)
    - Test (resolveFfmpegPath integration — Pitfall 1): the controller is constructed with `{resolveFfmpegPath: vi.fn(opts => opts.rawPath + '.RESOLVED'), getFfmpegRawPath: () => '/raw', isPackaged: true}` — assert the worker's workerData.ffmpegPath equals '/raw.RESOLVED' (the helper from Task 3 is wired correctly)
    - Test (resume hook present but stub): controller.resume(conversionId) signature exists and accepts a conversionId; in this plan it throws `Error('Plan 03-03 will implement resume')` so the IPC handler in Task 9 can wire to it without crashing the build. Plan 03-03 replaces this stub.
    - Test (worker exit without terminal): worker emits 'exit' without prior 'done'/'cancelled'/'error' → controller posts a synthesized `{type:'error', conversionId, message:'worker exited unexpectedly'}` to repo.complete(status:'crashed') + send (defensive — covers Pitfall 5 worst case)
  </behavior>
  <action>
    Create src/main/conversion/controller.ts mirroring Phase 2 createScanController structure but with these additions:
    - `export interface ConversionControllerDeps { spawnWorker: (data: ConversionWorkerSpawnData) => WorkerLike; repo: ConversionRepo; send: (channel: string, payload: ConversionEvent) => void; resolveFfmpegPath: (opts: {rawPath:string; isPackaged:boolean}) => string; getFfmpegRawPath: () => string; isPackaged: boolean; cpuCount?: () => number; now?: () => number; heartbeatMs?: number }`
    - `BatchAlreadyActive` exported as a typed Error subclass (`export class BatchAlreadyActive extends Error`) so the IPC handler in Task 9 can `instanceof`-check it and shape a clean rejection
    - `start({rootFolder, filePaths, preset}): Promise<string>`: if activeConversion → throw new BatchAlreadyActive(). Else: compute parallelism = `Math.max(1, Math.min((deps.cpuCount?.() ?? require('node:os').cpus().length) - 1, 4))`. Compute outputDir = `path.join(rootFolder, 'converted', preset.slug)`. (Bug guard: ensure the dir exists before spawn — `await fs.mkdir(outputDir, { recursive: true })`.) conversionId = randomUUID(). repo.createConversion(...); repo.insertFileBatch(rows={file_path, status:'pending'} for each filePath). Resolve ffmpegPath via `deps.resolveFfmpegPath({rawPath: deps.getFfmpegRawPath(), isPackaged: deps.isPackaged})`. Spawn worker with the full workerData. Start the heartbeat timer (`setInterval` at heartbeatMs ?? 5000 calling `deps.repo.bumpHeartbeat(conversionId, deps.now())`).
    - `cancel(conversionId): Promise<void>`: if active.conversionId !== conversionId return. SIGTERM-first ordering per Pitfall 5: postMessage({type:'cancel'}) → await worker settle (use a Promise resolved by the 'cancelled' or 'exit' event, with a timeout fallback of 5s) → worker.terminate() → repo.complete(status:'cancelled', endedAt) → send 'cancelled' → clear timer + clear active.
    - `resume(conversionId): Promise<void>`: throw new Error('not implemented in Plan 03-01; see Plan 03-03'). Signature stable.
    - `attachWorker(...)`: switches on msg.type for progress/fileStart/fileDone/done/cancelled/error per RESEARCH event union. progress → forward only. fileStart → repo.updateFile(...status:'running'). fileDone → repo.updateFile then send (assert order via invocationCallOrder in test). done → repo.complete(done) + clear timer + clear active. error/cancelled similar.
    - Heartbeat timer is stored in `activeConversion.heartbeatTimer` and cleared in `clearActive()`.

    Create src/main/conversion/controller.test.ts. Mock spawnWorker with EventEmitter-shaped worker (postMessage/terminate vi.fn()). Mock repo, send, resolveFfmpegPath, getFfmpegRawPath. Use vi.useFakeTimers() for heartbeat tests. Drive the controller, emit synthetic worker messages, assert call ordering via vi.fn().mock.invocationCallOrder.

    DO NOT instantiate a real Worker in tests. DO NOT import 'electron' (deps inject everything).
  </action>
  <verify>
    <automated>npm run test -- --run src/main/conversion/controller</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/conversion/controller` passes all behaviors above
    - controller.ts has zero `import 'electron'` (handlers in Task 9 own the webContents wiring)
    - start() while active throws BatchAlreadyActive (LOCKED single-active per CONTEXT D-CONV-CONCURRENCY); prior batch is NOT cancelled (assert worker.postMessage not called with 'cancel')
    - The `updateFile` call happens BEFORE the `send` call inside the 'fileDone' branch (test asserts via invocationCallOrder)
    - Cancel sequence asserts strict order: postMessage('cancel') → await settle → terminate() → repo.complete (no terminate-before-postMessage path)
    - Heartbeat timer fires every 5s while active; clears on done/cancelled/error
    - resume() throws "not implemented" — Plan 03-03 replaces this
  </acceptance_criteria>
  <done>End-to-end conversion backbone wired and unit-tested with a fake worker + fake ffmpeg: BatchAlreadyActive, persist-then-forward, SIGTERM-first cancel, heartbeat — all locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: conversion IPC handlers + main wiring (folder allowlist on filePaths, webContents binding)</name>
  <files>src/main/ipc/conversion.ts, src/main/ipc/conversion.test.ts, src/main/index.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-RESEARCH.md (Pitfall 8 — file path allowlist; Security Domain V4/V5; Code Examples §1 contract for handlers)
    - src/main/conversion/controller.ts (from Task 8 — BatchAlreadyActive class)
    - src/main/db/connection.ts (getSettingsRepo + getConversionRepo from Task 4)
    - src/main/ipc/scan.ts (Phase 2 handler pattern + handler unit-test pattern)
    - src/main/index.ts (after-app-ready handler registration site, BrowserWindow + webContents)
  </read_first>
  <behavior>
    - Test (start handler, V5 validation): conversionStartHandler({controller, settingsRepo}, params) when typeof params !== 'object' OR params.rootFolder !== settingsRepo.get('rootFolder') OR !Array.isArray(params.filePaths) OR !params.preset.slug → throws; controller.start NOT called
    - Test (allowlist gate — load-bearing): conversionStartHandler with params.filePaths containing one path that does NOT resolve under settings.rootFolder (e.g., '/etc/passwd' or '/Music/../etc/passwd') → throws an error mentioning rootFolder. controller.start NOT called. This is Pitfall 8 mitigation; without this a compromised renderer reads/writes any disk location.
    - Test (happy path): valid params where every filePath resolves under rootFolder → controller.start called with the params; returned conversionId surfaced to caller
    - Test (BatchAlreadyActive surfaces as rejection): when controller.start throws BatchAlreadyActive, the handler rejects with a clear message ("Une conversion est déjà en cours") so the renderer can show a toast
    - Test (cancel handler): conversionCancelHandler with non-string id throws; with string id calls controller.cancel(id)
    - Test (list-resumable handler — stub in this plan): returns []; Plan 03-03 replaces this stub with `repo.findResumable()` (the controller has access to repo; this handler is a thin pass-through). The TEST asserts the stub returns [] so Plan 03-02 useConversionStore.checkResumable() doesn't crash.
    - Test (resume handler — stub): throws "not implemented" — Plan 03-03 replaces.
    - Test (registerConversionHandlers): registers exactly four ipcMain.handle channels matching IpcChannels.ConversionStart/Cancel/ListResumable/Resume
  </behavior>
  <action>
    Create src/main/ipc/conversion.ts following the Phase 2 handler pattern (pure functions + ipcMain.handle registration):

    - `export function conversionStartHandler(deps: { controller; settingsRepo }, params: unknown): Promise<string>`:
      - V5 input validation: assert `params` is an object with `rootFolder: string`, `filePaths: string[]`, `preset: Preset` (typeof + Array.isArray guards; reject otherwise)
      - Allowlist: assert `params.rootFolder === deps.settingsRepo.get('rootFolder')`
      - Per-file path allowlist (Pitfall 8): for each filePath, assert `path.resolve(filePath).startsWith(path.resolve(params.rootFolder) + path.sep)`. Reject if ANY file fails. Test the path-traversal case (`/Music/../etc/passwd`) — `path.resolve` collapses `..` so the startsWith check catches it.
      - Validate preset.slug — must be in PRESETS slugs OR equal `'custom'` (escape hatch for the Custom form in Plan 03-02). If custom, require codec ∈ {'libmp3lame', 'aac', 'flac', 'pcm_s16le', 'libopus'} and bitrateKbps either null OR a positive integer ≤ 1024.
      - Returns `controller.start(params)`. Catch `BatchAlreadyActive` and rethrow as a clean Error with `"Une conversion est déjà en cours"` message.

    - `export function conversionCancelHandler(deps: { controller }, id: unknown): Promise<void>`:
      - Validate `typeof id === 'string'`; call `controller.cancel(id)`.

    - `export function conversionListResumableHandler(_deps): Promise<ResumableBatch[]>`:
      - Stub: `return []`. Plan 03-03 replaces with `deps.repo.findResumable()`.

    - `export function conversionResumeHandler(_deps, _id): Promise<void>`:
      - Stub: `throw new Error('conversion:resume not implemented in Plan 03-01; see Plan 03-03')`.

    - `export function registerConversionHandlers(opts: { ipcMain; controller; settingsRepo; getSender: () => WebContents | null }): void`:
      - Wires `ipcMain.handle(IpcChannels.ConversionStart, (_e, params) => conversionStartHandler({controller, settingsRepo}, params))` etc. for all four channels.
      - Construct the controller's `send` dep as `(channel, payload) => opts.getSender()?.send(channel, payload)`.

    Update src/main/index.ts:
    - Import `createConversionController`, `registerConversionHandlers`; import `getConversionRepo` from db/connection; import `resolveFfmpegPath` from `./conversion/ffmpegPath`.
    - Inside `app.whenReady().then(...)` AFTER createWindow() and AFTER registerScanHandlers(): construct the conversion controller with `spawnWorker = (data) => new Worker(path.join(__dirname, 'workers/conversionWorker.js'), { workerData: data })`, repo from getConversionRepo(), send wired via the same `mainWindow?.webContents` pattern as scan, `resolveFfmpegPath`, `getFfmpegRawPath: () => require('ffmpeg-static') as string`, `isPackaged: app.isPackaged`.
    - Call `registerConversionHandlers({ ipcMain, controller, settingsRepo, getSender: () => mainWindow?.webContents ?? null })` AFTER `registerScanHandlers(...)`.

    Create src/main/ipc/conversion.test.ts in the node project. Fake ipcMain/controller/settingsRepo as vi.fn() shapes. Exercise the behaviors above. The allowlist + path-traversal tests are the load-bearing ones (T-3-01 mitigation).
  </action>
  <verify>
    <automated>npm run test -- --run src/main/ipc/conversion</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/ipc/conversion` passes all behaviors
    - Path-traversal test: filePath of `/Music/../etc/passwd` is rejected even when params.rootFolder='/Music' (path.resolve collapses, startsWith catches)
    - conversion:list-resumable is wired but returns [] (Plan 03-03 fills)
    - conversion:resume is wired but throws "not implemented" (Plan 03-03 fills)
    - src/main/index.ts constructs the worker via `new Worker(path.join(__dirname, 'workers/conversionWorker.js'), { workerData })` (grep "workers/conversionWorker.js")
    - registerConversionHandlers is called AFTER registerScanHandlers (preserves wave ordering)
    - `npm run build` exits 0; Phase 1/2 IPC handlers unchanged (no regression)
  </acceptance_criteria>
  <done>Bridge live; folder + per-file allowlist guard tested (T-3-01 mitigated); controller wired to the real Worker constructor with the bundled path; resume/list-resumable stubs in place for Plan 03-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 3 main-process backbone end-to-end through the typed bridge: extended IpcChannels + DjUtilsApi.conversion namespace, conversions + conversion_files tables behind the createXRepo+initXSchema factory pattern, PRESETS registry (5 entries, Rekordbox-locked `-id3v2_version 3` for MP3 targets), pure conversionCore (chunk-aware stderr parsing), the conversionWorker.ts with an intra-batch child_process ffmpeg pool + SIGTERM-first cancel + partial-output unlink, a ConversionController with single-active (BatchAlreadyActive) + heartbeat timer + persist-then-forward, conversion:start with a per-file folder allowlist gate (Pitfall 8 mitigation), and the worker entry bundled to out/main/workers/conversionWorker.js (Pitfall 1 carry-forward). Tag round-trip integration test proves CONV-06 across MP3→MP3, FLAC→MP3, M4A→MP3 using the real bundled ffmpeg. No UI yet — verification is purely via the DevTools console + existing Phase 1/2 nav shell.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. The Phase 1+2 three-tool nav still renders. Confirm no Phase 2 regression: `await window.djUtils.scan.start(rootFolder)` still emits row events.
    2. In DevTools console, run a 3-file conversion against the rootFolder:
       ```
       const off = window.djUtils.conversion.onEvent(e => console.log('CONV', e));
       const id = await window.djUtils.conversion.start({
         rootFolder: '/path/to/music',
         filePaths: ['/path/to/music/a.mp3', '/path/to/music/b.flac', '/path/to/music/c.wav'],
         preset: { slug:'mp3-320', label:'MP3 320', codec:'libmp3lame', bitrateKbps:320, vbrQuality:null, sampleRate:null, extension:'.mp3' }
       });
       console.log('conversionId', id);
       ```
       Expect: a stream of `{type:'progress', percent: …}` events, then a `{type:'fileDone', status:'done'}` per file, then `{type:'done'}`. The `/path/to/music/converted/mp3-320/` directory now contains three .mp3 files.
    3. Inspect tag preservation: open one output .mp3 in Rekordbox (or any ID3 reader). Genre/BPM/Key should match the source. (CONV-06 manual confirmation.)
    4. Confirm folder allowlist gate: `await window.djUtils.conversion.start({ rootFolder:'/path/to/music', filePaths:['/etc/hosts'], preset: … })` → expect rejection. `await window.djUtils.conversion.start({ rootFolder:'/path/to/music', filePaths:['/path/to/music/../etc/hosts'], preset: … })` → also rejection (Pitfall 8 — path.resolve collapse + startsWith).
    5. Confirm single-active LOCKED: start one batch, then immediately call start again → second call rejects with "Une conversion est déjà en cours".
    6. Confirm cancel cleanup: start a batch of 5+ large files, after the first finishes call `await window.djUtils.conversion.cancel(id)`. Inspect the output dir — partials should be unlinked (no zombie .mp3 files mid-encode). `ps aux | grep ffmpeg` should show no orphan processes.
    7. Confirm worker bundling: `ls out/main/workers/conversionWorker.js` after `npm run build` — file exists and >3 KB.
    8. Confirm CSV stubs in place: `await window.djUtils.conversion.listResumable()` returns `[]`; `await window.djUtils.conversion.resume('any-id')` rejects with "not implemented" (Plan 03-03 will fill these).
    9. Call `off()` then quit. No worker / ffmpeg process should remain in `ps`.
  </how-to-verify>
  <resume-signal>Type "approved" if (a) Phase 1+2 still work, (b) a 3-file conversion produces progress events + fileDone events + done, (c) tag preservation confirmed in Rekordbox, (d) allowlist gate rejects both off-root and path-traversal, (e) single-active rejects second start, (f) cancel unlinks partials and orphans no ffmpeg, (g) bundled worker file exists, (h) listResumable returns []. Otherwise describe which step failed (most likely: ffmpeg-static path missing in dev → check `require('ffmpeg-static')` returns a non-null string; or ABI mismatch → run `npx electron-rebuild -f -w better-sqlite3`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload → main | Phase 1+2 boundary preserved; conversion namespace crosses the same contextBridge with sandbox:true + contextIsolation:true. |
| main → filesystem (output dir under rootFolder) | NEW WRITE SURFACE: main writes to `{rootFolder}/converted/{preset-slug}/`. Output dir composed server-side; never renderer-supplied. |
| main → spawned Worker | NEW: worker_threads.Worker via bundled path. workerData is {conversionId, files, ffmpegPath, preset, outputDir, parallelism} only. |
| Worker → spawned ffmpeg child_process | NEW SURFACE: worker spawns up to `min(cpus-1, 4)` ffmpeg processes via `spawn(ffmpegPath, args, { stdio: ['ignore','ignore','pipe'] })`. No shell interpretation (argv as array). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | BrowserWindow | mitigate (Phase 1 carry) | contextIsolation:true, nodeIntegration:false, sandbox:true preserved |
| T-2-01 | Information Disclosure / Tampering | scan:start folder allowlist | mitigate (Phase 2 carry) | Unchanged |
| T-3-01 | Information Disclosure / Tampering | conversion:start accepts renderer-supplied filePaths | mitigate | Task 9 conversionStartHandler asserts `path.resolve(filePath).startsWith(path.resolve(rootFolder) + path.sep)` for EVERY filePath. Without this, a compromised renderer reads any disk location and writes ffmpeg output anywhere (Pitfall 8). Test enforces rejection of both off-root paths and `..` traversal. |
| T-3-02 | Tampering | Renderer-supplied ffmpeg binary path | mitigate | ffmpegPath is **never** accepted from renderer. Resolved server-side in main via `require('ffmpeg-static') + resolveFfmpegPath()` (asar.unpacked rewrite); passed to worker via workerData. |
| T-3-03 | Tampering | Command injection via filenames containing shell metacharacters | mitigate | `child_process.spawn(cmd, [args])` passes argv as array — no shell interpretation. `shell: false` (default). Filenames with spaces/quotes/`;` are safe. |
| T-3-04 | Tampering | Path traversal via preset.slug used in outputDir composition | mitigate | Preset slugs validated in Task 9 — must be in PRESETS slugs OR literal `'custom'`. Custom presets compose outputDir with literal slug `'custom'`, not arbitrary user input. |
| T-3-05 | DoS (resource leak) | worker.terminate leaks ffmpeg children | mitigate | LOCKED cancel ordering (Pitfall 5): postMessage('cancel') → worker SIGTERMs children → worker awaits → controller terminates worker as safety net. Test asserts strict call order. |
| T-3-06 | DoS (disk fill) | Partial outputs accumulate on cancel/error | mitigate | Pitfall 3: `await fs.unlink(out).catch(() => undefined)` on every cancelled or non-zero-exit branch in worker convertOne. |
| T-3-07 | DoS | Malformed audio crashes ffmpeg → worker crash | mitigate | Per-file try/catch in slot-runner emits `fileDone status:'error'` and continues. Top-level try/catch posts `{type:'error'}` so controller cleanup always fires. |
| T-3-08 | Tampering / V5 | Renderer-supplied preset shape malformed (codec='/bin/sh') | mitigate | Task 9 validates preset.slug ∈ PRESETS slugs ∪ {'custom'}; for custom, codec ∈ allowed-list ({'libmp3lame','aac','flac','pcm_s16le','libopus'}); bitrateKbps is positive int ≤ 1024. |
| T-3-09 | DoS (false-positive resume) | Heartbeat race confuses live batch with crashed | mitigate (Pitfall 9) | Heartbeat fires every 5s; threshold is 30s; `findResumable` runs ONCE on boot before any window opens (Plan 03-03 wires this). In-session, activeConversionId in main is the source of truth. |
| T-3-SC | Tampering | npm installs (no new deps in this phase) | mitigate | RESEARCH Package Legitimacy Audit confirms ffmpeg-static, music-metadata, better-sqlite3 already pinned + used in Phases 1-2; no new packages. |
</threat_model>

<verification>
- `npm run build` green; `out/main/workers/conversionWorker.js`, `out/main/workers/scanWorker.js`, `out/main/index.js` all exist.
- `npm run test -- --run src/main/conversion src/main/workers/conversionCore src/main/ipc/conversion` green (CONV-02 mapping; CONV-03 stderr parsing; CONV-04 error semantics at repo level; CONV-06 tag round-trip on three formats).
- `npm run test -- --run src/main/conversion/controller` green (LOCKED single-active BatchAlreadyActive; persist-then-forward order; SIGTERM-first cancel order; heartbeat timer).
- `npm run test -- --run src/main/conversion/tagRoundtrip` green (real ffmpeg + real fixtures — load-bearing CONV-06 evidence).
- grep: no `import.*worker_threads` in conversionCore.ts; no `import.*child_process` in conversionCore.ts; no `import.*electron` in conversionWorker.ts; no template-literal SQL in conversionRepo.ts; preload imports only 'electron' and shared types.
- Manual checkpoint: real folder produces progress + fileDone + done; allowlist rejects off-root + path-traversal; single-active rejects second start; cancel unlinks partials + no orphan ffmpeg.
</verification>

<success_criteria>
- CONV-01 (selection → batch start) observable through `window.djUtils.conversion.start({rootFolder, filePaths, preset})` returning a conversionId; Plan 03-02 adds the UI on top.
- CONV-02 (target format configurable, MP3 320 default) observable through `PRESETS` exports + `buildFfmpegArgs` producing valid argv per preset; custom preset escape hatch validated.
- CONV-03 (per-file + global progress) mechanism in place: worker emits batched `{type:'progress', percent}` events at 20/200ms; controller forwards; renderer can derive global = done_count / total_count.
- CONV-04 (per-file errors don't abort) proven at the controller level: one fileDone status:'error' doesn't trigger repo.complete; batch transitions to 'done' only after all files terminal.
- CONV-06 (tag preservation) proven empirically by tagRoundtrip.test.ts on three source formats with the real ffmpeg binary.
- LOCKED single-active (BatchAlreadyActive) at the controller level (D-CONV-CONCURRENCY).
- LOCKED output dir `{rootFolder}/converted/{preset-slug}/` composed server-side; skip-on-conflict policy implemented in worker.
- LOCKED tag flags `-map_metadata 0 -id3v2_version 3` (MP3 only) hardcoded in buildFfmpegArgs.
- Worker bundling (Pitfall 1) verified by file existence + manual conversion working in dev.
- Per-file folder allowlist gate (Pitfall 8) verified by reject-path test + manual path-traversal attempt.
- SIGTERM-first cancel ordering (Pitfall 5) verified by invocationCallOrder assertion + manual no-orphan-ffmpeg confirmation.
</success_criteria>

<output>
Create `.planning/phases/03-conversion/03-01-SUMMARY.md` when done.
</output>
