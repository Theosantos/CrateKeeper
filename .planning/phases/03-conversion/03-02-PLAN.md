---
phase: 03-conversion
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - src/renderer/src/store/useScanStore.ts
  - src/renderer/src/store/useScanStore.test.ts
  - src/renderer/src/store/useConversionStore.ts
  - src/renderer/src/store/useConversionStore.test.ts
  - src/renderer/src/components/analyser/VirtualizedFileTable.tsx
  - src/renderer/src/components/analyser/ScanToolbar.tsx
  - src/renderer/src/components/convertir/ConvertirView.tsx
  - src/renderer/src/components/convertir/PresetSelector.tsx
  - src/renderer/src/components/convertir/CustomPresetForm.tsx
  - src/renderer/src/components/convertir/ConversionProgress.tsx
  - src/renderer/src/components/convertir/convertir.css
  - src/renderer/src/App.tsx
  - src/renderer/src/assets/main.css
autonomous: false
requirements: [CONV-01, CONV-02, CONV-03, CONV-04]
user_setup: []

must_haves:
  truths:
    - "VirtualizedFileTable shows a leftmost checkbox column (~32px); header checkbox selects/deselects every visible row of the current scan"
    - "useScanStore.selectedFilePaths is a Set<string> persisted across view switches; resets to empty when a new scan starts"
    - "ScanToolbar shows a primary action `Convertir N fichiers` button when selectedFilePaths.size > 0; clicking it switches the renderer view to Convertir and pre-loads the selection into useConversionStore"
    - "useConversionStore exposes status ('idle'|'running'|'done'|'cancelled'|'error'), perFileProgress (Map<filePath, {percent, status}>), errors (array of {filePath, errorMessage}), and selectedPreset"
    - "useConversionStore.onEvent wires window.djUtils.conversion.onEvent on mount and unsubscribes on unmount; mutations are pure (Zustand setter functions, no direct mutation)"
    - "ConvertirView shows the preset selector (5 radio options + Custom), Lancer button, and a live progress list (one row per file) + a global progress strip"
    - "Convertir → Lancer calls window.djUtils.conversion.start with the validated payload; selectedPreset persists to settings.conversion.lastPreset via existing settings repo"
    - "Custom preset form is hidden by default; opens inline when user picks `Custom...`; inputs validate locally (codec ∈ allowed list, bitrate positive int ≤ 1024) before Lancer is enabled"
    - "Per-file progress updates within 200ms of a `progress` event; per-file completion (fileDone) updates immediately"
    - "Error summary line at the bottom: `N converti, M en erreur, K ignorés` updates from useConversionStore terminal counts"
    - "Editorial dark-studio tokens from src/renderer/src/assets/main.css are reused; convertir.css adds NO new color tokens"
    - "All renderer copy is in French (LOCKED Editorial dark-studio decision)"
  artifacts:
    - path: "src/renderer/src/store/useScanStore.ts"
      provides: "Extended Zustand store: selectedFilePaths (Set<string>), toggleFile(path), toggleAll(paths[]), clearSelection(); selection resets when a new scanId arrives"
      contains: "selectedFilePaths"
    - path: "src/renderer/src/store/useConversionStore.ts"
      provides: "Zustand store: status, conversionId, perFileProgress, fileStatuses, errors, selectedPreset, customPreset; actions startBatch(filePaths, rootFolder), cancelBatch(), setPreset(p), updateCustom(p); selectors progressGlobal, summaryCounts; lifecycle subscribeEvents() returns unsubscribe"
      contains: "create<ConversionState>"
    - path: "src/renderer/src/components/analyser/VirtualizedFileTable.tsx"
      provides: "Existing table extended with a leftmost CheckboxCell column (~32px); header CheckboxAll selects/deselects all visible rows for the current scanId; checkbox state bound to useScanStore.selectedFilePaths"
      contains: "selectedFilePaths"
    - path: "src/renderer/src/components/analyser/ScanToolbar.tsx"
      provides: "Existing toolbar extended with a primary action button `Convertir N fichiers` shown only when selectedFilePaths.size > 0; onClick switches activeView in App.tsx"
      contains: "Convertir"
    - path: "src/renderer/src/components/convertir/ConvertirView.tsx"
      provides: "Top-level Convertir view: PresetSelector + (CustomPresetForm if custom) + Lancer + Cancel button + ConversionProgress; renders when activeView === 'convertir'"
      contains: "ConvertirView"
    - path: "src/renderer/src/components/convertir/PresetSelector.tsx"
      provides: "5 radio buttons (one per PRESETS entry) + a sixth `Custom…` radio that toggles CustomPresetForm; selection updates useConversionStore.selectedPreset"
      contains: "PRESETS"
    - path: "src/renderer/src/components/convertir/CustomPresetForm.tsx"
      provides: "Inline form: codec select (5 options), bitrate input (kbps, ignored for FLAC/WAV), sample rate input (optional, default preserve); local validation; updates useConversionStore.customPreset"
      contains: "codec"
    - path: "src/renderer/src/components/convertir/ConversionProgress.tsx"
      provides: "Per-file rows (filename + percent bar + status badge) + global progress strip + summary counts; reads from useConversionStore"
      contains: "perFileProgress"
    - path: "src/renderer/src/App.tsx"
      provides: "activeView state extended to include 'convertir'; renders ConvertirView when active; nav switcher route added"
      contains: "convertir"
  key_links:
    - from: "src/renderer/src/components/analyser/VirtualizedFileTable.tsx"
      to: "src/renderer/src/store/useScanStore.ts"
      via: "useScanStore(s => s.selectedFilePaths) + useScanStore(s => s.toggleFile)"
      pattern: "selectedFilePaths"
    - from: "src/renderer/src/components/analyser/ScanToolbar.tsx"
      to: "src/renderer/src/App.tsx"
      via: "onClick handler sets App.activeView = 'convertir' AND seeds useConversionStore.pendingFilePaths from useScanStore.selectedFilePaths"
      pattern: "activeView"
    - from: "src/renderer/src/components/convertir/ConvertirView.tsx"
      to: "src/renderer/src/store/useConversionStore.ts"
      via: "useConversionStore(s => s.startBatch) + useConversionStore(s => s.cancelBatch)"
      pattern: "useConversionStore"
    - from: "src/renderer/src/store/useConversionStore.ts"
      to: "window.djUtils.conversion (from preload)"
      via: "djUtils.conversion.start / cancel / onEvent"
      pattern: "djUtils\\.conversion"
    - from: "src/renderer/src/store/useConversionStore.ts"
      to: "window.djUtils.setSetting / getSetting (existing Phase 1 surface)"
      via: "persist selectedPreset to 'conversion.lastPreset' on Lancer"
      pattern: "conversion.lastPreset"
---

<objective>
Ship the Convertir vertical-slice UI on top of the Plan 03-01 backbone. Add a checkbox column + select-all to the existing VirtualizedFileTable so the user can pick a subset of scanned files; surface a `Convertir N fichiers` primary action in ScanToolbar that switches to the Convertir view with the selection pre-loaded; build the Convertir view itself with a 5-preset radio selector + Custom escape hatch + Lancer button + live per-file progress list + global progress strip + summary counts; wire a useConversionStore that mirrors useScanStore's shape and consumes the `conversion.*` IPC bridge.

Purpose: Plan 03-01 proved CONV-01/02/03/04/06 at the bridge level. This plan makes them user-observable through actual UI. Plan 03-03 builds the resume-after-crash banner on top of this view.

Output: A fully wired Convertir flow — pick files in Analyser → click `Convertir N fichiers` → pick preset → click Lancer → watch live progress → see per-file errors and a summary. No backend changes; this plan is renderer-only except for one micro-extension to useScanStore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-conversion/03-CONTEXT.md
@.planning/phases/03-conversion/03-RESEARCH.md
@.planning/phases/03-conversion/03-01-PLAN.md
@.planning/phases/02-scanning/02-02-SUMMARY.md
@src/shared/ipc-types.ts
@src/renderer/src/App.tsx
@src/renderer/src/store/useScanStore.ts
@src/renderer/src/components/analyser/VirtualizedFileTable.tsx
@src/renderer/src/components/analyser/ScanToolbar.tsx
@src/renderer/src/assets/main.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useScanStore with selectedFilePaths + toggle/toggleAll/clear</name>
  <files>src/renderer/src/store/useScanStore.ts, src/renderer/src/store/useScanStore.test.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED: selection lives in useScanStore, persists across view switches, resets on new scan)
    - src/renderer/src/store/useScanStore.ts (existing Phase 2 shape — Zustand store with files, scanId, status, onEvent)
    - src/renderer/src/store/useScanStore.test.ts (existing test patterns)
  </read_first>
  <behavior>
    - Test: initial state has `selectedFilePaths: Set<string>` empty
    - Test: toggleFile('/Music/a.mp3') adds path; calling again removes it; immutable update (new Set instance per call — Zustand setter discipline; assert previous Set reference !== new Set reference)
    - Test: toggleAll(['a.mp3','b.mp3','c.mp3']) when none selected → all selected; when all selected → none; when partial → all selected (mirror native "select-all" semantics)
    - Test: clearSelection() empties the set
    - Test: when a new scanId arrives (existing handleScanEvent path for 'rows' with a fresh scanId, or an explicit `resetForScan(newScanId)` action called by the existing scan-start flow), selectedFilePaths is reset to empty Set — LOCKED "selection resets on new scan" semantics
    - Test: selectedFilePaths persists across unrelated state updates (e.g., adding a row to files) — verify by toggling a file then dispatching a rows event and re-checking the set
  </behavior>
  <action>
    Extend src/renderer/src/store/useScanStore.ts ADDITIVELY:
    - Add to state shape: `selectedFilePaths: Set<string>` (initial value: `new Set()`)
    - Add action `toggleFile(filePath: string)` → `set(s => { const next = new Set(s.selectedFilePaths); next.has(filePath) ? next.delete(filePath) : next.add(filePath); return { selectedFilePaths: next } })`
    - Add action `toggleAll(filePaths: string[])` → if `filePaths.every(p => selectedFilePaths.has(p))` then remove all (delete each); else add all. Always return a new Set instance (immutability — TypeScript coding-style rule).
    - Add action `clearSelection()` → `set({ selectedFilePaths: new Set() })`
    - Hook into the existing scan-start path (wherever the store handles a new scanId) to call `clearSelection()` so a fresh scan starts with no selection. If the existing code is `set({ scanId: newId, files: [] })`, extend it to `set({ scanId: newId, files: [], selectedFilePaths: new Set() })`.

    DO NOT change the existing useScanStore API surface (files, scanId, status, onEvent etc. remain identical — Phase 2 02-02 contract is untouched).

    Extend useScanStore.test.ts with the 6 behaviors above. Use `act(() => useScanStore.getState().toggleFile('/x'))` pattern from existing tests.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/store/useScanStore</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/store/useScanStore` passes existing Phase 2 tests + the 6 new behaviors
    - Existing useScanStore consumers in Phase 2 components (VirtualizedFileTable rows, ScanToolbar status) still typecheck under `npm run build`
    - toggleFile/toggleAll always produce a NEW Set instance (Zustand reactivity — assert via reference inequality in test)
    - Starting a new scan (new scanId) resets selectedFilePaths to empty
  </acceptance_criteria>
  <done>Selection state lives in useScanStore per LOCKED decision; survives view switches but resets on new scan; all mutations immutable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checkbox column in VirtualizedFileTable + `Convertir N` button in ScanToolbar</name>
  <files>src/renderer/src/components/analyser/VirtualizedFileTable.tsx, src/renderer/src/components/analyser/ScanToolbar.tsx, src/renderer/src/App.tsx</files>
  <read_first>
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED: leftmost ~32px column; header checkbox selects all; "Convertir N fichiers" is primary action in toolbar)
    - src/renderer/src/components/analyser/VirtualizedFileTable.tsx (existing column layout from Phase 2 — preserve)
    - src/renderer/src/components/analyser/ScanToolbar.tsx (existing toolbar buttons — Cancel / Export CSV; place Convertir button as primary visual)
    - src/renderer/src/App.tsx (activeView state; nav switcher)
    - src/renderer/src/assets/main.css (LOCKED design tokens — reuse :root variables)
  </read_first>
  <behavior>
    Component tests via Testing Library + jsdom:
    - Test: VirtualizedFileTable renders a CheckboxCell at the leftmost position for each row; the cell is ~32px wide (assert via class or inline style or column-config index 0)
    - Test: clicking a row's checkbox calls useScanStore.toggleFile with that row's filePath
    - Test: VirtualizedFileTable renders a header CheckboxAll; clicking it calls useScanStore.toggleAll with the current visible filePaths
    - Test: When selectedFilePaths contains a row's path, the checkbox is `checked`
    - Test (header indeterminate): when SOME but not all visible rows are selected, header checkbox renders the `indeterminate` ARIA state (assert `aria-checked="mixed"` per WAI-ARIA tri-state checkbox)
    - Test: ScanToolbar shows the "Convertir N fichiers" button only when selectedFilePaths.size > 0; hidden when 0
    - Test: clicking "Convertir N fichiers" calls a prop callback `onConvertSelection` with the array of selected paths; App.tsx wires this to set activeView='convertir' AND seed useConversionStore.pendingFilePaths
    - Test (App): when activeView='convertir', ConvertirView is rendered; when 'analyser', the Analyser stack is rendered (regression guard for Phase 2 nav)
  </behavior>
  <action>
    Update src/renderer/src/components/analyser/VirtualizedFileTable.tsx:
    - Add a new leftmost column `select` with width 32px. Each row renders an `<input type="checkbox" role="checkbox" aria-label="Sélectionner {basename}" />` whose `checked` state reads from `useScanStore(s => s.selectedFilePaths.has(row.path))` and `onChange` calls `useScanStore.getState().toggleFile(row.path)`.
    - In the table header, render a CheckboxAll: `checked` is true when every visible row's path is in selectedFilePaths; render `indeterminate` (set via ref + DOM property; React doesn't support indeterminate as a prop directly) when some are selected; `onChange` calls `toggleAll(visiblePaths)`.
    - Preserve all other Phase 2 columns and the @tanstack/react-virtual virtualization.

    Update src/renderer/src/components/analyser/ScanToolbar.tsx:
    - Read `selectedCount = useScanStore(s => s.selectedFilePaths.size)`.
    - When `selectedCount > 0`, render a primary-styled button `Convertir {selectedCount} fichier{selectedCount > 1 ? 's' : ''}` at the toolbar's right side (or wherever the primary action slot lives). Use existing Editorial dark-studio tokens (`--accent-primary` etc.) — DO NOT introduce a new color.
    - The button's `onClick` calls a new prop `onConvertSelection: (paths: string[]) => void` with `Array.from(selectedFilePaths)`.
    - When `selectedCount === 0`, the button is not rendered (or `display:none` per existing toolbar pattern — preserve hide/show semantics).

    Update src/renderer/src/App.tsx:
    - Extend the `activeView` state to include 'convertir' as a valid value (alongside the existing 'analyser' / 'tagger' / 'convertir' nav placeholders from Phase 1).
    - Wire `<ScanToolbar onConvertSelection={paths => { useConversionStore.getState().seedFilePaths(paths); setActiveView('convertir'); }} />`.
    - When activeView === 'convertir', render `<ConvertirView />` (component built in Task 4). For now (since ConvertirView is built in Task 4), render a placeholder `<div>convertir</div>` if necessary so this task's build is green. Update once Task 4 lands.

    Add `useConversionStore.seedFilePaths(paths: string[])` action as a temporary forward-reference; Task 3 builds the store and includes this action.

    Tests via @testing-library/react + vitest jsdom:
    - Render VirtualizedFileTable inside a Zustand provider; assert checkbox presence, toggle behavior, indeterminate state
    - Render ScanToolbar with a mocked useScanStore; assert button visibility + click handler
    - Render App with mocked stores; assert activeView switching + ConvertirView placeholder rendering

    Use the existing test setup from Phase 2 02-02; do not introduce new test infra.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/components/analyser src/renderer/src/App</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/components/analyser` passes existing Phase 2 tests + the new checkbox tests
    - `npm run build` exits 0 (App.tsx + new types compile under tsconfig)
    - Checkbox column is leftmost (column index 0); ~32px wide (assert via inline style or className with corresponding rule in CSS)
    - Header checkbox renders aria-checked="mixed" when partial selection (tri-state)
    - ScanToolbar button label is exactly `Convertir {N} fichier{s if N>1}` (singular/plural French)
    - Clicking the toolbar button switches activeView to 'convertir' AND seeds useConversionStore with the selected paths
    - The pluralization branch (1 file → "fichier", >1 → "fichiers") covered by a test
  </acceptance_criteria>
  <done>Selection mechanic visible in Analyser; toolbar primary action wired to seed Convertir view with the user's choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useConversionStore — Zustand store mirroring useScanStore shape</name>
  <files>src/renderer/src/store/useConversionStore.ts, src/renderer/src/store/useConversionStore.test.ts</files>
  <read_first>
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED: status enum, perFileProgress, summary counts, selectedPreset persisted in settings.conversion.lastPreset)
    - src/renderer/src/store/useScanStore.ts (mirror this shape — Zustand store + onEvent lifecycle pattern)
    - src/shared/ipc-types.ts (Preset, ConversionEvent, PRESETS from Plan 03-01 Task 2)
    - src/main/conversion/presets.ts (PRESETS const — re-imported into renderer or duplicated; preference: import the const from the shared module to avoid drift)
  </read_first>
  <behavior>
    - Test: initial state — status='idle', conversionId=null, perFileProgress=Map empty, fileStatuses=Map empty, errors=[], selectedPreset=PRESETS[0] (mp3-320 default per LOCKED decision), customPreset=null, pendingFilePaths=[]
    - Test: seedFilePaths(paths[]) → state.pendingFilePaths = paths (used by ScanToolbar's onConvertSelection)
    - Test: setPreset(preset) → state.selectedPreset = preset
    - Test: updateCustom(partial) → merges into state.customPreset
    - Test: startBatch() calls window.djUtils.conversion.start({rootFolder: from settings, filePaths: pendingFilePaths, preset: selectedPreset or customPreset if 'custom'}) and sets state.status='running', state.conversionId = the returned id
    - Test: startBatch persists selectedPreset to settings via window.djUtils.setSetting('conversion.lastPreset', JSON.stringify(preset)) (LOCKED persistence rule)
    - Test: cancelBatch() calls window.djUtils.conversion.cancel(conversionId)
    - Test: on subscribeEvents() the store wires window.djUtils.conversion.onEvent(handler) and returns an unsubscribe closure
    - Test: handler on {type:'progress', filePath, percent} updates perFileProgress.set(filePath, {percent, phase}); the Map instance is replaced (immutability)
    - Test: handler on {type:'fileDone', status:'done'} updates fileStatuses.set(filePath, 'done'); on status='error' also pushes {filePath, errorMessage} into errors
    - Test: handler on {type:'done'} sets state.status='done'
    - Test: handler on {type:'cancelled'} sets state.status='cancelled'
    - Test: handler on {type:'error'} sets state.status='error'
    - Test (derived selectors): progressGlobal = doneCount / totalCount; summaryCounts = {done, error, skipped}
    - Test: BatchAlreadyActive rejection from startBatch surfaces a toast-friendly error in state.error and state.status stays 'idle' (LOCKED: starting a second batch rejects)
  </behavior>
  <action>
    Create src/renderer/src/store/useConversionStore.ts using Zustand's `create` (same shape as useScanStore):

    ```ts
    interface ConversionState {
      status: 'idle' | 'running' | 'done' | 'cancelled' | 'error'
      conversionId: string | null
      pendingFilePaths: string[]
      perFileProgress: Map<string, { percent: number; phase: 'transcoding' | 'finalizing' }>
      fileStatuses: Map<string, ConversionFileStatus>
      errors: Array<{ filePath: string; errorMessage: string }>
      selectedPreset: Preset
      customPreset: Preset | null
      error: string | null
      // actions:
      seedFilePaths(paths: string[]): void
      setPreset(preset: Preset): void
      updateCustom(partial: Partial<Preset>): void
      startBatch(rootFolder: string): Promise<void>
      cancelBatch(): Promise<void>
      subscribeEvents(): () => void
      reset(): void
    }
    ```

    Mirror useScanStore's mutation discipline — all setters use the `set(s => ({ ... }))` form producing new objects/Maps. NEVER mutate. (TypeScript coding-style rule: immutable updates with spread or new Set/Map.)

    `startBatch`:
    1. Read selectedPreset (or customPreset if slug === 'custom')
    2. `set({ status: 'running', error: null, conversionId: null, perFileProgress: new Map(), fileStatuses: new Map(), errors: [] })`
    3. Try `const id = await window.djUtils.conversion.start({rootFolder, filePaths: pendingFilePaths, preset})`; on success `set({ conversionId: id })` and persist preset via `await window.djUtils.setSetting('conversion.lastPreset', JSON.stringify(preset))`.
    4. On rejection: catch BatchAlreadyActive (message check or error-type check) → set status='idle', error= the message; rethrow if not a known error so devtools show it.

    `subscribeEvents`: returns `window.djUtils.conversion.onEvent(handler)` where handler switches on event.type and applies the mutations described in the behaviors above. Used by ConvertirView via `useEffect(() => subscribeEvents(), [])`.

    Add a load-from-settings boot path: on first store access, attempt to read `await window.djUtils.getSetting('conversion.lastPreset')`; if present, JSON.parse and apply via setPreset. This MAY be done lazily in ConvertirView's useEffect rather than at module load (avoids async-at-module-load complications); planner discretion — pick one and document.

    Selectors as pure functions (not in store state):
    ```ts
    export const selectGlobalProgress = (s: ConversionState): number => {
      const total = s.pendingFilePaths.length
      if (total === 0) return 0
      const done = Array.from(s.fileStatuses.values()).filter(st => st === 'done' || st === 'skipped').length
      return Math.round((done / total) * 100)
    }
    export const selectSummaryCounts = (s: ConversionState) => ({
      done: countByStatus(s.fileStatuses, 'done'),
      error: countByStatus(s.fileStatuses, 'error'),
      skipped: countByStatus(s.fileStatuses, 'skipped'),
      cancelled: countByStatus(s.fileStatuses, 'cancelled')
    })
    ```

    Create useConversionStore.test.ts. Mock window.djUtils via vitest globalThis stub. Use act() + getState() for assertions.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/store/useConversionStore</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/store/useConversionStore` passes all behaviors above
    - All state updates produce new Map/Set/Array instances (assert reference inequality in 2+ tests)
    - subscribeEvents returns an unsubscribe closure that, when called, prevents further handler invocations
    - BatchAlreadyActive rejection keeps status='idle' and surfaces a French message
    - selectGlobalProgress returns 0 for empty pendingFilePaths (guard against /0)
    - Preset persistence: startBatch calls window.djUtils.setSetting with key 'conversion.lastPreset' and JSON-serialized preset
  </acceptance_criteria>
  <done>Conversion store mirrors scan store shape; subscribes to the bridge; persists last preset; survives BatchAlreadyActive cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: ConvertirView + PresetSelector + CustomPresetForm + ConversionProgress (the UI)</name>
  <files>src/renderer/src/components/convertir/ConvertirView.tsx, src/renderer/src/components/convertir/PresetSelector.tsx, src/renderer/src/components/convertir/CustomPresetForm.tsx, src/renderer/src/components/convertir/ConversionProgress.tsx, src/renderer/src/components/convertir/convertir.css</files>
  <read_first>
    - .planning/phases/03-conversion/03-CONTEXT.md (LOCKED French copy; Editorial dark-studio token reuse from main.css; LOCKED 5 presets + Custom; LOCKED per-file + global progress; LOCKED summary line shape)
    - .planning/phases/03-conversion/03-RESEARCH.md (Open Question 1 — UI inline in PLAN, not separate /gsd-ui-phase)
    - src/main/conversion/presets.ts (PRESETS const — import via shared types or relative)
    - src/renderer/src/store/useConversionStore.ts (from Task 3)
    - src/renderer/src/assets/main.css (LOCKED design tokens — reuse :root)
    - src/renderer/src/components/analyser/* (mirror Editorial dark-studio shapes — toolbar, table row treatment)
    - rules/ecc/web/design-quality.md (anti-template policy — Convertir view must have intentional hierarchy + designed states, not a default form stack)
  </read_first>
  <behavior>
    Component tests via @testing-library/react + jsdom:
    - Test (PresetSelector): renders 6 radio inputs (5 presets + Custom); clicking one calls setPreset; the active radio is checked; labels are French ("MP3 320 kbps (CBR)" etc. from PRESETS[i].label)
    - Test (CustomPresetForm): renders only when selectedPreset.slug === 'custom'; codec select has 5 options; bitrate input rejects non-positive ints and values >1024; sample rate input optional (empty = preserve source)
    - Test (CustomPresetForm validation): when bitrate=0 or bitrate=2048, the Lancer button (parent) is disabled (assert via aria-disabled or button.disabled prop)
    - Test (CustomPresetForm — FLAC/WAV branch): when codec is 'flac' or 'pcm_s16le', the bitrate input is greyed-out / disabled (bitrate ignored for lossless per RESEARCH §2)
    - Test (ConvertirView): renders PresetSelector + Lancer button + ConversionProgress + a back-to-Analyser link/button
    - Test (Lancer): clicking Lancer when status='idle' calls useConversionStore.startBatch(rootFolder); when status='running' the button text changes to "Annuler" and clicking calls cancelBatch
    - Test (Lancer disabled state): when pendingFilePaths is empty, Lancer is disabled
    - Test (ConversionProgress per-file row): renders one row per pendingFilePath with the basename + a progress bar reading perFileProgress.get(path)?.percent + a status badge (En attente / En cours / Converti / Erreur / Ignoré in French)
    - Test (global progress strip): renders a single bar at the top showing selectGlobalProgress(state)%; displays "{done}/{total} fichiers convertis" beside it
    - Test (summary counts at terminal): when status='done' or 'cancelled', renders "{N} converti, {M} en erreur, {K} ignorés" using selectSummaryCounts
    - Test (error row): files in errors[] show their errorMessage in the per-file row (truncate if >80 chars with a tooltip / title attribute)
    - Test (boot-load lastPreset): on mount ConvertirView reads window.djUtils.getSetting('conversion.lastPreset'); if present, calls setPreset on the parsed Preset — LOCKED persistence load
    - Test (subscribe lifecycle): on mount calls useConversionStore.subscribeEvents(); the returned unsubscribe is called on unmount (via useEffect cleanup)
  </behavior>
  <action>
    Create the four components + the CSS file. Design language: Editorial dark-studio — reuse `--color-bg`, `--color-surface`, `--color-text`, `--color-text-secondary`, `--color-accent` (or whatever the LOCKED Phase 1/2 token names are; consult main.css). DO NOT introduce new color variables. New CSS only for layout/spacing/typography composition — class names kebab-case (LOCKED naming).

    `src/renderer/src/components/convertir/convertir.css`: layout scaffolding for the view (a single column with a top header, a two-column section for PresetSelector + summary, and a bottom progress list with a fixed-height max + overflow-y:auto for long lists). Per design-quality.md rules: avoid generic card grid; the layout has hierarchy via type scale; per-file rows use rhythm not uniform padding; status badges use semantic color (success/error/muted).

    `PresetSelector.tsx`:
    - Renders a fieldset with `<legend>Format de sortie</legend>`.
    - Six radio inputs: 5 from PRESETS (mp3-320 selected by default), plus a sixth `Custom…` option that toggles the CustomPresetForm visibility (and sets selectedPreset to `{slug:'custom', label:'Personnalisé', codec:'libmp3lame', bitrateKbps:320, vbrQuality:null, sampleRate:null, extension:'.mp3'}` as the seed).
    - Each label shows the preset.label in French.

    `CustomPresetForm.tsx`:
    - Renders only when selectedPreset.slug === 'custom'.
    - Codec select: 5 options (`libmp3lame` → MP3, `aac` → AAC, `flac` → FLAC, `pcm_s16le` → WAV PCM 16-bit, `libopus` → Opus). When codec changes, also updates extension (`.mp3` / `.aac` / `.flac` / `.wav` / `.opus`).
    - Bitrate input: number, min=32, max=1024, step=8; greyed out (disabled + visual treatment) when codec ∈ {'flac', 'pcm_s16le'}.
    - Sample rate input: number, optional, placeholder "Conserver la source"; valid values 8000-192000.
    - Local validation: invalid inputs display an inline error message in French and disable the Lancer button via shared state (`useConversionStore.customPreset.isValid` flag, or compute validity at the parent ConvertirView level).

    `ConversionProgress.tsx`:
    - Top: a single global progress bar (use `<progress max="100" value={globalPercent} />` for accessible default semantics) + a text node showing `{done}/{total} fichiers convertis`.
    - Below: a virtualized list (use @tanstack/react-virtual since pendingFilePaths can be ~thousands) of per-file rows. Each row shows: basename (path.basename via a small helper), inline progress bar, status badge with French label.
    - At bottom (only when status ∈ {'done','cancelled'}): summary line `{N} converti, {M} en erreur, {K} ignorés` (or just `{N} converti` if M=K=0).

    `ConvertirView.tsx`:
    - Container with header showing `Convertir {N} fichiers` (where N = pendingFilePaths.length) and a "← Retour à l'Analyser" link.
    - Mounts subscribeEvents via useEffect.
    - Mounts the lastPreset load via useEffect (one-shot on mount).
    - Renders PresetSelector + CustomPresetForm + Lancer/Annuler button + ConversionProgress.

    Tests via @testing-library/react. Use the same test infra as Phase 2 02-02. For the @tanstack/react-virtual rendering, mock the virtualizer or test against the rendered DOM with a small pendingFilePaths array (the virtualizer renders all if total < windowHeight/rowHeight).
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/components/convertir</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/components/convertir` passes all behaviors above
    - convertir.css introduces NO new CSS custom properties (`--color-*`, `--space-*` etc.) — grep `--color-` in convertir.css returns no MATCHING new declarations (only references via `var(--color-...)` are allowed)
    - All user-visible copy is in French (grep convertir.tsx for English progress labels like "Loading" / "Done" — should be zero matches)
    - Lancer button is disabled when (pendingFilePaths.length === 0) OR (status === 'running' AND it shows Annuler instead) OR (custom preset is invalid)
    - PresetSelector default state matches PRESETS[0] (mp3-320 — LOCKED default)
    - Custom form codec change auto-updates extension
    - Bitrate input disabled visually + functionally when codec ∈ {flac, pcm_s16le}
    - ConversionProgress global bar reads from selectGlobalProgress (not from a stale duplicate count)
    - Summary line uses French singular/plural correctly: `{N} converti` (no plural form needed — past participle is invariant on number agreement in this construction)
  </acceptance_criteria>
  <done>Convertir view is a complete, designed, French, accessible UI satisfying CONV-01 trigger, CONV-02 preset+custom config, CONV-03 per-file + global progress, and CONV-04 error/skipped summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end Convertir UI on top of the Plan 03-01 backbone: checkbox column + select-all in Analyser's VirtualizedFileTable; "Convertir N fichiers" primary action in ScanToolbar that switches the view and seeds the selection; useConversionStore mirroring useScanStore with status/progress/errors/preset + IPC bridge subscription; ConvertirView with the 5-preset selector + Custom escape hatch + Lancer + live per-file progress list + global progress + summary counts. All copy French; Editorial dark-studio tokens reused; no new design primitives.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. Run a scan in Analyser (Phase 2 happy path) — no regression: rows appear, progress finishes.
    2. Click the header checkbox → all rows selected. Notice the toolbar grows a primary "Convertir 25 fichiers" (or however many) button.
    3. Click a few rows individually to deselect them. The header checkbox should show the tri-state indeterminate (`aria-checked="mixed"`).
    4. Click "Convertir 22 fichiers" (post-deselection count). The view switches to Convertir. The header shows "Convertir 22 fichiers" + "← Retour à l'Analyser".
    5. The preset selector is showing MP3 320 by default. Pick FLAC, then Custom → form appears, change codec to AAC, set bitrate to 192. The Lancer button stays enabled.
    6. Click `Custom…` → set codec to FLAC → notice the bitrate input greys out (LOCKED FLAC/WAV branch).
    7. Switch back to MP3 320 preset. Click Lancer. Live per-file progress bars fill up (animation visible). The global bar progresses. The Lancer button text changes to "Annuler".
    8. While running, click Annuler. Within ~1s the batch reports cancelled; the summary shows X converti + the rest cancelled. `ps aux | grep ffmpeg` shows no orphans. The `{rootFolder}/converted/mp3-320/` dir contains exactly the X completed outputs (no partials — Pitfall 3).
    9. Re-launch a small batch (3-5 files). Let it finish. The summary line shows `N converti, 0 en erreur, 0 ignorés` (or similar).
    10. Re-run the EXACT same selection again — the conflict policy should skip every file with `output_exists` → summary shows `0 converti, 0 en erreur, N ignorés`.
    11. Restart `npm run dev`. Open Convertir directly via the nav (if a nav switcher exists). Notice the preset selector remembers the last selection (LOCKED persistence — load from `conversion.lastPreset`).
    12. Verify Analyser doesn't regress: the row checkboxes still toggle, the scan still completes, the export CSV button still works (Plan 02-03).
  </how-to-verify>
  <resume-signal>Type "approved" if (a) Analyser checkbox + select-all + tri-state work, (b) toolbar "Convertir N fichiers" appears/disappears correctly with French singular/plural, (c) ConvertirView loads with MP3 320 default + last-preset persisted, (d) Custom form codec/bitrate/sample inputs validate locally, (e) Lancer runs a batch and the live progress bars + global bar update, (f) Annuler stops cleanly with no orphan ffmpeg + no partial outputs, (g) summary line shows accurate French counts, (h) re-running the same selection produces N ignorés (conflict policy), (i) Phase 2 Analyser + Export CSV still work. Otherwise describe which step failed (most likely: react-virtual sizing on Convertir progress list → check the parent container has an explicit height; or Phase 1 setSetting/getSetting binding missing → confirm via DevTools `await window.djUtils.getSetting('conversion.lastPreset')`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer DOM events → useConversionStore actions | Renderer-internal — no new IPC surface in this plan. All IPC goes through the bridge already validated in Plan 03-01. |
| renderer → main (conversion.*) | Phase 3 Plan 01 boundary; all payloads pass the V5 + allowlist gates established there. This plan only consumes that surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Elevation of Privilege | renderer components | mitigate (Phase 1 carry) | sandbox:true preload + contextBridge unchanged; this plan doesn't loosen anything |
| T-3-01 | Information Disclosure | conversion:start allowlist (Plan 03-01) | mitigate (carry) | Renderer can only ask main to convert files it received from a scan rooted under settings.rootFolder; main re-validates |
| T-3-10 | Tampering (V5 client-side) | CustomPresetForm bitrate / sample rate inputs accept arbitrary strings | mitigate | Local validation rejects non-positive ints and values >1024 (Task 4); the main-side handler (Plan 03-01 Task 9) re-validates server-side. Defence in depth — local validation is UX, not security. |
| T-3-11 | DoS (UI) | Per-file row rendering blows up with thousands of selected files | mitigate | Use @tanstack/react-virtual for the per-file progress list (Phase 2 carry-forward; already a dep). Tested implicitly by the row-count rendering test. |
| T-3-12 | Confused deputy | "Convertir N fichiers" wired to a user-controllable file list from useScanStore.selectedFilePaths | mitigate | Selection state is renderer-only and seeded from scan results that already passed the Phase 2 allowlist. When startBatch fires, main re-validates every path under settings.rootFolder (Plan 03-01 T-3-01). |
</threat_model>

<verification>
- `npm run build` green; existing Phase 1/2 renderer surfaces untouched.
- `npm run test -- --run src/renderer/src/store src/renderer/src/components/analyser src/renderer/src/components/convertir src/renderer/src/App` green.
- grep: convertir.css declares no new `--color-` custom properties (only `var(--color-...)` references).
- grep: no English progress labels in convertir/*.tsx ("Loading", "Done", "Cancel" → should be zero; French equivalents only).
- Manual checkpoint: select files → switch view → run → cancel → re-run → conflict policy fires → preset persists across restart.
</verification>

<success_criteria>
- CONV-01 (user selects + launches): checkbox column + toolbar action + Lancer wired end-to-end through the bridge.
- CONV-02 (target format configurable, MP3 320 default): 5-preset selector + Custom form; persisted to settings.conversion.lastPreset; reloaded on mount.
- CONV-03 (per-file + global progress): live progress bars driven by useConversionStore subscribing to conversion.onEvent.
- CONV-04 (per-file errors don't abort): batch reports `done` after all files terminal; summary shows N converti + M en erreur + K ignorés.
- LOCKED Editorial dark-studio tokens reused (no new `--color-*`); French copy throughout.
- LOCKED selection lives in useScanStore + resets on new scan + persists across view switches.
- LOCKED preset persistence in settings.conversion.lastPreset.
- @tanstack/react-virtual used for the per-file row list (DoS-resistance for large selections).
</success_criteria>

<output>
Create `.planning/phases/03-conversion/03-02-SUMMARY.md` when done.
</output>
