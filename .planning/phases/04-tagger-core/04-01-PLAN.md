---
phase: 04-tagger-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/shared/ipc-types.ts
  - src/main/db/connection.ts
  - src/main/db/settingsRepo.ts
  - src/main/tagger/taggerRepo.ts
  - src/main/tagger/taggerRepo.test.ts
  - src/main/tagger/queueBuilder.ts
  - src/main/tagger/queueBuilder.test.ts
  - src/main/tagger/audioProtocol.ts
  - src/main/tagger/audioProtocol.test.ts
  - src/main/scan/scanRepo.ts
  - src/main/scan/scanRepo.test.ts
  - src/main/ipc/tagger.ts
  - src/main/ipc/tagger.test.ts
  - src/main/index.ts
  - src/preload/index.ts
autonomous: false
requirements: [TAGG-01, TAGG-02, TAGG-04, TAGG-05, TAGG-06, TAGG-07, TAGG-10]
user_setup: []

must_haves:
  truths:
    - "scanRepo.findLatestScan(rootFolder) returns the most recent scan WHERE status='done' for that folder, or null (excludes running/cancelled/error)"
    - "scanRepo.listIncompleteFiles(scanId) returns scanned_files rows WHERE has_genre=0 OR has_bpm=0 OR has_key=0, ordered by path ASC"
    - "initTaggerSchema creates pending_tag_edits (PK file_path, CHECK rating BETWEEN 1 AND 5) and tagger_session (single-row CHECK id=1); safe to call twice"
    - "taggerRepo.upsertEdit(row, now) UPSERTs into pending_tag_edits via prepared statement; updated_at=now; applied_at preserved on update"
    - "taggerRepo.deleteEdit(filePath) removes the pending row (used by Undo-after-Keep when prior state was null)"
    - "taggerRepo.topGenres(limit) executes a single GROUP BY query over scanned_files.genre and returns [{genre,count}] in count DESC order"
    - "Six IPC channels registered: tagger:load-queue, tagger:save-edit, tagger:delete-edit, tagger:get-session, tagger:set-session, tagger:get-genre-presets — all gated by resolvesUnderRoot + AUDIO_EXTS where they accept a filePath"
    - "tagger:get-genre-presets merges library top-9 with hardcoded fallback (House, Techno, Afro House, Melodic, Disco, Hip-Hop, Funk, Deep, Electronica), deduped, library order first; returns exactly 9 presets"
    - "Custom protocol 'cratekeeper' registered via protocol.registerSchemesAsPrivileged({stream:true, supportFetchAPI:true, secure:true, standard:true}) BEFORE app.whenReady, AND protocol.handle inside whenReady"
    - "Audio protocol handler returns 403 for paths not under settings.rootFolder (T-4-01), 415 for extensions not in AUDIO_EXTS (T-4-02), and streams via net.fetch('file://'+abs) otherwise"
    - "BrowserWindow webPreferences includes autoplayPolicy:'no-user-gesture-required'"
    - "SETTINGS_KEY_ALLOWLIST includes 'tagger.muteEnabled' (T-4-07)"
    - "window.crateKeeper.tagger exposes loadQueue, saveEdit, deleteEdit, getSession, setSession, getGenrePresets — all typed against the shared IpcChannels"
  artifacts:
    - path: "src/shared/ipc-types.ts"
      provides: "Extended IpcChannels.TaggerLoadQueue/SaveEdit/DeleteEdit/GetSession/SetSession/GetGenrePresets; PendingTagEdit, TaggerQueueResult, TaggerSession, GenrePresetsResult interfaces; CrateKeeperTaggerApi; SETTINGS_KEY_ALLOWLIST extended with tagger.muteEnabled"
      contains: "tagger:"
    - path: "src/main/tagger/taggerRepo.ts"
      provides: "initTaggerSchema(db); createTaggerRepo(db) returning {upsertEdit, deleteEdit, getEdit, listEditsByPaths, getSession, setSession, topGenres}"
      contains: "pending_tag_edits"
    - path: "src/main/tagger/queueBuilder.ts"
      provides: "mergeGenrePresets(libraryTop9, fallback) pure helper enforcing 9-slot output and dedup"
      contains: "mergeGenrePresets"
    - path: "src/main/tagger/audioProtocol.ts"
      provides: "AUDIO_PROTOCOL_SCHEME constant; registerAudioProtocol(settingsRepo) with protocol.handle + folder-allowlist + AUDIO_EXTS gate"
      contains: "protocol.handle"
    - path: "src/main/scan/scanRepo.ts"
      provides: "Extended ScanRepo with findLatestScan(rootFolder): ScanRow|null and listIncompleteFiles(scanId): ScannedFile[]"
      contains: "findLatestScan"
    - path: "src/main/ipc/tagger.ts"
      provides: "registerTaggerHandlers({ipcMain, taggerRepo, scanRepo, settingsRepo}) registering the 6 channels with V5 validation + folder/extension allowlist"
      contains: "tagger:load-queue"
    - path: "src/preload/index.ts"
      provides: "Extended window.crateKeeper.tagger namespace with the 6 methods"
      contains: "tagger:"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/main/ipc/tagger.ts"
      via: "ipcRenderer.invoke('tagger:load-queue' | 'tagger:save-edit' | 'tagger:delete-edit' | 'tagger:get-session' | 'tagger:set-session' | 'tagger:get-genre-presets')"
      pattern: "tagger:(load-queue|save-edit|delete-edit|get-session|set-session|get-genre-presets)"
    - from: "src/main/ipc/tagger.ts"
      to: "src/main/tagger/taggerRepo.ts"
      via: "taggerRepo.upsertEdit / deleteEdit / getSession / setSession / topGenres / listEditsByPaths"
      pattern: "taggerRepo\\."
    - from: "src/main/ipc/tagger.ts"
      to: "src/main/scan/scanRepo.ts"
      via: "scanRepo.findLatestScan(rootFolder) + scanRepo.listIncompleteFiles(scanId)"
      pattern: "findLatestScan|listIncompleteFiles"
    - from: "src/main/index.ts"
      to: "src/main/tagger/audioProtocol.ts"
      via: "registerAudioProtocol(settingsRepo) inside app.whenReady, after settingsRepo init"
      pattern: "registerAudioProtocol"
    - from: "src/main/db/connection.ts"
      to: "src/main/tagger/taggerRepo.ts"
      via: "initTaggerSchema(db) at openDb() + getTaggerRepo() lazy singleton"
      pattern: "initTaggerSchema"
---

<objective>
Ship the entire main-process backbone for Phase 4 Tagger as one vertical slice. Extend the typed IPC bridge with a `tagger` namespace (6 channels), add `pending_tag_edits` + `tagger_session` SQLite tables behind the established `createXRepo(db) + initXSchema(db)` factory pattern, extend `scanRepo` with `findLatestScan(rootFolder)` + `listIncompleteFiles(scanId)`, register the `cratekeeper://audio/<encoded-path>` custom protocol with folder-allowlist + AUDIO_EXTS gates, and add `autoplayPolicy:'no-user-gesture-required'` to BrowserWindow. This satisfies TAGG-01 (queue source), TAGG-02 (preview streaming substrate), TAGG-04 (presets query), and the storage spine for TAGG-05/06/07/10 — without yet rendering anything.

Purpose: Plan 04-02 builds the card UX on top of this surface; Plan 04-03 layers session resume + undo polish. Threat model T-4-01..08 is mitigated here (path-traversal at protocol layer + IPC layer, V5 length/range caps, AUDIO_EXTS allowlist, settings allowlist extension).

Output: Typed `window.crateKeeper.tagger` API; pending_tag_edits + tagger_session tables; custom protocol streaming files under rootFolder with folder + extension gates; six IPC handlers with V5 validation; one-line autoplay policy + privileged-scheme registration; unit tests on repo + queueBuilder + audioProtocol + scanRepo extensions + ipc/tagger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tagger-core/04-CONTEXT.md
@.planning/phases/04-tagger-core/04-RESEARCH.md
@.planning/phases/04-tagger-core/04-VALIDATION.md
@.planning/phases/02-scanning/02-01-SUMMARY.md
@.planning/phases/03-conversion/03-01-SUMMARY.md
@src/shared/ipc-types.ts
@src/main/index.ts
@src/main/db/connection.ts
@src/main/db/settingsRepo.ts
@src/main/scan/scanRepo.ts
@src/main/conversion/conversionRepo.ts
@src/main/ipc/conversion.ts
@src/preload/index.ts
@src/main/workers/scanCore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend IPC type surface (tagger namespace) + SETTINGS allowlist + preload bridge</name>
  <files>src/shared/ipc-types.ts, src/preload/index.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-CONTEXT.md (LOCKED IPC surface — 6 channels exact names)
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Validation Architecture; T-4-07 settings allowlist extension)
    - src/shared/ipc-types.ts (current IpcChannels + CrateKeeperApi.scan/conversion — preserve additively; locate SETTINGS_KEY_ALLOWLIST constant)
    - src/preload/index.ts (current crateKeeper.scan / crateKeeper.conversion exposure pattern; sandbox:true only `electron` + shared types imports)
  </read_first>
  <behavior>
    - Type-level additive change. Compile-time test = `npm run build` typechecks renderer/main/preload against the extended CrateKeeperApi.
    - Channels added: TaggerLoadQueue, TaggerSaveEdit, TaggerDeleteEdit, TaggerGetSession, TaggerSetSession, TaggerGetGenrePresets — values exactly per LOCKED decision.
    - SETTINGS_KEY_ALLOWLIST gains `tagger.muteEnabled` (T-4-07 mitigation).
    - preload exposes `crateKeeper.tagger.*` as a sibling of `crateKeeper.scan` / `crateKeeper.conversion`. No new imports beyond `electron` + `../shared/ipc-types`.
  </behavior>
  <action>
    Extend src/shared/ipc-types.ts ADDITIVELY (per D-CONTEXT carry-forward: never mutate existing channel values).

    Add to IpcChannels:
    - `TaggerLoadQueue: 'tagger:load-queue'`
    - `TaggerSaveEdit: 'tagger:save-edit'`
    - `TaggerDeleteEdit: 'tagger:delete-edit'`
    - `TaggerGetSession: 'tagger:get-session'`
    - `TaggerSetSession: 'tagger:set-session'`
    - `TaggerGetGenrePresets: 'tagger:get-genre-presets'`

    Add interfaces:
    - `PendingTagEdit { filePath: string; genre: string | null; bpm: number | null; key: string | null; artist: string | null; title: string | null; comment: string | null; rating: number | null; updatedAt: number; appliedAt: number | null }`
    - `TaggerSession { rootFolder: string; currentFilePath: string | null; scanId: string | null; updatedAt: number }`
    - `TaggerQueueResult { scanId: string | null; files: ScannedFile[]; pendingEdits: Record<string, PendingTagEdit> }`
    - `GenrePresetsResult { source: 'library' | 'defaults' | 'mixed'; presets: string[] }` (length always 9)
    - `SaveTagEditInput { filePath: string; genre?: string | null; bpm?: number | null; key?: string | null; artist?: string | null; title?: string | null; comment?: string | null; rating?: number | null }`

    Add `interface CrateKeeperTaggerApi`:
    - `loadQueue(): Promise<TaggerQueueResult>`
    - `saveEdit(input: SaveTagEditInput): Promise<void>`
    - `deleteEdit(filePath: string): Promise<void>`
    - `getSession(): Promise<TaggerSession | null>`
    - `setSession(input: { currentFilePath: string | null; scanId: string | null }): Promise<void>`
    - `getGenrePresets(): Promise<GenrePresetsResult>`

    Extend `CrateKeeperApi` to include `tagger: CrateKeeperTaggerApi` alongside existing `scan`, `conversion`, and Phase 1 members.

    Locate the existing `SETTINGS_KEY_ALLOWLIST` constant (added in Phase 3 Plan 02). Add the literal `'tagger.muteEnabled'` to the array. Document the addition with a one-line comment citing T-4-07.

    Extend src/preload/index.ts: build a `tagger` object wrapping `ipcRenderer.invoke(IpcChannels.TaggerLoadQueue)` etc. Compose into the existing `crateKeeper` object as `{ ...existing, tagger }`. No new imports.

    Run `npm run build` to confirm preload + main + renderer all typecheck.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - grep "tagger:" src/shared/ipc-types.ts shows the 6 new channels + interface declarations
    - grep "tagger.muteEnabled" src/shared/ipc-types.ts shows the allowlist entry
    - grep "tagger" src/preload/index.ts shows wrapper functions wired to ipcRenderer.invoke using shared IpcChannels constants (no string literals)
    - Existing Phase 1/2/3 members untouched
    - preload imports only 'electron' and '../shared/ipc-types' (grep — no other imports)
  </acceptance_criteria>
  <done>Typed Tagger bridge surface end-to-end; settings allowlist mitigates T-4-07; per D-CONTEXT LOCKED IPC namespace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend scanRepo with findLatestScan + listIncompleteFiles</name>
  <files>src/main/scan/scanRepo.ts, src/main/scan/scanRepo.test.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Open Q 6 — findLatestScan scoping; LOCKED status='done' filter)
    - .planning/phases/04-tagger-core/04-CONTEXT.md (LOCKED queue order = path ASC; filter hasGenre=0 OR hasBpm=0 OR hasKey=0)
    - src/main/scan/scanRepo.ts (preserve existing surface — pure additive)
    - src/main/scan/scanRepo.test.ts (existing test patterns)
  </read_first>
  <behavior>
    - Test: findLatestScan('/Music') returns the most recent scan WHERE root_folder='/Music' AND status='done' ORDER BY started_at DESC LIMIT 1
    - Test: findLatestScan excludes status='running' (returns the older 'done' instead, even if a 'running' is newer)
    - Test: findLatestScan excludes status='cancelled' AND status='error'
    - Test: findLatestScan returns null when no scans exist for the folder
    - Test: findLatestScan returns null when scans exist but only with non-'done' status
    - Test: listIncompleteFiles(scanId) returns rows WHERE has_genre=0 OR has_bpm=0 OR has_key=0; ORDER BY path ASC; deserialised to ScannedFile shape
    - Test: listIncompleteFiles excludes files where all three flags are 1 (fully tagged)
    - Test: listIncompleteFiles on a scanId with no matches returns []
    - Test: parameterised statements only (assert by reading scanRepo.ts source — no template literals embedding values)
  </behavior>
  <action>
    Extend src/main/scan/scanRepo.ts additively. Update the ScanRepo interface to add:
    - `findLatestScan(rootFolder: string): ScanRow | null`
    - `listIncompleteFiles(scanId: string): ScannedFile[]`

    Implement using prepared statements:
    ```
    const findLatestScanStmt = db.prepare(
      "SELECT * FROM scans WHERE root_folder = ? AND status = 'done' " +
      'ORDER BY started_at DESC LIMIT 1'
    )
    const listIncompleteFilesStmt = db.prepare(
      'SELECT path, format, bitrate, size_bytes, sample_rate, duration_seconds, ' +
      'has_genre, has_bpm, has_key, parsed_ok, error_message ' +
      'FROM scanned_files WHERE scan_id = ? AND ' +
      '(has_genre = 0 OR has_bpm = 0 OR has_key = 0) ORDER BY path ASC'
    )
    ```

    Map results through the existing `toScanRow` / `toScannedFile` helpers (no new mapping).

    Extend scanRepo.test.ts with the 9 behaviors above. Use the same `new Database(':memory:')` + `initScanSchema(db)` + `createScanRepo(db)` scaffolding as the existing tests. Seed 3-4 scans with varied statuses + timestamps and 5-6 file rows with varied has_* flags.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/scan/scanRepo</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/scan/scanRepo` passes existing Phase 2 tests + the 9 new behaviors
    - findLatestScan SQL contains the literal `status = 'done'` (status filter via prepared statement value, not template literal)
    - listIncompleteFiles result excludes rows with all three has_* flags true
    - All Phase 2 ScanRepo consumers (controller, ipc/scan) still typecheck under `npm run build`
  </acceptance_criteria>
  <done>scanRepo extended with the two helpers Tagger needs; clean boundary preserved (Analyser owns scanning, Tagger only reads).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: taggerRepo schema + UPSERT/get/session/topGenres</name>
  <files>src/main/tagger/taggerRepo.ts, src/main/tagger/taggerRepo.test.ts, src/main/db/connection.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Pattern 1 repo factory; CHECK constraint on rating; T-4-04 + T-4-05 input bounds documented)
    - .planning/phases/04-tagger-core/04-CONTEXT.md (LOCKED schema fields; tagger_session single-row CHECK id=1; identity = file path)
    - src/main/conversion/conversionRepo.ts (Phase 3 repo pattern — EXACTLY mirror this shape and connection.ts wiring)
    - src/main/db/connection.ts (existing openDb + initScanSchema + initConversionSchema + getConversionRepo — extend symmetrically)
  </read_first>
  <behavior>
    - Test: initTaggerSchema creates `pending_tag_edits` (PK file_path, rating CHECK 1..5 or NULL, updated_at NOT NULL, applied_at NULL) + `tagger_session` (CHECK id=1, root_folder NOT NULL); safe to call twice (`IF NOT EXISTS`)
    - Test: upsertEdit({filePath, genre:'House', bpm:128, ...}, now=1000) inserts a row with updated_at=1000 and applied_at=NULL
    - Test: upsertEdit twice for the same filePath REPLACEs the row but preserves applied_at if it was set by a previous direct update (use INSERT ... ON CONFLICT DO UPDATE)
    - Test: upsertEdit with rating=6 throws (DB CHECK constraint enforces 1-5)
    - Test: upsertEdit with rating=0 throws (CHECK rejects 0)
    - Test: upsertEdit with rating=null is accepted (nullable per schema)
    - Test: deleteEdit(filePath) removes the row; subsequent getEdit returns null
    - Test: getEdit(filePath) returns the row when present, null otherwise; deserialised to PendingTagEdit shape (camelCase keys)
    - Test: listEditsByPaths(['a.mp3','b.mp3']) returns a Map keyed by file_path with only the matching rows
    - Test: listEditsByPaths([]) returns an empty Map without running a query
    - Test: getSession() returns null when no row exists
    - Test: setSession({rootFolder:'/Music', currentFilePath:'/Music/a.mp3', scanId:'s1'}, now=2000) UPSERTs the single row (id=1); a second call REPLACEs and bumps updated_at
    - Test: getSession() after setSession returns the row with TaggerSession shape (camelCase, updatedAt = the now from setSession)
    - Test: topGenres(9) returns up to 9 rows {genre, count} ordered by count DESC; excludes rows where scanned_files.genre IS NULL OR genre = ''
    - Test: topGenres(9) on an empty scanned_files table returns []
    - Test: parameterised statements only (grep the source: no template-literal SQL with values)
  </behavior>
  <action>
    Create src/main/tagger/taggerRepo.ts mirroring createConversionRepo verbatim:

    Export `initTaggerSchema(db: Database.Database)` that runs (single `db.exec` call):
    ```
    CREATE TABLE IF NOT EXISTS pending_tag_edits (
      file_path TEXT PRIMARY KEY,
      genre TEXT,
      bpm INTEGER,
      key TEXT,
      artist TEXT,
      title TEXT,
      comment TEXT,
      rating INTEGER CHECK(rating IS NULL OR (rating >= 1 AND rating <= 5)),
      updated_at INTEGER NOT NULL,
      applied_at INTEGER
    );
    CREATE TABLE IF NOT EXISTS tagger_session (
      id INTEGER PRIMARY KEY CHECK(id = 1),
      root_folder TEXT NOT NULL,
      current_file_path TEXT,
      scan_id TEXT,
      updated_at INTEGER NOT NULL
    );
    ```
    Then `db.pragma('foreign_keys = ON')` for symmetry with conversionRepo (no FKs here but keep the pragma).

    Export `interface TaggerRepo` per the must_haves above.

    Export `createTaggerRepo(db)` that prepares statements once and returns the implementation:
    - upsertEdit: `INSERT INTO pending_tag_edits (file_path, genre, bpm, key, artist, title, comment, rating, updated_at) VALUES (@filePath, @genre, ...) ON CONFLICT(file_path) DO UPDATE SET genre=excluded.genre, bpm=excluded.bpm, key=excluded.key, artist=excluded.artist, title=excluded.title, comment=excluded.comment, rating=excluded.rating, updated_at=excluded.updated_at` — note: `applied_at` is NOT updated, preserving Phase 5's write marker
    - deleteEdit: `DELETE FROM pending_tag_edits WHERE file_path = ?`
    - getEdit: `SELECT * FROM pending_tag_edits WHERE file_path = ?`; map to PendingTagEdit
    - listEditsByPaths(paths): build a `SELECT * FROM pending_tag_edits WHERE file_path IN (?, ?, ...)` statement with dynamic placeholder count (still parameterised — placeholders are `?`, values are bound). Return Map<string, PendingTagEdit>. Early-return new Map() when paths is empty.
    - getSession: `SELECT * FROM tagger_session WHERE id = 1`; map to TaggerSession or null
    - setSession: `INSERT INTO tagger_session (id, root_folder, current_file_path, scan_id, updated_at) VALUES (1, @rootFolder, @currentFilePath, @scanId, @now) ON CONFLICT(id) DO UPDATE SET root_folder=excluded.root_folder, current_file_path=excluded.current_file_path, scan_id=excluded.scan_id, updated_at=excluded.updated_at`
    - topGenres(limit): `SELECT genre, COUNT(*) AS count FROM scanned_files WHERE genre IS NOT NULL AND genre != '' GROUP BY genre ORDER BY count DESC LIMIT ?`. Note: queries scanned_files (Phase 2 table) — taggerRepo intentionally crosses to scanRepo's data; document the cross-table read with a one-line comment.

    Extend src/main/db/connection.ts symmetrically with conversionRepo wiring:
    - import `initTaggerSchema, createTaggerRepo, type TaggerRepo`
    - In openDb(), AFTER `initConversionSchema(db)`, also call `initTaggerSchema(db)`
    - Add `let taggerRepoInstance: TaggerRepo | null = null` module-scope
    - Export `function getTaggerRepo(): TaggerRepo` lazy singleton
    - Update closeDb() to also null `taggerRepoInstance`

    Create src/main/tagger/taggerRepo.test.ts. Use `new Database(':memory:')`, initScanSchema (for topGenres' scanned_files dependency) + initTaggerSchema. Seed scanned_files rows directly for topGenres tests.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/tagger/taggerRepo</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/tagger/taggerRepo` passes all 15 behaviors above
    - taggerRepo.ts uses db.prepare(...) exclusively; grep `\$\{` inside SQL strings returns no matches
    - upsertEdit preserves applied_at on conflict update (explicit test asserts: pre-set applied_at remains after second upsert)
    - rating CHECK constraint enforced at DB level (rating=6 throws SqliteError)
    - connection.ts calls initTaggerSchema(db) inside openDb() AFTER initConversionSchema(db); exports getTaggerRepo() lazy singleton
    - topGenres excludes empty-string and NULL genres (explicit test)
  </acceptance_criteria>
  <done>Persistence backbone proven in-memory; CHECK constraint mitigates T-4-04; INTEGER bpm column shape is foundation for T-4-05 (V5 in handler refines further).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: queueBuilder.mergeGenrePresets pure helper</name>
  <files>src/main/tagger/queueBuilder.ts, src/main/tagger/queueBuilder.test.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-CONTEXT.md (LOCKED hardcoded fallback list AND merge order: library first, fallback fills remaining slots, dedup preserving library order)
  </read_first>
  <behavior>
    - Test: mergeGenrePresets([], FALLBACK) returns the 9 fallback genres in order
    - Test: mergeGenrePresets(['House','Techno','Disco','Funk','Soul','Reggae','Jazz','Pop','Rock'], FALLBACK) returns those 9 verbatim (library fills all 9 slots, fallback unused)
    - Test: mergeGenrePresets(['House','Techno','Disco'], FALLBACK) returns ['House','Techno','Disco', ...fallback items NOT already in library, capped at 9]
    - Test: mergeGenrePresets(['House','Techno'], FALLBACK) where FALLBACK[0]='House' — 'House' MUST NOT appear twice; library order wins for the duplicate; result length still 9
    - Test: mergeGenrePresets(libraryWith20Items, FALLBACK) returns only the first 9 library items (truncate, fallback unused)
    - Test: mergeGenrePresets is case-sensitive for dedup ('house' != 'House' — both appear)
    - Test: HARDCODED_GENRE_FALLBACK exports exactly the 9 strings in LOCKED order: House, Techno, Afro House, Melodic, Disco, Hip-Hop, Funk, Deep, Electronica
    - Test: helper is pure (no imports beyond optional types; same input → same output)
  </behavior>
  <action>
    Create src/main/tagger/queueBuilder.ts:

    ```ts
    export const HARDCODED_GENRE_FALLBACK: ReadonlyArray<string> = [
      'House', 'Techno', 'Afro House', 'Melodic', 'Disco',
      'Hip-Hop', 'Funk', 'Deep', 'Electronica'
    ] as const

    export function mergeGenrePresets(
      libraryTop: readonly string[],
      fallback: readonly string[] = HARDCODED_GENRE_FALLBACK
    ): string[] {
      const out: string[] = []
      const seen = new Set<string>()
      for (const g of libraryTop) {
        if (out.length >= 9) break
        if (!seen.has(g)) { out.push(g); seen.add(g) }
      }
      for (const g of fallback) {
        if (out.length >= 9) break
        if (!seen.has(g)) { out.push(g); seen.add(g) }
      }
      return out
    }
    ```

    No imports from electron, no fs, no DB — pure module.

    Create queueBuilder.test.ts covering the 8 behaviors. Use the exported HARDCODED_GENRE_FALLBACK in tests (do not re-declare; assert it equals the LOCKED literal exactly).
  </action>
  <verify>
    <automated>npm run test -- --run src/main/tagger/queueBuilder</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/tagger/queueBuilder` passes all 8 behaviors
    - Output length is exactly 9 for any non-empty input (when library + fallback together cover ≥ 9 unique values)
    - grep src/main/tagger/queueBuilder.ts: no imports beyond `type` imports (pure module)
    - HARDCODED_GENRE_FALLBACK order matches LOCKED literal exactly
  </acceptance_criteria>
  <done>Genre preset merge logic isolated as a pure helper; tested without DB; LOCKED 9-slot invariant enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Custom protocol registration with folder + extension allowlist (T-4-01, T-4-02)</name>
  <files>src/main/tagger/audioProtocol.ts, src/main/tagger/audioProtocol.test.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Pattern 2 protocol.handle; Pitfalls 2 registerSchemesAsPrivileged + 5 autoplay + 6 AIFF + 8 path-traversal; T-4-01 + T-4-02 threats)
    - src/main/workers/scanCore.ts (AUDIO_EXTS constant — the SAME allowlist Phase 2 uses; import don't redefine)
    - src/main/db/settingsRepo.ts (SettingsRepo.get('rootFolder') signature)
  </read_first>
  <behavior>
    - Test: AUDIO_PROTOCOL_SCHEME exported as the literal 'cratekeeper'
    - Test: registerAudioProtocol(mockSettingsRepo) calls protocol.handle('cratekeeper', handler) once
    - Test: handler called with `cratekeeper://wrong-host/something.mp3` returns Response status 404 (hostname !== 'audio')
    - Test: handler called with `cratekeeper://audio/foo.mp3` when settingsRepo returns rootFolder=null returns 403
    - Test: handler with a path that does NOT start with rootFolder + path.sep returns 403 (T-4-01 path-traversal mitigation)
    - Test: handler with path exactly equal to rootFolder returns 403 (rootFolder itself is a directory, not an audio file)
    - Test: handler with `../` segments that resolve outside rootFolder returns 403 (path.resolve normalisation defence)
    - Test: handler with a path under rootFolder but extension '.txt' returns 415 (T-4-02 AUDIO_EXTS gate)
    - Test: handler with extension '.MP3' (uppercase) is normalised to lowercase and accepted (case-insensitive ext check)
    - Test: handler with a valid path + valid extension calls net.fetch with `file://<abs>` and returns its Response (mock net.fetch)
    - Test: handler decodes the URL pathname (encodeURIComponent of a path with spaces decodes correctly)
  </behavior>
  <action>
    Create src/main/tagger/audioProtocol.ts:

    ```ts
    import { protocol, net } from 'electron'
    import path from 'node:path'
    import type { SettingsRepo } from '../db/settingsRepo'
    import { AUDIO_EXTS } from '../workers/scanCore'

    export const AUDIO_PROTOCOL_SCHEME = 'cratekeeper'

    export function registerAudioProtocol(settingsRepo: SettingsRepo): void {
      protocol.handle(AUDIO_PROTOCOL_SCHEME, async (request) => {
        const url = new URL(request.url)
        if (url.hostname !== 'audio') {
          return new Response('Not Found', { status: 404 })
        }
        const decoded = decodeURIComponent(url.pathname.replace(/^\//, ''))
        const abs = path.resolve(decoded)
        const root = settingsRepo.get('rootFolder')
        if (root === null || abs === root || !abs.startsWith(root + path.sep)) {
          // T-4-01: never serve files outside settings.rootFolder
          return new Response('Forbidden', { status: 403 })
        }
        const ext = path.extname(abs).toLowerCase()
        if (!(AUDIO_EXTS as readonly string[]).includes(ext)) {
          // T-4-02: extension allowlist defence-in-depth
          return new Response('Unsupported Media Type', { status: 415 })
        }
        return net.fetch('file://' + abs)
      })
    }
    ```

    Create audioProtocol.test.ts. Mock `electron`'s protocol + net via vitest:
    ```ts
    vi.mock('electron', () => ({
      protocol: { handle: vi.fn() },
      net: { fetch: vi.fn(async (u: string) => new Response('ok')) }
    }))
    ```
    Extract the registered handler by capturing the second arg of `protocol.handle.mock.calls[0][1]`. Build a mock `Request` with the URL under test and invoke the handler.

    Mock SettingsRepo as `{ get: vi.fn() }` returning the desired rootFolder per test case.

    Cover all 11 behaviors. For the case-insensitive ext test, use a real file path string `/root/Track.MP3` and assert status !== 415.

    DO NOT import settingsRepo's real constructor — only the type. The mock supplies the runtime.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/tagger/audioProtocol</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/tagger/audioProtocol` passes all 11 behaviors
    - grep src/main/tagger/audioProtocol.ts: contains `path.resolve`, `startsWith(root + path.sep)`, `AUDIO_EXTS`, `net.fetch`
    - Handler returns 403 for ANY path not strictly under rootFolder (T-4-01 explicit test)
    - Handler returns 415 for ANY non-audio extension (T-4-02 explicit test)
    - Handler does NOT call net.fetch when the 403 or 415 branch fires (explicit test asserts net.fetch.mock.calls.length === 0)
  </acceptance_criteria>
  <done>Custom protocol path-traversal + extension gates locked at handler level; T-4-01 and T-4-02 mitigated; defence-in-depth for the IPC layer's gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: ipc/tagger handlers — V5 validation + folder-allowlist on every channel</name>
  <files>src/main/ipc/tagger.ts, src/main/ipc/tagger.test.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Validation Architecture; T-4-03 to T-4-08 threats)
    - .planning/phases/04-tagger-core/04-CONTEXT.md (LOCKED IPC contract returns; carry-forward V5 + folder-allowlist + lazy require('electron'))
    - src/main/ipc/conversion.ts (Phase 3 handler shape — mirror assertObject/assertPreset/resolvesUnderRoot helpers verbatim)
    - src/main/workers/scanCore.ts (AUDIO_EXTS for extension gate)
    - src/main/tagger/taggerRepo.ts (from Task 3)
    - src/main/scan/scanRepo.ts (from Task 2 — findLatestScan + listIncompleteFiles)
    - src/main/tagger/queueBuilder.ts (mergeGenrePresets + HARDCODED_GENRE_FALLBACK)
  </read_first>
  <behavior>
    - Test: registerTaggerHandlers({ipcMain, taggerRepo, scanRepo, settingsRepo, now}) registers handlers for all 6 channels (ipcMain.handle called 6× with the IpcChannels constants)
    - Test (tagger:load-queue): when scanRepo.findLatestScan returns null, returns {scanId: null, files: [], pendingEdits: {}}
    - Test (tagger:load-queue): when a scan exists, returns {scanId, files: listIncompleteFiles result, pendingEdits: keyed by file_path}
    - Test (tagger:load-queue): when settings.rootFolder is null, returns {scanId: null, files: [], pendingEdits: {}} (no exception — empty state)
    - Test (tagger:save-edit): rejects payload that is not an object (TypeError)
    - Test (tagger:save-edit): rejects filePath that does NOT resolve under settings.rootFolder (T-4-01 mirror — `Error: file not under rootFolder`)
    - Test (tagger:save-edit): rejects filePath whose extension is not in AUDIO_EXTS (T-4-02 mirror)
    - Test (tagger:save-edit): rejects rating not in {null, 1, 2, 3, 4, 5} (T-4-04 — V5 cap mirrors DB CHECK)
    - Test (tagger:save-edit): rejects bpm where bpm !== null && (!Number.isInteger(bpm) || bpm <= 0 || bpm >= 400) (T-4-05)
    - Test (tagger:save-edit): rejects genre/artist/title/comment longer than 500 chars (T-4-03 length cap; key capped at 16 chars — Camelot 'A12'/'12A' style)
    - Test (tagger:save-edit): valid payload calls taggerRepo.upsertEdit with normalised values and now(); returns undefined
    - Test (tagger:delete-edit): rejects filePath that does not resolve under rootFolder; valid path calls taggerRepo.deleteEdit
    - Test (tagger:get-session): returns taggerRepo.getSession() result verbatim
    - Test (tagger:set-session): accepts currentFilePath = null (clearing)
    - Test (tagger:set-session): rejects non-null currentFilePath that does not resolve under rootFolder
    - Test (tagger:set-session): calls taggerRepo.setSession({rootFolder: settings.rootFolder, currentFilePath, scanId}, now())
    - Test (tagger:get-genre-presets): when topGenres returns ≥9, returns {source:'library', presets: first-9 names}
    - Test (tagger:get-genre-presets): when topGenres returns 3, returns {source:'mixed', presets: 9 strings — library first, fallback filling}
    - Test (tagger:get-genre-presets): when topGenres returns 0, returns {source:'defaults', presets: HARDCODED_GENRE_FALLBACK}
    - Test: registration uses lazy `require('electron')` style — ipcMain is passed in as a dependency (mirror conversion.ts) so vitest can construct the registrar without importing real electron
  </behavior>
  <action>
    Create src/main/ipc/tagger.ts mirroring src/main/ipc/conversion.ts:

    Imports: `import type { IpcMain, IpcMainInvokeEvent } from 'electron'`; types from '../../shared/ipc-types'; `import { AUDIO_EXTS } from '../workers/scanCore'`; types-only from '../tagger/taggerRepo' + '../scan/scanRepo' + '../db/settingsRepo'; `import { mergeGenrePresets, HARDCODED_GENRE_FALLBACK } from '../tagger/queueBuilder'`; `import path from 'node:path'`.

    Constants:
    - `const ROOT_FOLDER_KEY = 'rootFolder'`
    - `const MAX_TEXT_LEN = 500` (T-4-03 cap for genre/artist/title/comment)
    - `const MAX_KEY_LEN = 16` (Camelot strings short)
    - `const MIN_BPM = 1`, `const MAX_BPM = 399` (T-4-05 bounds; integer)
    - `const ALLOWED_RATINGS = new Set([1, 2, 3, 4, 5])` (T-4-04)

    Helpers (mirror conversion.ts verbatim):
    - `assertObject(v, label)` — TypeError on non-object
    - `resolvesUnderRoot(filePath, rootFolder)` — path.resolve comparison with sep guard
    - `assertAudioExt(filePath)` — throws if !AUDIO_EXTS.includes(path.extname(filePath).toLowerCase())

    Helper `assertSaveEditInput(payload, rootFolder): SaveTagEditInput`:
    1. assertObject(payload, 'tagger:save-edit')
    2. typeof payload.filePath === 'string', else TypeError
    3. resolvesUnderRoot + assertAudioExt
    4. For each optional field (genre, artist, title, comment, key, bpm, rating):
       - If undefined or null: pass through (null in normalised output)
       - text fields: typeof string && length ≤ MAX_TEXT_LEN; key field: length ≤ MAX_KEY_LEN
       - bpm: null OR (Number.isInteger && MIN_BPM ≤ bpm ≤ MAX_BPM)
       - rating: null OR ALLOWED_RATINGS.has(rating)
    5. Return normalised SaveTagEditInput (undefined-fields coerced to null)

    `registerTaggerHandlers({ipcMain, taggerRepo, scanRepo, settingsRepo, now = () => Date.now()})`:

    - `ipcMain.handle(IpcChannels.TaggerLoadQueue, async (_e: IpcMainInvokeEvent) => { ... })`:
      - const root = settingsRepo.get(ROOT_FOLDER_KEY); if !root → return {scanId:null, files:[], pendingEdits:{}}
      - const scan = scanRepo.findLatestScan(root); if !scan → return {scanId:null, files:[], pendingEdits:{}}
      - const files = scanRepo.listIncompleteFiles(scan.id)
      - const editsMap = taggerRepo.listEditsByPaths(files.map(f => f.path))
      - const pendingEdits: Record<string, PendingTagEdit> = {}; for ([k,v] of editsMap) pendingEdits[k] = v
      - return {scanId: scan.id, files, pendingEdits}

    - `ipcMain.handle(IpcChannels.TaggerSaveEdit, async (_e, payload) => { const root = settingsRepo.get(ROOT_FOLDER_KEY); if (!root) throw new Error('rootFolder not set'); const input = assertSaveEditInput(payload, root); taggerRepo.upsertEdit({ filePath: input.filePath, genre: input.genre ?? null, bpm: input.bpm ?? null, key: input.key ?? null, artist: input.artist ?? null, title: input.title ?? null, comment: input.comment ?? null, rating: input.rating ?? null }, now()) })`

    - `ipcMain.handle(IpcChannels.TaggerDeleteEdit, async (_e, filePath) => { const root = settingsRepo.get(ROOT_FOLDER_KEY); if (!root) throw new Error('rootFolder not set'); if (typeof filePath !== 'string') throw new TypeError('tagger:delete-edit: filePath must be a string'); if (!resolvesUnderRoot(filePath, root)) throw new Error('tagger:delete-edit: file not under rootFolder'); taggerRepo.deleteEdit(filePath) })`

    - `ipcMain.handle(IpcChannels.TaggerGetSession, async () => taggerRepo.getSession())`

    - `ipcMain.handle(IpcChannels.TaggerSetSession, async (_e, payload) => { assertObject(payload, 'tagger:set-session'); const root = settingsRepo.get(ROOT_FOLDER_KEY); if (!root) throw new Error('rootFolder not set'); const cfp = (payload as any).currentFilePath; if (cfp !== null) { if (typeof cfp !== 'string') throw new TypeError('tagger:set-session: currentFilePath must be string or null'); if (!resolvesUnderRoot(cfp, root)) throw new Error('tagger:set-session: file not under rootFolder') } const scanId = (payload as any).scanId; if (scanId !== null && typeof scanId !== 'string') throw new TypeError('tagger:set-session: scanId must be string or null'); taggerRepo.setSession({rootFolder: root, currentFilePath: cfp, scanId}, now()) })`

    - `ipcMain.handle(IpcChannels.TaggerGetGenrePresets, async () => { const top = taggerRepo.topGenres(9); const libraryNames = top.map(r => r.genre); const presets = mergeGenrePresets(libraryNames, HARDCODED_GENRE_FALLBACK); let source: 'library' | 'defaults' | 'mixed'; if (libraryNames.length === 0) source = 'defaults'; else if (libraryNames.length >= 9) source = 'library'; else source = 'mixed'; return { source, presets } })`

    Create src/main/ipc/tagger.test.ts. Build a fake ipcMain `{ handle: vi.fn() }`, fake repos with vi.fn() methods. Extract each handler from the `handle.mock.calls` and invoke directly. Cover all 19 behaviors. Use a fake now=() => 1000 for deterministic timestamp assertions.
  </action>
  <verify>
    <automated>npm run test -- --run src/main/ipc/tagger</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/main/ipc/tagger` passes all 19 behaviors
    - grep src/main/ipc/tagger.ts: NO `from 'electron'` runtime import (only `import type` for IpcMain/IpcMainInvokeEvent — lazy require pattern for vitest purity, per carry-forward)
    - Every handler that accepts a filePath checks resolvesUnderRoot AND (for save-edit) AUDIO_EXTS (T-4-01 + T-4-02 mirror)
    - save-edit rejects rating=6 (T-4-04) AND bpm=500 (T-4-05) AND genre longer than 500 chars (T-4-03)
    - get-genre-presets returns exactly 9 presets for all three source branches
  </acceptance_criteria>
  <done>Six IPC handlers wired with V5 validation; T-4-01..05 mitigated at the IPC layer; T-4-06 (SQL injection) mitigated by prepared statements in taggerRepo.</done>
</task>

<task type="auto">
  <name>Task 7: Wire main/index.ts — registerSchemesAsPrivileged + autoplayPolicy + registerAudioProtocol + registerTaggerHandlers</name>
  <files>src/main/index.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Pitfall 2 — registerSchemesAsPrivileged BEFORE app.whenReady; Pitfall 5 — autoplayPolicy in webPreferences)
    - src/main/index.ts (current bootstrap — already wires dialog, settings, scan, conversion handlers + windows)
    - src/main/tagger/audioProtocol.ts (from Task 5 — AUDIO_PROTOCOL_SCHEME + registerAudioProtocol)
    - src/main/ipc/tagger.ts (from Task 6 — registerTaggerHandlers)
    - src/main/db/connection.ts (getTaggerRepo from Task 3; getSettingsRepo, getScanRepo)
  </read_first>
  <action>
    Edit src/main/index.ts at the top, BEFORE the `app.whenReady()` call (order matters per Pitfall 2):

    1. Add `import { protocol } from 'electron'` (or extend existing electron import).
    2. Add `import { AUDIO_PROTOCOL_SCHEME, registerAudioProtocol } from './tagger/audioProtocol'`
    3. Add `import { registerTaggerHandlers } from './ipc/tagger'`
    4. Add `import { getTaggerRepo } from './db/connection'` (extend existing connection.ts import — getTaggerRepo + existing getSettingsRepo, getScanRepo, getConversionRepo)
    5. At module top-level (before `let mainWindow = null`), call:
       ```ts
       protocol.registerSchemesAsPrivileged([
         {
           scheme: AUDIO_PROTOCOL_SCHEME,
           privileges: {
             stream: true,
             supportFetchAPI: true,
             secure: true,
             standard: true,
             bypassCSP: false
           }
         }
       ])
       ```

    In `createWindow()`, extend the `webPreferences` object to include `autoplayPolicy: 'no-user-gesture-required'` (preserve all existing keys: preload, contextIsolation, nodeIntegration, sandbox).

    Inside `app.whenReady().then(...)`, AFTER `registerSettingsHandlers()` and AFTER `getSettingsRepo()` is callable:
    - `registerAudioProtocol(getSettingsRepo())`
    - `registerTaggerHandlers({ ipcMain, taggerRepo: getTaggerRepo(), scanRepo: getScanRepo(), settingsRepo: getSettingsRepo() })`

    Add a one-line comment above registerSchemesAsPrivileged: `// Pitfall 2: must run before app.whenReady so <audio> treats cratekeeper:// as a streaming origin`.
    Add a one-line comment above autoplayPolicy: `// Pitfall 5: Tagger preview auto-plays on card mount (no user gesture required)`.

    DO NOT touch the existing CSP `<meta>` tag in renderer/index.html in this task (Plan 04-02 owns CSP `media-src 'self' cratekeeper:;` — if the renderer already has a strict CSP that blocks media-src, Plan 04-02 will extend it then).

    Run `npm run build` and confirm the bundle still produces `out/main/index.js`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -q "registerSchemesAsPrivileged" out/main/index.js &amp;&amp; grep -q "autoplayPolicy" out/main/index.js &amp;&amp; grep -q "registerAudioProtocol" out/main/index.js &amp;&amp; grep -q "registerTaggerHandlers" out/main/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Bundled out/main/index.js contains references to registerSchemesAsPrivileged, autoplayPolicy, registerAudioProtocol, registerTaggerHandlers
    - registerSchemesAsPrivileged is called BEFORE app.whenReady (verify by reading src/main/index.ts source — call appears in module top-level scope, not inside the whenReady callback)
    - webPreferences in createWindow includes autoplayPolicy:'no-user-gesture-required' alongside existing sandbox:true + contextIsolation:true (no security regression)
    - Existing Phase 1/2/3 wiring (dialog, settings, scan, conversion handlers) untouched
  </acceptance_criteria>
  <done>Custom protocol scheme privileged before app.whenReady (Pitfall 2 fixed); autoplay policy unlocked (Pitfall 5 fixed); tagger IPC handlers registered; audio protocol handler live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Main-process Tagger backbone end-to-end: six typed IPC channels (`tagger:load-queue`, `tagger:save-edit`, `tagger:delete-edit`, `tagger:get-session`, `tagger:set-session`, `tagger:get-genre-presets`) with V5 validation + folder-allowlist + AUDIO_EXTS gate; `pending_tag_edits` + `tagger_session` SQLite tables behind taggerRepo + getTaggerRepo lazy singleton; scanRepo extended with findLatestScan + listIncompleteFiles; pure mergeGenrePresets helper with locked 9-slot hardcoded fallback (House, Techno, Afro House, Melodic, Disco, Hip-Hop, Funk, Deep, Electronica); `cratekeeper://audio/<encoded>` custom protocol registered via protocol.handle + registerSchemesAsPrivileged with folder + extension allowlist (T-4-01 + T-4-02 mitigated); autoplayPolicy unlocked in BrowserWindow; SETTINGS_KEY_ALLOWLIST extended with `tagger.muteEnabled` (T-4-07 mitigated); preload exposes `window.crateKeeper.tagger.*`.</what-built>
  <how-to-verify>
    DEFERRED to end-of-phase. Manual verification of this plan's surface is rolled into the Plan 04-03 final checkpoint (per workflow.human_verify_mode=end-of-phase guidance). Automated coverage is sufficient for now:

    1. `npm run build` exits 0; `out/main/index.js` exists with the four new symbols (verify grep above).
    2. `npm test -- --run` passes — all Phase 1/2/3 tests still green PLUS the new ones from Tasks 2-6 (scanRepo extensions, taggerRepo, queueBuilder, audioProtocol, ipc/tagger).
    3. `npm run dev`: app launches without console errors. Open DevTools and run `await window.crateKeeper.tagger.loadQueue()` → returns `{scanId: null|string, files: [...], pendingEdits: {}}` based on whether a Phase 2 scan exists. Run `await window.crateKeeper.tagger.getGenrePresets()` → returns `{source: 'library'|'mixed'|'defaults', presets: [9 strings]}`.
    4. In DevTools, attempt `await window.crateKeeper.tagger.saveEdit({filePath: '/etc/passwd', rating: 5})` → must reject with an error containing 'not under rootFolder' (T-4-01 mirror at IPC layer).
    5. In DevTools, attempt `fetch('cratekeeper://audio/' + encodeURIComponent('/etc/passwd'))` → response status 403 (T-4-01 mitigation at protocol layer).
    6. In DevTools, attempt `fetch('cratekeeper://audio/' + encodeURIComponent('<some valid file under rootFolder>.txt'))` → response status 415 (T-4-02).
  </how-to-verify>
  <resume-signal>Type "approved" once `npm run build` + `npm test -- --run` are green AND DevTools spot-checks 3-6 behave as described. Otherwise describe which step failed (most likely: registerSchemesAsPrivileged ran inside whenReady instead of module top-level → revisit Task 7).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (tagger:*) | Renderer is sandboxed; every IPC handler must V5-validate + folder-gate + extension-gate before touching the DB or filesystem |
| renderer → cratekeeper:// protocol | `<audio>` element requests files via the custom scheme; the protocol handler is the SECOND chokepoint after the IPC layer for path-traversal |
| main → better-sqlite3 | Parameterised statements only; no template-literal SQL with embedded values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Information Disclosure / Tampering | cratekeeper:// custom protocol handler | mitigate | path.resolve + startsWith(rootFolder + path.sep); 403 on miss (Task 5). IPC handlers re-check via resolvesUnderRoot (Task 6) |
| T-4-02 | Tampering / DoS | cratekeeper:// custom protocol handler | mitigate | path.extname().toLowerCase() in AUDIO_EXTS allowlist; 415 on miss (Task 5). save-edit handler mirrors (Task 6) |
| T-4-03 | Tampering (oversized strings) | tagger:save-edit handler | mitigate | MAX_TEXT_LEN=500 for genre/artist/title/comment; MAX_KEY_LEN=16; rejected via TypeError (Task 6) |
| T-4-04 | Tampering (out-of-range rating) | tagger:save-edit handler + DB schema | mitigate | ALLOWED_RATINGS Set in handler (Task 6); DB CHECK(rating IS NULL OR 1 ≤ rating ≤ 5) (Task 3). Double gate. |
| T-4-05 | Tampering (negative/huge bpm) | tagger:save-edit handler | mitigate | Number.isInteger && 1 ≤ bpm ≤ 399 in handler (Task 6) |
| T-4-06 | Tampering (SQL injection) | taggerRepo | mitigate | Prepared statements only (Task 3); grep gate in acceptance criteria |
| T-4-07 | Tampering (settings key pollution) | settings:set channel | mitigate | SETTINGS_KEY_ALLOWLIST extended with 'tagger.muteEnabled' (Task 1); existing Phase 3 allowlist gate enforces |
| T-4-08 | DoS (audio playback) | `<audio>` element | accept | Default browser behavior is lazy-load + bounded buffer; preview is 30s loop so total memory bounded (CONTEXT.md decision). No mitigation needed in Plan 04-01. |
| T-4-SC | Tampering | npm installs | mitigate | RESEARCH "Package Legitimacy Audit" confirms zero new dependencies in Phase 4; no install task needed |
</threat_model>

<verification>
- `npm run build` green
- `npm test -- --run` green; existing Phase 1/2/3 tests untouched
- grep src/shared/ipc-types.ts: 6 new `tagger:*` channel constants + `tagger.muteEnabled` in SETTINGS_KEY_ALLOWLIST
- grep src/main/tagger/taggerRepo.ts: no template-literal SQL with `${`
- grep src/main/tagger/audioProtocol.ts: contains `path.resolve`, `startsWith`, `AUDIO_EXTS`, `net.fetch`
- grep src/main/index.ts: `registerSchemesAsPrivileged` appears OUTSIDE the `app.whenReady` callback (module top-level)
- DevTools spot-checks documented in the checkpoint resume signal
</verification>

<success_criteria>
- TAGG-01 substrate: scanRepo.findLatestScan + listIncompleteFiles + tagger:load-queue ship the queue source per LOCKED decision (latest 'done' scan of rootFolder, filtered to incomplete-tag rows, path ASC).
- TAGG-02 substrate: cratekeeper:// custom protocol streams audio files under rootFolder + AUDIO_EXTS gate; autoplay policy unlocked. (UI in Plan 04-02.)
- TAGG-04 substrate: tagger:get-genre-presets returns 9 presets via library top-9 + hardcoded fallback merge (LOCKED order). (UI in Plan 04-02.)
- TAGG-05/06/07 substrate: pending_tag_edits schema accepts integer bpm, free-text key, integer rating 1-5, text artist/title/genre/comment. (UI in Plan 04-02.)
- TAGG-10 substrate: tagger_session single-row table + getSession/setSession with file-path identity. (Resume orchestration in Plan 04-03.)
- T-4-01..07 mitigated at handler + protocol + DB layers.
- LOCKED carry-forwards honoured: sandboxed preload, folder allowlist on every IPC, IpcChannels SoT, repo factory + lazy singleton, lazy require('electron'), no mutation, French copy (no user-facing strings in this plan), ID3v2.3 lock awareness (no audio file writes in this phase).
</success_criteria>

<output>
Create `.planning/phases/04-tagger-core/04-01-SUMMARY.md` when done.
</output>
