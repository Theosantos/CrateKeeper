---
phase: 04-tagger-core
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/renderer/src/store/useTaggerStore.ts
  - src/renderer/src/store/useTaggerStore.test.ts
  - src/renderer/src/views/TaggerView.tsx
  - src/renderer/src/views/TaggerView.test.tsx
  - src/renderer/src/components/tagger/TaggerCard.tsx
  - src/renderer/src/views/__tests__/TaggerEndToEnd.test.tsx
autonomous: false
requirements: [TAGG-09, TAGG-10]
user_setup: []

must_haves:
  truths:
    - "useTaggerStore.lastAction holds {type:'keep'|'skip', filePath, prior?: PendingTagEdit | null} | null; null at boot and after each undo consumption (no double-undo)"
    - "store.keep snapshots the prior pending row (or null) BEFORE upsert, then stores it in lastAction.prior"
    - "store.undo when lastAction.type='keep': calls window.crateKeeper.tagger.saveEdit OR tagger.deleteEdit to restore the prior state; rewinds currentIndex by 1; clears lastAction"
    - "store.undo when lastAction.type='skip': only rewinds currentIndex by 1; clears lastAction"
    - "store.undo when lastAction === null is a no-op (returns null)"
    - "Undo button is disabled (aria-disabled='true' + disabled attribute) when lastAction === null"
    - "Cmd+Z / Ctrl+Z keyboard route invokes the same undo action"
    - "On Keep/Skip, TaggerView triggers a debounced tagger:set-session call (500ms trailing) with the NEW currentFilePath after advance"
    - "window.addEventListener('beforeunload', flushSession) flushes the pending debounced setSession synchronously (Pitfall 1)"
    - "On mount, TaggerView calls tagger.getSession() AFTER tagger.loadQueue() resolves; if session.currentFilePath is found in the queue, currentIndex jumps to its position; else currentIndex stays at 0 (library-change resilience)"
    - "Session restore is identity-by-path — robust to library additions/removals between sessions"
    - "End-to-end RTL test exercises a full Tagger run (load → edit → keep → skip → undo-keep → undo-skip → mount-time resume) without hitting the real DB or filesystem"
  artifacts:
    - path: "src/renderer/src/store/useTaggerStore.ts"
      provides: "Extended with lastAction state; undo action; setSessionDebounced action; restoreSession action; keep/skip extended to snapshot prior + push lastAction"
      contains: "lastAction"
    - path: "src/renderer/src/views/TaggerView.tsx"
      provides: "Extended with beforeunload flush + mount-time getSession restore + Cmd-Z keyboard routing to store.undo + undo-direction slide (reverse of last action)"
      contains: "beforeunload"
  key_links:
    - from: "src/renderer/src/views/TaggerView.tsx"
      to: "src/renderer/src/store/useTaggerStore.ts"
      via: "useTaggerStore(s => s.undo) wired into useTaggerKeyboard.onUndo + Annuler button onClick"
      pattern: "useTaggerStore.*undo"
    - from: "src/renderer/src/views/TaggerView.tsx"
      to: "window.crateKeeper.tagger.setSession"
      via: "useDebouncedCallback wrapping tagger:set-session with 500ms delay"
      pattern: "setSession"
    - from: "src/renderer/src/views/TaggerView.tsx"
      to: "window.crateKeeper.tagger.getSession"
      via: "mount-time await after loadQueue completes; jumps currentIndex by file-path lookup"
      pattern: "getSession"
---

<objective>
Layer session resume + undo state machine on top of the Plan 04-02 card UX. Extend `useTaggerStore` with `lastAction` (in-memory, 1-level undo stack) + `undo` action; extend `TaggerView` to (a) debounce `tagger:set-session` at 500ms trailing on every Keep/Skip with a `window.addEventListener('beforeunload', flushNow)` last-write guarantee (Pitfall 1), and (b) restore session on mount by reading `tagger:get-session` AFTER `tagger:load-queue` and jumping `currentIndex` to the matching file path (library-change resilient). Wire Cmd-Z to `store.undo`, enable the Annuler button when `lastAction !== null`. Ship a single end-to-end RTL test exercising load → edit → keep → skip → undo-keep → undo-skip → resume-on-remount in one flow.

Purpose: This is the Tagger's persistence layer (TAGG-10) + the one-level Undo invariant (TAGG-09). After this plan ships, the user can close the app mid-card and reopen exactly where they were; can hit Cmd-Z once to revert a Keep (restoring prior pending state) or a Skip (rewinding the index); and double-undo is prevented (lastAction clears after consumption).

Output: A Tagger v1 with session continuity + 1-level undo; an end-to-end run-through test that proves the integration; a manual end-of-phase checkpoint validating audio playback per format, library-change resilience, keyboard vs input focus, and slide-animation smoothness on real Electron.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tagger-core/04-CONTEXT.md
@.planning/phases/04-tagger-core/04-RESEARCH.md
@.planning/phases/04-tagger-core/04-01-PLAN.md
@.planning/phases/04-tagger-core/04-02-PLAN.md
@src/shared/ipc-types.ts
@src/renderer/src/store/useTaggerStore.ts
@src/renderer/src/views/TaggerView.tsx
@src/renderer/src/hooks/useDebouncedCallback.ts
@src/renderer/src/components/tagger/TaggerCard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useTaggerStore.lastAction + undo action + keep/skip snapshot</name>
  <files>src/renderer/src/store/useTaggerStore.ts, src/renderer/src/store/useTaggerStore.test.ts</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-CONTEXT.md (LOCKED Undo semantics: 1 level, in-memory; undo-after-Keep restores prior pending OR deletes if it was null; undo-after-Skip rewinds index only; lastAction cleared after consumption — no double-undo)
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Anti-pattern: optimistic UI without DB confirmation — undo MUST await the saveEdit/deleteEdit roundtrip before clearing lastAction)
    - src/renderer/src/store/useTaggerStore.ts (from Plan 04-02 Task 2 — keep currently returns the snapshot but lastAction is not yet stored)
  </read_first>
  <behavior>
    - Test: initial state — lastAction === null
    - Test: after keep() succeeds, lastAction === {type:'keep', filePath, prior: <prior PendingTagEdit | null>, savedEdit: <SaveTagEditInput>}; new instance per call
    - Test: after skip() succeeds, lastAction === {type:'skip', filePath}
    - Test: undo() with lastAction === null returns null and is a no-op (state unchanged)
    - Test: undo() with lastAction.type='skip' rewinds currentIndex by 1; clears lastAction; does NOT call any IPC
    - Test: undo() with lastAction.type='keep' AND prior === null: calls window.crateKeeper.tagger.deleteEdit(filePath); rewinds currentIndex by 1; removes the filePath entry from local pendingEdits; clears lastAction
    - Test: undo() with lastAction.type='keep' AND prior !== null: calls window.crateKeeper.tagger.saveEdit with the prior values; rewinds currentIndex by 1; restores the prior PendingTagEdit in local pendingEdits; clears lastAction
    - Test: double-undo is prevented — calling undo twice in succession only fires once; the second call is a no-op (lastAction was cleared)
    - Test: keep() after undo() clears the prior lastAction and pushes a NEW one (no stacking — locked 1-level)
    - Test: store.lastAction transitions are immutable (each push creates a new object reference)
  </behavior>
  <action>
    Extend src/renderer/src/store/useTaggerStore.ts:

    Add to state shape:
    ```ts
    interface LastAction {
      type: 'keep' | 'skip'
      filePath: string
      prior?: PendingTagEdit | null
      savedEdit?: SaveTagEditInput
    }
    // ...
    lastAction: LastAction | null
    undo(): Promise<void>
    ```

    Initial value: `lastAction: null`.

    Modify the existing `keep` action (Plan 04-02 Task 2) so that AFTER the successful saveEdit + state update, it ALSO sets `lastAction: { type: 'keep', filePath, prior, savedEdit: merged }` in the same `set(...)` call (atomic with the pendingEdits/dirtyEdits/currentIndex update).

    Modify the existing `skip` action so it ALSO sets `lastAction: { type: 'skip', filePath }` in the same set() call.

    Add the `undo` action:

    ```ts
    async undo() {
      const { lastAction, pendingEdits, currentIndex } = get()
      if (lastAction === null) return
      if (lastAction.type === 'skip') {
        set({
          currentIndex: Math.max(0, currentIndex - 1),
          lastAction: null
        })
        return
      }
      // type === 'keep'
      const filePath = lastAction.filePath
      const prior = lastAction.prior ?? null
      if (prior === null) {
        // Undoing a Keep where there was no prior pending row → delete
        await window.crateKeeper.tagger.deleteEdit(filePath)
        const nextPending = new Map(pendingEdits)
        nextPending.delete(filePath)
        set({
          pendingEdits: nextPending,
          currentIndex: Math.max(0, currentIndex - 1),
          lastAction: null
        })
        return
      }
      // Restore prior
      await window.crateKeeper.tagger.saveEdit({
        filePath,
        genre: prior.genre, bpm: prior.bpm, key: prior.key,
        artist: prior.artist, title: prior.title,
        comment: prior.comment, rating: prior.rating
      })
      const nextPending = new Map(pendingEdits)
      nextPending.set(filePath, prior)
      set({
        pendingEdits: nextPending,
        currentIndex: Math.max(0, currentIndex - 1),
        lastAction: null
      })
    }
    ```

    Also extend `reset()` to clear `lastAction: null`.

    Extend useTaggerStore.test.ts with the 10 behaviors above. Reuse the existing window.crateKeeper mock; verify saveEdit / deleteEdit are called the correct number of times across the keep → undo → keep → undo cycle.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/store/useTaggerStore</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/store/useTaggerStore` passes existing Plan 04-02 tests + the 10 new undo behaviors
    - undo() with lastAction === null is a pure no-op (verified by snapshotting state before and after, asserting deep equality)
    - undo() after undo() (double-undo) does not double-call the IPC (assert via mock call count)
    - keep() after undo() resets lastAction to the new {type:'keep',...} (no stacking)
    - lastAction immutability: each transition creates a new object reference (assert via reference inequality)
  </acceptance_criteria>
  <done>1-level in-memory undo state machine locked at the store level; TAGG-09 logic complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TaggerView session resume (mount-time getSession) + debounced setSession + beforeunload flush</name>
  <files>src/renderer/src/views/TaggerView.tsx, src/renderer/src/views/TaggerView.test.tsx</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-CONTEXT.md (LOCKED: identity = file path; library-change resilience falls back to index 0; session updates debounced 500ms)
    - .planning/phases/04-tagger-core/04-RESEARCH.md (Pitfall 1 — beforeunload flush; Open Q 3 — renderer-side flush is primary, no main-side before-quit needed)
    - src/renderer/src/hooks/useDebouncedCallback.ts (from Plan 04-02 Task 1 — has call/flush/cancel)
    - src/renderer/src/views/TaggerView.tsx (from Plan 04-02 Task 5 — extend additively)
    - src/renderer/src/store/useTaggerStore.ts (from Task 1 — undo is now available)
  </read_first>
  <behavior>
    - Test (mount-time restore): on mount, after loadQueue resolves with files including '/Music/c.mp3' AND getSession returns {currentFilePath:'/Music/c.mp3', scanId:'s1'}, currentIndex is set to the index of '/Music/c.mp3' in the queue (e.g. 2 if queue is [a,b,c,d])
    - Test (library-change resilience): if getSession returns currentFilePath='/Music/missing.mp3' which is NOT in the queue, currentIndex stays at 0
    - Test (no session): if getSession returns null, currentIndex stays at 0
    - Test (getSession order): getSession is awaited AFTER loadQueue resolves (verify via mock call order — loadQueue resolves first, then getSession is called)
    - Test (debounce on keep): pressing Keep calls store.keep AND schedules a debounced setSession({currentFilePath: <next file path>, scanId}); after 500ms (fake timers), setSession IPC fires once with the new path
    - Test (debounce on skip): same as above for Skip
    - Test (debounce collapses): rapid Keep×3 in <500ms results in ONE setSession call with the final file path (not 3)
    - Test (beforeunload flush): firing a synthetic 'beforeunload' event on window calls the pending debounce's flush() — the setSession IPC fires synchronously with the latest pending args
    - Test (cleanup): unmounting TaggerView removes the beforeunload listener (assert via spy on removeEventListener)
    - Test (Cmd-Z wiring): pressing Cmd+Z (or Ctrl+Z) calls store.undo
    - Test (undo button enabled state): when lastAction === null, the Annuler button is disabled; when non-null, enabled (and clicking it also calls store.undo)
    - Test (undo slide direction): undo-after-Keep slides RIGHT (reverse of Keep's left); undo-after-Skip slides LEFT (reverse of Skip's right). Implementation note: capture lastAction.type BEFORE calling undo, derive direction from it
    - Test (end-of-queue setSession): when keep/skip advances past the end (currentIndex === queue.length), the debounced setSession is called with currentFilePath = null (clearing) — defensive UX so re-opening lands at the empty/completion state
  </behavior>
  <action>
    Extend src/renderer/src/views/TaggerView.tsx:

    1. Add the undo wiring + Cmd-Z route. Replace the stub `onUndo: () => { /* Plan 04-03 */ }` in useTaggerKeyboard with:
       ```ts
       onUndo: () => { void runUndoWithSlide() }
       ```
       Where `runUndoWithSlide` reads the lastAction.type via the store BEFORE calling undo and reverses the slide direction:
       ```ts
       const runUndoWithSlide = async (): Promise<void> => {
         const la = useTaggerStore.getState().lastAction
         if (la === null) return
         const dir: 'left' | 'right' = la.type === 'keep' ? 'right' : 'left'
         await runWithSlide(dir, useTaggerStore.getState().undo)
       }
       ```

    2. Add session resume on mount. Replace the current mount useEffect with:
       ```ts
       useEffect(() => {
         let cancelled = false
         void (async () => {
           await Promise.all([loadQueue(), loadGenrePresets(), loadMuteSetting()])
           if (cancelled) return
           const session = await window.crateKeeper.tagger.getSession()
           if (cancelled || session === null || session.currentFilePath === null) return
           const queue = useTaggerStore.getState().queue
           const idx = queue.findIndex(f => f.path === session.currentFilePath)
           if (idx >= 0) {
             useTaggerStore.setState({ currentIndex: idx })
           }
         })()
         return () => { cancelled = true }
       }, [loadQueue, loadGenrePresets, loadMuteSetting])
       ```

    3. Add the debounced setSession + beforeunload flush:
       ```ts
       const sessionDebounced = useDebouncedCallback(
         async (currentFilePath: string | null) => {
           const scanId = /* derive from queue source — store does not currently track scanId; add it now */
           await window.crateKeeper.tagger.setSession({ currentFilePath, scanId })
         },
         500
       )

       useEffect(() => {
         const onBeforeUnload = (): void => {
           sessionDebounced.flush()
         }
         window.addEventListener('beforeunload', onBeforeUnload)
         return () => window.removeEventListener('beforeunload', onBeforeUnload)
       }, [sessionDebounced])
       ```

       Note: the store does not currently expose `scanId`. Extend `useTaggerStore` (touch Task 2's file again — small additive change): add `scanId: string | null` to state, set it inside `loadQueue` from the result, default null. Add a selector or pass via `useTaggerStore(s => s.scanId)`. Acceptable since this plan already touches useTaggerStore.ts in Task 1.

    4. Schedule the debounced setSession on every Keep / Skip / Undo. After each `runWithSlide(...)` completes (or in the runWithSlide wrapper), call `sessionDebounced.call(<derived current file path>)` where:
       - After keep/skip advances: `useTaggerStore.getState().queue[useTaggerStore.getState().currentIndex]?.path ?? null`
       - After undo rewinds: same expression, computed AFTER the undo completes

    5. Update the Annuler button (TaggerCard footer) to read `lastAction` from store + disable when null. Pass an `onUndo` prop from TaggerView (lifted) into TaggerCard, or have TaggerCard read directly from store. Planner discretion — prefer the store-read pattern (matches Keep/Skip).

    Extend TaggerView.test.tsx with the 12 new behaviors. Use `vi.useFakeTimers()` for debounce tests. For beforeunload, dispatch `new Event('beforeunload')` on window after calling Keep, then verify setSession.flush fired.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/views/TaggerView</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/views/TaggerView` passes existing Plan 04-02 tests + the 12 new behaviors
    - getSession is awaited AFTER loadQueue resolves (mock call ordering verified)
    - Library-change resilience: session.currentFilePath not in queue → currentIndex stays 0 (explicit test)
    - Debounced setSession collapses 3 rapid Keep calls to 1 IPC invocation (explicit test with fake timers)
    - beforeunload flush fires the pending setSession synchronously (explicit test)
    - Cmd-Z routes through store.undo (explicit test); slide direction reversed correctly
    - Annuler button disabled state reflects lastAction === null
  </acceptance_criteria>
  <done>Session resume + debounced persistence + Pitfall 1 mitigation locked; Cmd-Z + Annuler button wired end-to-end with reversed slide animation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end Tagger run-through RTL test</name>
  <files>src/renderer/src/views/__tests__/TaggerEndToEnd.test.tsx</files>
  <read_first>
    - .planning/phases/04-tagger-core/04-VALIDATION.md (Wave 0 fixtures + per-requirement verification map)
    - src/renderer/src/views/TaggerView.tsx (from Task 2)
    - src/renderer/src/store/useTaggerStore.ts (from Task 1)
    - src/renderer/src/store/useTaggerStore.test.ts (mock patterns)
  </read_first>
  <behavior>
    Single integration test exercising the full Tagger flow with all IPC mocked:

    1. Mock `window.crateKeeper.tagger.loadQueue` to return 4 files with mixed has_* flags.
    2. Mock `getGenrePresets` to return 9 presets from the hardcoded fallback (deterministic).
    3. Mock `getSetting('tagger.muteEnabled')` to return false.
    4. Mock `getSession` to return null (first launch).
    5. Mock `saveEdit`, `deleteEdit`, `setSession` to resolve.

    Run the flow:
    - Render `<TaggerView />` inside a test wrapper.
    - Wait for the first card to render (queue[0]).
    - Type into the Genre input → assert dirtyEdits updates (via store inspection).
    - Press the '3' key → assert applyPreset called, genre set to presets[2].
    - Click Sauver (Keep) → assert saveEdit called with merged values; assert currentIndex advanced to 1; assert lastAction.type === 'keep'.
    - Press '→' (Skip) → assert currentIndex === 2; assert lastAction.type === 'skip'.
    - Press Cmd+Z → assert currentIndex === 1; assert lastAction === null (single-level undo consumed).
    - Press Cmd+Z again → no-op (lastAction still null); assert no extra saveEdit/deleteEdit call.
    - Advance through the queue (Keep × 3) to reach end. Assert end-of-queue completion message renders.

    Then test resume:
    - Unmount the view.
    - Remock `getSession` to return {currentFilePath: queue[2].path, scanId: 'test-scan'}.
    - Remount `<TaggerView />`. Assert that after loadQueue + getSession resolve, currentIndex === 2.

    Then test library-change resilience:
    - Unmount.
    - Remock `getSession` to return {currentFilePath: '/missing/file.mp3', scanId: 'test-scan'}.
    - Remount. Assert currentIndex === 0 (fallback).

    Then test beforeunload flush:
    - Trigger a Keep.
    - Fire `new Event('beforeunload')` on window.
    - Assert setSession was called synchronously (flushed from the debounce).

    Then test input-focus gate:
    - Focus the Genre input.
    - Press '3'. Assert applyPreset is NOT called (focus gate intercepts).
    - Press ArrowLeft. Assert keep is NOT called.
  </behavior>
  <action>
    Create src/renderer/src/views/__tests__/TaggerEndToEnd.test.tsx using @testing-library/react + vitest:

    - Setup: `vi.useFakeTimers()` for debounce; build a `mockTagger` object holding vi.fn() per method; assign to `globalThis.window.crateKeeper`.
    - Build 4 mock ScannedFile rows with varying tag completeness.
    - Use `render(<TaggerView />)`, `waitFor`, `screen.getByLabelText`, `fireEvent`.
    - For keyboard events: `fireEvent.keyDown(document, { key: '3' })` etc.
    - For Cmd-Z: `fireEvent.keyDown(document, { key: 'z', metaKey: true })`.
    - For input focus: `userEvent.tab()` or `inputEl.focus()` then key event.
    - For end-of-queue: keep × 3 then assert `screen.getByText(/Bibliothèque terminée/)`.
    - For remount: use a wrapper component that conditionally renders TaggerView via state, then toggle false → true to remount; OR call `unmount()` from render result then re-render.
    - For beforeunload: `vi.runAllTimers()` first? No — to test the flush path, do NOT advance timers; instead dispatch the event and assert setSession was called BEFORE the 500ms elapsed.

    The test should be a single `describe('Tagger end-to-end', ...)` block with logical `it(...)` sections, but they should share setup state where it makes the test read more naturally. Reset mocks between `it` blocks via `vi.clearAllMocks()`.

    Test runtime budget: < 3 seconds (use fake timers liberally).
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/views/__tests__/TaggerEndToEnd</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/views/__tests__/TaggerEndToEnd` passes
    - All 9 behavior sub-cases (keep, skip, undo, double-undo no-op, end-of-queue, remount-with-session, library-change resilience, beforeunload flush, input-focus gate) verified
    - Test runtime < 3 seconds (no real timers waited)
    - The test exercises the REAL useTaggerStore + REAL TaggerView wiring; only window.crateKeeper.tagger.* is mocked
  </acceptance_criteria>
  <done>End-to-end integration test proves Plan 04-01 + 04-02 + 04-03 compose correctly under all flows; covers TAGG-01..10 at the integration level.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tagger Phase 4 end-to-end:
- Plan 04-01: main-process backbone (6 IPC handlers, taggerRepo + tagger_session schemas, scanRepo extensions, cratekeeper:// custom protocol with folder + extension allowlist, autoplay policy, settings allowlist extension).
- Plan 04-02: renderer card UX (useTaggerStore + TaggerCard composite + AudioPreview with 30s loop + AIFF fallback + GenrePresetBar + RatingStars + ArtistTitleSplit + document-level keyboard with input-focus gate + transform-only slide animation + CSP media-src extension).
- Plan 04-03: session resume (mount-time getSession + library-change resilience + debounced setSession + beforeunload flush) + one-level undo (Cmd-Z + Annuler button + reversed slide direction).

User-facing capability shipped: open the Tagger view; see one card at a time for files with incomplete tags; hear a 30s auto-play preview from the project's bundled `cratekeeper://` protocol (with French "Aperçu indisponible pour ce format" fallback for AIFF); edit every Rekordbox-relevant tag inline; apply a genre with the 1-9 keys; accept an Artist/Title split suggestion with one click; rate 1-5 stars; press Sauver (←) to save and advance, Passer (→) to skip and advance, or Cmd-Z to undo the last action; close the app and reopen exactly on the same card. Phase 5 will eventually write the pending edits into the actual audio files; Phase 4 ships the editor + intent store.</what-built>
  <how-to-verify>
    1. **Build + tests.** `npm run build` exits 0. `npm test -- --run` exits 0; all Phase 1-3 + Plan 04-01/02/03 tests pass (~300+ tests total).

    2. **Empty state.** Launch `npm run dev` on a fresh DB (or a DB whose rootFolder has no completed scan). Open Tagger. Confirm the French CTA: "Lance un scan dans l'Analyser pour démarrer."

    3. **Happy path with a scanned library.** Switch to Analyser, scan a folder containing at least 5-10 audio files with at least one missing tag each (genre, BPM, or key). Switch back to Tagger.
       - The first incomplete file's card is visible.
       - The 30s preview auto-plays. Tweak the mute toggle — confirm it persists across a `Cmd+Q` + relaunch (T-4-07 + persistence proof).
       - Confirm the read-only chips show "Genre détecté" / "BPM détecté" / "Key détectée" only for the flags that are true on that file.
       - The 9-button genre bar shows your library's top-9 genres (or the hardcoded fallback if your library is undertaaged).

    4. **Per-format playback smoke test.** Point rootFolder at a folder containing one file per supported format: `.mp3`, `.flac`, `.m4a`, `.aac`, `.wav`, `.ogg`, `.opus`, `.aiff`. For each:
       - MP3 / FLAC / M4A / AAC / WAV / OGG / Opus: preview plays. The 30s loop is visible (after 30s, the playhead resets to 0:00).
       - AIFF: the card shows "Aperçu indisponible pour ce format" (Pitfall 6 fallback). All other editing fields still work.

    5. **Keyboard shortcuts + input focus gate.**
       - Press `←` — Keep fires; slide-left animation runs; card advances.
       - Press `→` — Skip fires; slide-right; advances.
       - Press `1`-`9` — genre changes to the corresponding preset.
       - Focus the BPM input. Type "120". Confirm the genre presets do NOT swap on the `1` and `2` keystrokes (T-4-input-focus gate). The BPM value lands as 120.
       - Press `Space` to play/pause; press `M` to toggle mute (also persists).

    6. **Slide animation smoothness.** Hold a tone in your head and click Sauver three times in rapid succession. Audio doesn't stutter mid-slide (Pitfall 5 — slide runs on the compositor, audio on the media thread).

    7. **One-level Undo (TAGG-09).**
       - Make an edit (genre + BPM). Click Sauver. New card appears.
       - Press `Cmd+Z`. Previous card reappears with the prior pending state restored (or empty if it was the first edit on that file). Slide-right animation plays (reverse of Keep's left).
       - Press `Cmd+Z` again. No-op (no double-undo). Annuler button is now disabled.
       - Click Passer (Skip). Press `Cmd+Z`. Card index rewinds; slide-left plays (reverse of Skip's right). Annuler disables.

    8. **Session resume (TAGG-10).**
       - Tag 3 cards. While the 4th card is on screen, `Cmd+Q` (Mac) or close the window (Win) to quit.
       - Relaunch the app. Open Tagger. Confirm the 4th card is what's displayed (NOT card 1).
       - Add a new audio file to the rootFolder. Re-scan via Analyser. Open Tagger again. Confirm session-resume gracefully lands at index 0 if the previous card is no longer in the new (filtered) queue (library-change resilience). If the previous card is still in the queue, it lands on it.

    9. **Security spot-checks (DevTools console).**
       - `await window.crateKeeper.tagger.saveEdit({filePath: '/etc/passwd', rating: 5})` → rejects with "not under rootFolder" (T-4-01).
       - `await window.crateKeeper.tagger.saveEdit({filePath: '<a real file under rootFolder>', rating: 6})` → rejects with rating-range error (T-4-04).
       - `await window.crateKeeper.tagger.saveEdit({filePath: '<a real file>', bpm: -1})` → rejects (T-4-05).
       - `fetch('cratekeeper://audio/' + encodeURIComponent('/etc/passwd'))` → status 403 (T-4-01 at protocol layer).
       - `fetch('cratekeeper://audio/' + encodeURIComponent('<real file>.txt'))` → status 415 (T-4-02).
       - In Devtools Application → IndexedDB / SQL inspection: confirm pending_tag_edits.rating values are integers 1-5 only.

    10. **No regression on Phase 1/2/3.** Switch to Analyser — scan + virtualised file table + selection + Export CSV still work. Switch to Convertir — preset selector + progress bars + resume banner still work.
  </how-to-verify>
  <resume-signal>Type "approved" if steps 1-10 all pass. Otherwise describe which step failed.

Most likely failure modes:
- Audio doesn't play on a valid format → check renderer/index.html CSP includes `media-src 'self' cratekeeper:;` AND check src/main/index.ts registers schemes-as-privileged BEFORE app.whenReady AND has autoplayPolicy in webPreferences (Pitfalls 2 + 5).
- Session resume lands on wrong card → check the order: loadQueue MUST resolve before getSession is awaited; library-change resilience must fall back to index 0.
- Cmd-Z double-undoes → confirm lastAction is cleared in store.undo's final set() call (Task 1).
- Genre preset 1 fires while typing in BPM → input focus gate broken; check useTaggerKeyboard's isTextInput predicate (Plan 04-02 Task 3).
- AIFF playback fails silently instead of showing fallback → check AudioPreview.tsx ext check is case-insensitive.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → window.crateKeeper.tagger.setSession | Carry-forward — handled by Plan 04-01's V5 + folder-allowlist on tagger:set-session |
| renderer beforeunload → main IPC | Synchronous flush relies on ipcRenderer.invoke dispatching the message before the renderer is torn down. Empirically reliable for one small call (Pitfall 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-09 | Data loss (session drift on quit) | Debounced setSession + beforeunload flush | mitigate | useDebouncedCallback exposes flush(); window.addEventListener('beforeunload', flush) (Task 2). Renderer-side flush is primary per RESEARCH Open Q 3 — no main-side before-quit handler needed. |
| T-4-10 | Stale session pointing at deleted file | mount-time getSession + library-change resilience | mitigate | Identity-by-path findIndex; falls back to currentIndex=0 when the path is not in the new queue (Task 2). |
| T-4-11 | Double-undo causes data corruption | store.undo state machine | mitigate | lastAction set to null in the same set() call that consumes it; subsequent undo() returns immediately (Task 1). Verified by the end-to-end test. |
| T-4-01..08 (carry-forward) | various | various | mitigate | Plan 04-01 + 04-02 mitigations remain in force; this plan adds no new IPC surface. |
</threat_model>

<verification>
- `npm run build` green
- `npm test -- --run` green across Phase 1-3 + Plan 04-01/02/03
- End-to-end RTL test exercises load → edit → keep → skip → undo×2 → resume cycle in one test
- Manual checkpoint validates per-format playback (8 formats), library-change resume, input-focus gate, slide-animation smoothness on real Electron
- Security spot-checks (steps 9 of the checkpoint) confirm T-4-01/02/04/05 mitigations are observable in DevTools
</verification>

<success_criteria>
- TAGG-09 (Undo 1 niveau): lastAction in-memory state machine restores prior pending edit on Keep-undo, rewinds index on Skip-undo, clears after consumption (no double-undo). Cmd-Z + Annuler button wired.
- TAGG-10 (Reprise de session): tagger_session single-row updated on every Keep/Skip via 500ms debounce + beforeunload synchronous flush. Mount-time getSession + library-change resilient findIndex restoration.
- LOCKED carry-forwards: French copy; immutable Zustand updates; transform-only slide animation reversed on undo; no new --color-* tokens; sandboxed preload; folder allowlist already enforced upstream.
- T-4-09/10/11 mitigated; all earlier T-4-XX from Plans 04-01 + 04-02 remain in force.
- End-of-phase manual checkpoint exercises the visual + auditory dimensions that no automated test can: per-format audio playback, slide-animation smoothness during audio playback, real session-quit-relaunch cycle.
</success_criteria>

<output>
Create `.planning/phases/04-tagger-core/04-03-SUMMARY.md` when done.
</output>
