---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/store/useAppStore.ts
  - src/renderer/src/store/useAppStore.test.ts
  - src/renderer/src/components/nav/NavBar.tsx
  - src/renderer/src/components/folder/RootFolderPicker.tsx
  - src/renderer/src/views/AnalyserView.tsx
  - src/renderer/src/views/ConvertirView.tsx
  - src/renderer/src/views/TaggerView.tsx
  - src/renderer/src/App.test.tsx
  - src/renderer/src/assets/main.css
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-03]
user_setup: []

must_haves:
  truths:
    - "On launch the app shows a navigation interface with three distinct tool areas: Analyser, Convertir, Tagger"
    - "Clicking a nav item switches the visible view to that tool"
    - "Clicking the folder-picker control opens the native OS directory dialog and shows the chosen path"
    - "The selected folder is displayed after restart without re-selecting (hydrated on mount)"
  artifacts:
    - path: "src/renderer/src/store/useAppStore.ts"
      provides: "Zustand store: activeTool, rootFolder, setActiveTool, loadRootFolder, pickRootFolder"
      contains: "activeTool"
    - path: "src/renderer/src/components/nav/NavBar.tsx"
      provides: "Three-tool navigation"
      contains: "Tagger"
    - path: "src/renderer/src/components/folder/RootFolderPicker.tsx"
      provides: "Folder-pick button + selected path display"
      contains: "pickRootFolder"
    - path: "src/renderer/src/App.tsx"
      provides: "Shell: nav + active view + mount-time folder hydration"
      contains: "loadRootFolder"
  key_links:
    - from: "src/renderer/src/store/useAppStore.ts"
      to: "window.djUtils"
      via: "pickFolder / getRootFolder / setRootFolder"
      pattern: "window\\.djUtils\\.(pickFolder|getRootFolder|setRootFolder)"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/store/useAppStore.ts"
      via: "useEffect → loadRootFolder on mount"
      pattern: "loadRootFolder"
    - from: "src/renderer/src/components/nav/NavBar.tsx"
      to: "src/renderer/src/store/useAppStore.ts"
      via: "setActiveTool"
      pattern: "setActiveTool"
---

<objective>
Build the renderer vertical slice that makes the Phase 1 user story real: a three-tool navigation shell (Analyser / Convertir / Tagger), a Zustand store for active tool + root folder, a RootFolderPicker that triggers the native dialog over window.djUtils, and mount-time hydration so the previously selected folder shows after restart. This is the thinnest UI that exercises the full stack built in Plan 01.

Purpose: Completes the walking skeleton — after this plan a real user can launch the app, see and switch between three tools, pick a folder, and have that choice persist across sessions (the three ROADMAP success criteria).
Output: A runnable app satisfying FOUND-01, FOUND-02, FOUND-03 end-to-end through the secure IPC bridge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/SKELETON.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@src/shared/ipc-types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zustand app store (activeTool + rootFolder) with FOUND-01 nav transition test</name>
  <files>src/renderer/src/store/useAppStore.ts, src/renderer/src/store/useAppStore.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("App boot → hydrate persisted folder" code example, Architectural Responsibility Map)
    - .planning/phases/01-foundation/01-VALIDATION.md (task 1-01-05 row, useAppStore.test.ts Wave 0 gap)
    - src/shared/ipc-types.ts (DjUtilsApi shape consumed via window.djUtils)
    - .planning/phases/01-foundation/01-01-SUMMARY.md (confirm bridge channel names + API)
  </read_first>
  <behavior>
    - Test: initial activeTool is 'analyser'
    - Test: setActiveTool('tagger') transitions activeTool to 'tagger' (covers FOUND-01 nav switching)
    - Test: loadRootFolder() sets rootFolder from a mocked window.djUtils.getRootFolder
    - Test: pickRootFolder() with mocked pickFolder returning a path calls setRootFolder and updates rootFolder; returning null leaves rootFolder unchanged
  </behavior>
  <action>
    Create src/renderer/src/store/useAppStore.ts with zustand 5 `create`. Type Tool = 'analyser' | 'convertir' | 'tagger'. State: activeTool (default 'analyser'), rootFolder (default null), setActiveTool(t), loadRootFolder() → set rootFolder from window.djUtils.getRootFolder(), pickRootFolder() → const p = await window.djUtils.pickFolder(); if (p) { await window.djUtils.setRootFolder(p); set rootFolder } (RESEARCH App-boot example).
    Create useAppStore.test.ts (jsdom project) stubbing globalThis.window.djUtils with vi.fn() mocks to cover the four behaviors. Reset store state between tests.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/store</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/renderer/src/store` passes all four behaviors
    - store reads only window.djUtils.* (no direct Node/electron import in renderer — RESEARCH anti-pattern)
    - default activeTool is 'analyser'; setActiveTool transitions correctly (FOUND-01)
  </acceptance_criteria>
  <done>Zustand store drives nav + folder state via the typed bridge; tests green in jsdom.</done>
</task>

<task type="auto">
  <name>Task 2: Nav shell, three tool views, RootFolderPicker, mount hydration</name>
  <files>src/renderer/src/App.tsx, src/renderer/src/components/nav/NavBar.tsx, src/renderer/src/components/folder/RootFolderPicker.tsx, src/renderer/src/views/AnalyserView.tsx, src/renderer/src/views/ConvertirView.tsx, src/renderer/src/views/TaggerView.tsx, src/renderer/src/assets/main.css</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (System Architecture Diagram, Recommended Project Structure renderer tree)
    - src/renderer/src/store/useAppStore.ts (from Task 1)
    - src/renderer/src/App.tsx (scaffold default — to replace)
    - /Users/theo/.claude/rules/ecc/web/design-quality.md (anti-template policy — pick an intentional visual direction)
  </read_first>
  <action>
    Create NavBar.tsx: three nav items (Analyser, Convertir, Tagger) reading activeTool and calling setActiveTool from useAppStore; mark the active one with an aria-current and a distinct designed active state (not a default flat link list — apply hierarchy + an intentional active treatment per web/design-quality.md). Use semantic &lt;nav aria-label&gt; + &lt;header&gt;.
    Create the three view components (AnalyserView, ConvertirView, TaggerView) as labelled placeholders (each an &lt;section aria-labelledby&gt; with a heading naming the tool) — real content arrives in Phases 2/3/4.
    Create RootFolderPicker.tsx: a "Choisir un dossier" button calling store.pickRootFolder, and a display of the current rootFolder (or an empty-state hint when null). Use the store, never window.djUtils directly in the component.
    Rewrite App.tsx: useEffect on mount calls loadRootFolder() (FOUND-03 hydration), renders &lt;header&gt; with NavBar + RootFolderPicker, and renders the view matching activeTool (state-based switch, NOT React Router per RESEARCH). Use &lt;main&gt; for the active view.
    Add intentional styling to main.css using CSS custom properties (palette/spacing/type tokens, kebab-case classes) per the web coding-style and design-quality rules — avoid a generic default-template look. Define design tokens in :root.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - NavBar renders exactly three items: Analyser, Convertir, Tagger
    - App.tsx calls loadRootFolder() in a mount effect and switches views from activeTool with no react-router import
    - RootFolderPicker and NavBar consume useAppStore (grep: no window.djUtils reference inside component files)
    - main.css defines :root design tokens (custom properties) — no hardcoded repeated palette values
  </acceptance_criteria>
  <done>Three-tool nav shell renders, view switching works, folder picker wired, folder hydrated on mount; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: App render test — three tools visible + nav switching (FOUND-01)</name>
  <files>src/renderer/src/App.test.tsx</files>
  <read_first>
    - src/renderer/src/App.tsx (from Task 2)
    - src/renderer/src/components/nav/NavBar.tsx (from Task 2)
    - .planning/phases/01-foundation/01-VALIDATION.md (task 1-01-05 row, FOUND-01 unit test)
    - vitest.setup.ts (jest-dom matchers)
  </read_first>
  <behavior>
    - Test: rendering App shows the three tool labels Analyser, Convertir, Tagger (FOUND-01)
    - Test: the default visible view is the Analyser section
    - Test: clicking the "Tagger" nav item makes the Tagger section the visible view
  </behavior>
  <action>
    Create App.test.tsx (jsdom) with @testing-library/react. Stub globalThis.window.djUtils (getRootFolder → null, pickFolder/setRootFolder → vi.fn()) so the mount effect resolves. Render &lt;App /&gt;, assert the three nav labels are present, assert the Analyser section is shown by default, fire a click on the Tagger nav item and assert the Tagger section becomes visible. Use accessible queries (getByRole/getByText), not test ids.
  </action>
  <verify>
    <automated>npm run test -- --run src/renderer/src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run` passes the three-tools-visible, default-view, and nav-switch tests
    - test uses accessible queries (role/text), window.djUtils mocked
  </acceptance_criteria>
  <done>FOUND-01 is automated: three tools render and nav switching is asserted in jsdom.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 1 walking skeleton: launch → three-tool nav (Analyser/Convertir/Tagger) → native folder picker → folder persists across relaunch, all over the secure window.djUtils bridge.</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Confirm the window shows a navigation interface with three distinct areas: Analyser, Convertir, Tagger (FOUND-01). Click each — the visible view changes.
    3. Click "Choisir un dossier" — the native OS directory dialog opens (FOUND-02). Select any folder; its path is displayed in the UI.
    4. Quit the app fully and run `npm run dev` again — the previously selected folder is still shown without re-selecting (FOUND-03).
  </how-to-verify>
  <resume-signal>Type "approved" if all three success criteria pass, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → window.djUtils (preload) | The renderer holds no Node access; every privileged action goes through the typed contextBridge API defined in Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04 | Elevation of Privilege | renderer components | mitigate | Components and the Zustand store call only window.djUtils.* — no direct electron/Node import in the renderer (RESEARCH anti-pattern). Enforced by grep in acceptance criteria. |
| T-1-02 | Tampering | folder path flowing renderer→main | mitigate | Path originates from OS-validated showOpenDialog; main-side type-check (Plan 01) is the authoritative guard. Renderer never constructs filesystem paths. |
</threat_model>

<verification>
- `npm run build` green.
- `npm run test -- --run` green (store transitions + App render/nav-switch).
- Manual launch (checkpoint): three tools visible + switchable, native dialog opens, folder persists across relaunch.
- grep: no react-router import; no window.djUtils inside component files (store mediates).
</verification>

<success_criteria>
- FOUND-01: navigation shows Analyser/Convertir/Tagger and switches between them.
- FOUND-02: folder-picker control opens the native dialog and shows the chosen path.
- FOUND-03: selected folder persists and is shown after a real relaunch.
- Walking skeleton complete: renderer ↔ bridge ↔ main ↔ SQLite proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
